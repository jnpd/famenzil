# Q347F 12寸 Class150——第7J步：连接盘Z坐标 / F25接口面 / M12×50与键轴站位 V40

> **定位**：V39已关闭F25连接盘的径向接口，但把 `φ17.5` 直接写成ADAPTER孔存在过度指定。V40先纠正孔身份，再把连接盘放回V35/V11的Z轴内轨：填料压紧面 → 连接盘 → F25接口面 → φ60键轴 → 18×90键。
>
> **核心结论**：当前连接盘底面可与填料压紧/20寸成熟连接盘下端功能面重合，取 `Z_ADAPTER_BOTTOM_CAD≈+313.3`；24mm厚度给出 `Z_ADAPTER_TOP_CAD=Z_F25_INTERFACE_CAD≈+337.3`。20寸成熟结构中“键从连接盘上方约4mm开始”的无量纲映射给 `Z_KEY_START_CAD≈+339.8`，90mm键结束约 `+429.8`。同时F25连接孔改成互补模式：一侧M16×24级螺纹锚固，另一侧φ17.5通孔+螺母，锚固究竟属于ADAPTER还是蜗轮由厂家图最终决定。

[← 总导航](./00_Q347F_12in_Class150_文档导航_从这里开始.md)  
[← 数字化总装骨架总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)  
[← V35 上下盖完整Z向纵剖面](./Q347F_12in_Class150_第7E步_上下盖完整Z向纵剖面_M12长度反校核_V35.md)  
[← V39 F25连接盘](./Q347F_12in_Class150_第7I步_F25连接盘_8xM16反力矩_M16x65轴向闭合_V39.md)

---

# 1. 先纠正V39：F25同时存在螺纹孔和通孔口径

ISO 5211:2026 F25当前接口表包含：

```text
thread connection = M16
normal clearance hole = φ17.5
PCD = φ254
n = 8
```

本项目BOM：

```text
M16×65双头螺柱 ×8
M16螺母 ×8
```

这说明每根连接件只有一个外部螺母，当前最合理拓扑是：

```text
一端旋入某一侧M16螺纹孔
↓
穿过另一侧φ17.5通孔
↓
外部M16螺母夹紧
```

但是没有蜗轮厂家图时，不能确定：

```text
方案A：ADAPTER=M16螺纹孔，GEARBOX=φ17.5通孔
方案B：GEARBOX=M16螺纹孔，ADAPTER=φ17.5通孔
```

因此V39旧：

```text
ADAPTER直接固定8×φ17.5
```

降为：

```text
H/C-too-specific
```

永久变量改成：

```text
F25_THREADED_SIDE_FINAL=?
F25_CLEAR_SIDE_FINAL=?
```

---

# 2. F25的24mm螺纹深度非常关键

ISO 5211 F25的M16螺纹接口轴向要求为24mm级。

成熟执行器/齿轮箱厂家F25接口也常直接标：

```text
8×M16×24 DEEP
ON φ254 PCD
```

所以当前建立：

```text
F25_THREAD_DEPTH_MIN_CAD=24 mm
```

状态：`STD/C+ interface`。

这与当前连接盘厚度：

```text
ADAPTER_T_CAD=24
```

数值完全一致。

如果最终M16螺纹孔开在连接盘上：

```text
24mm厚连接盘
→ 可做贯穿/全厚M16螺纹
→ 满足24mm级有效螺纹深度空间
```

如果最终螺纹孔在蜗轮箱上：

```text
连接盘改φ17.5通孔
24mm厚度仍保持作为结构厚度候选
```

所以24mm连接盘不会因为孔模式切换而失效。

---

# 3. 连接盘底面由20寸成熟装配关系关闭

20寸原生总装：

```text
填料：Y≈478.5~483.5
连接盘：Y≈483~521
```

二者在：

```text
Y≈483
```

附近直接衔接。

当前12寸V11内轨已经得到：

```text
填料压紧面 / PACK_PRESS_FACE≈+313.3
```

因此V40把：

```text
连接盘下安装面
```

直接挂在：

```text
Z_ADAPTER_BOTTOM_CAD=+313.3 mm
```

状态：`C+ topology / C coordinate`。

这比把连接盘强行挂在V34的“前盖外法兰粗参考+300”更合理，因为：

```text
前盖外法兰 = 外半径轨
填料/连接盘 = 中心内轨
```

两个轨道可以在Z向嵌套，不要求端面相同。

---

# 4. 24mm连接盘给出F25接口面绝对Z

```text
ADAPTER_T_CAD=24
```

所以：

```text
Z_ADAPTER_TOP_CAD
=313.3+24
=+337.3 mm
```

连接盘上表面就是当前执行器安装接口基准面：

```text
Z_F25_INTERFACE_CAD=+337.3 mm
```

状态：

```text
Z_ADAPTER_BOTTOM_CAD=313.3 C
Z_ADAPTER_TOP_CAD=337.3 C
Z_F25_INTERFACE_CAD=337.3 C
```

最终厂家图若改变连接盘厚度，只需要更新：

```text
ADAPTER_T_FINAL
```

并由方程式重算顶面，不影响BODY主骨架。

---

# 5. 4×M12×50对连接盘Z位置形成独立反校核

当前：

```text
STEM_COVER → ADAPTER
=4×M12×50
```

若第一版CAD把M12螺钉头支承面放在：

```text
Z≈+337.3
```

向下50mm：

```text
Z_tip≈+287.3
```

连接盘自身厚24：

```text
337.3-313.3=24
```

所以螺钉进入下方STEM_COVER的轴向预算：

```text
50-24=26mm
```

当前：

```text
TOP_ADAPTER_THREAD_ENGAGEMENT_SPACE_CAD≈26mm
```

约为：

```text
2.17×M12直径
```

说明M12×50与“24mm连接盘 + 下方前盖”的装配链完全可行。

最终仍需确认孔是：

```text
ADAPTER通孔+STEM_COVER螺纹
```

还是其他厂家/原生结构方向。

---

# 6. 键从连接盘上表面上方约2.5mm开始

20寸成熟总装：

```text
连接盘上端 Y≈521
平键开始 Y≈525
```

差：

```text
4mm
```

按公司名义球径比例：

```text
4×465/748
≈2.49mm
```

所以当前12寸：

```text
KEY_START_OFFSET_FROM_F25≈2.5mm
```

得到：

```text
Z_KEY_START_CAD
=337.3+2.5
≈+339.8mm
```

这与V11历史：

```text
KEY_START≈+339.5
```

几乎完全一致。

因此升级：

```text
Z_KEY_START_CAD≈+339.8 C+
```

---

# 7. 90mm键给出键末端Z

BOM：

```text
KEY_L=90
```

所以：

```text
Z_KEY_END_CAD
=339.8+90
=+429.8mm
```

当前：

```text
Z_KEY_END_CAD≈+429.8 C
```

键轴本体必须至少覆盖这段功能长度。

因此第一版SolidWorks：

```text
Z_STEM_KEY_SHAFT_START_CAD<=339.8
Z_STEM_KEY_SHAFT_END_CAD>=429.8
```

不要让φ60轴段比键短。

---

# 8. 阀杆总长现在能关闭到CAD上端，但不能关闭最终总长

当前上端功能：

```text
KEY_END≈+429.8
```

所以第一版阀杆上端最低包络：

```text
Z_STEM_TOP_MIN_CAD≈+430mm
```

为了草模不让键悬空，当前可取：

```text
Z_STEM_TOP_CAD=+430mm C-space
```

但：

```text
STEM_TOTAL_LEN_FINAL=?
```

仍保持D，因为阀杆下端球体驱动头的最终绝对端面、上端厂家留量/端部螺孔仍未制造冻结。

---

# 9. 当前完整上部Z链

```text
                         +Z

阀杆/键最低上端包络       ≈ +430.0 C-space
18×11×90键结束             ≈ +429.8 C
18×11×90键开始             ≈ +339.8 C+
F25执行器接口面            = +337.3 C
连接盘上表面               = +337.3 C
连接盘下表面               = +313.3 C
填料压紧面                 ≈ +313.3 C
填料/双O圈/导向内轨        ≈ +253~+313
BODY—STEM_COVER安装面      ≈ +264.5（外半径轨）
φ105定位Boss肩面           ≈ +226.9
上主轴承                    +193.6~+223.6
球心                         0
```

注意：

```text
+264.5 BODY安装面
```

和：

```text
+313.3连接盘底面
```

不冲突——前盖是中空多级同轴结构，中心阀杆/填料轨道可以穿过外法兰继续向上。

---

# 10. F25孔模式SolidWorks做成配置切换

建议在：

```text
10_TOP_ADAPTER.SLDPRT
```

建立两个临时配置：

```text
CFG_F25_ADAPTER_THREADED
→ 8×M16 threaded, depth>=24

CFG_F25_ADAPTER_CLEAR
→ 8×φ17.5 thru
```

当前默认可以先用：

```text
CFG_F25_ADAPTER_THREADED
```

仅为了符合常见“阀门/连接盘侧螺纹锚固、执行器侧通孔”的装配习惯。

状态：

```text
C-space
```

不是制造冻结。

厂家图到位后删除另一配置。

---

# 11. 对V39的正式纠错

V39继续有效：

```text
F25 OD>=300
PCD254
8孔
22.5°起始
M16×65轴向预算
24mm连接盘
```

V39需要修正：

```text
“连接盘一定8×φ17.5通孔”
→ H/C-too-specific
```

当前：

```text
M16 threaded side=? D
φ17.5 clearance side=? D
```

两侧必须互补。

---

# 12. V40 SolidWorks变量块

```text
# ADAPTER Z DATUMS
Z_ADAPTER_BOTTOM_CAD=313.3
ADAPTER_T_CAD=24
Z_ADAPTER_TOP_CAD=337.3
Z_F25_INTERFACE_CAD=337.3

# STEM_COVER -> ADAPTER
TOP_ADAPTER_SCREW_SIZE=M12
TOP_ADAPTER_SCREW_L=50
TOP_ADAPTER_SCREW_QTY=4
TOP_ADAPTER_THREAD_ENGAGEMENT_SPACE_CAD=26
TOP_ADAPTER_BOLT_PCD_FINAL=?

# F25 HOLE MODE
F25_THREAD_SIZE=M16
F25_THREAD_DEPTH_MIN_CAD=24
F25_CLEAR_HOLE_D=17.5
F25_THREADED_SIDE_FINAL=?
F25_CLEAR_SIDE_FINAL=?
CFG_F25_ADAPTER_THREADED=C-space
CFG_F25_ADAPTER_CLEAR=C-space

# KEY / STEM Z
KEY_START_OFFSET_FROM_F25=2.5
Z_KEY_START_CAD=339.8
KEY_L=90
Z_KEY_END_CAD=429.8
Z_STEM_TOP_MIN_CAD=430
Z_STEM_TOP_CAD=430
STEM_TOTAL_LEN_FINAL=?
```

---

# 13. V40当前结论

上部驱动链已经第一次拥有完整绝对Z：

```text
PACK_PRESS_FACE≈313.3
↓
ADAPTER_BOTTOM=313.3
↓ 24mm
F25_INTERFACE=337.3
↓ 2.5mm
KEY_START=339.8
↓ 90mm
KEY_END=429.8
```

同时M12×50和20寸成熟装配关系都对这条链形成独立支持。

---

# 14. 下一步 V41

下一步做第一版完整总装骨架核对：

```text
X方向：-305~+305
Z方向：底盖~-289 → 驱动键轴~+430
径向：主中法兰φ562.5 / F25φ300 / BODY中央φ504
```

统一叠加：

```text
BALL
SEAT×2
BODY
BODY_COVER
STEM_COVER
BOTTOM_COVER
STEM
ADAPTER
F25 gearbox placeholder
```

检查：

```text
空间干涉
同轴关系
装配路径
主拆装路径
维修拆卸路径
F25与主中法兰/前盖附件是否碰撞
```

> **V40一句话结论：连接盘现在不再漂在空间里——它的底面+313.3、F25面+337.3、键起点+339.8、键终点+429.8已经形成连续的上部Z轴数字骨架。**