# 12寸固定球阀 5项并行计算执行表

本文件按 `12Q347F-150LB`、DN300、Class150、固定球阀、天然气、-46~200°C 的当前项目输入建立。它是第一轮计算执行表，不替代正式受控标准、材料证书、零件图和试验报告。

## 0. 统一输入

| 输入 | 当前值 | 状态 | 用途 |
|---|---:|---|---|
| 设计压力 | 38°C时2.00 MPa | 设计点 | 承压和密封初算 |
| 设计温度 | -46~200°C | 已知 | 温压包络和材料适用性 |
| 介质 | 天然气 | 已知 | 密封、NACE和防火要求 |
| 全通径 | 303 mm | API 6D表1/模型候选 | 流道、球孔和受压面积 |
| 球体外径 | 约465 mm | 模型候选 | 阀座和旋转间隙 |
| 阀座支承圈外径 | 约362.107 mm | 模型参考 | 座腔配合 |
| 阀体座腔 | 约364.595 mm | 模型参考 | 座腔配合 |
| 阀杆工作扭矩 | 1800 N·m | 暂定输入 | 阀杆、轴和驱动初算 |
| 阀杆有效计算直径 | 60 mm | 暂定输入 | 纯扭初算 |
| 预紧比压 | 2.5 MPa | 企业固定球阀规范参考 | 阀座弹簧初算 |
| 球体材料候选 | A182 F316 | BOM候选 | 低温/NACE/表面处理待确认 |
| 阀体材料候选 | A216 WCB；低温改A352 LCB | 项目输入 | -46°C冲击和MTC待确认 |

计算状态只使用三种结果：

- `PASS`：输入、公式、标准和证据均已闭合。
- `PENDING`：可以完成初算，但还缺正式输入或验收条件。
- `FAIL`：已知输入与要求明确冲突。

当前五项均只能形成“第一轮初算”；不能据此冻结制造尺寸。

## 1. 温压等级和材料依据

### 1.1 先算压力等级

本项目的 `2.00 MPa` 是 **38°C设计点压力**，不是自动覆盖-46~200°C全温区的同一个压力值。先只在38°C设计点做比较：本地固定球阀计算书的 Class150 基准压力为 `1.96 MPa`。

```text
超出量 = 2.00 - 1.96 = 0.04 MPa
超出比例 = 0.04 / 1.96 = 2.04%
```

因此，在正式温压表未确认前，38°C设计点暂时保留为核对项：

```text
38°C附近：PENDING，2.00 MPa高于1.96 MPa候选基准值2.04%。
```

200°C是另一个温度工况。200°C时应查项目采用的Class150压力-温度额定表，或者由项目给出200°C实际工作/设计压力；不能把38°C的2.00 MPa直接套到200°C。如果200°C实际压力低于该温度的Class150允许压力，则可以按实际压力计算；如果仍要求200°C也承受2.00 MPa，则需要重新评估压力等级或材料。

指南中记录的200°C Class150 LCB公开参考值约为 `1.38 MPa`，这里只能作为风险提醒，不能代替项目正式温压表。只有在项目确认200°C仍要求2.00 MPa时，才需要做下面的比较：

```text
2.00 / 1.38 = 1.449
```

也就是2.00 MPa约超过200°C参考额定值44.9%。所以在没有正式温压表之前，不能把200°C工况按2.00 MPa直接放行。

试验压力先保留为候选值：

```text
壳体试验候选 = 1.5 × 2.00 = 3.00 MPa
密封试验候选 = 1.1 × 2.00 = 2.20 MPa
```

这两个数必须由项目采用的API 6D/API 598版本和试验规程批准，不能只因为比例算出来就冻结。

### 1.2 材料逐项判定

| 零件 | 当前候选 | 本轮结论 | 必须补的证据 |
|---|---|---|---|
| 阀体 | A216 WCB；-46°C工况暂按A352 LCB | PENDING | LCB牌号、-46°C冲击、MTC、设计温度许用应力 |
| 阀盖 | A216 WCB | PENDING | -46°C承压材料确认，可能需要低温铸钢/锻钢材料、MTC和许用应力 |
| 球体 | A182 F316 | PENDING | 热处理、低温/NACE、表面处理、许用应力 |
| 阀杆 | A182 F51 | PENDING | 热处理、硬度、NACE、-46°C和200°C许用应力 |
| 底盖 | A182 F51 | PENDING | 低温/NACE和受控材料表 |
| 阀座 | DEVLON | PENDING/风险 | 当前企业资料给出的DELVON温度范围约为-29~120°C，不能覆盖-46~200°C；必须换材或取得厂家批准 |
| 弹簧 | Inconel X-750 | 候选可用 | 弹簧计算、低温、高温、NACE和疲劳数据 |
| O形圈 | VITON/VITON AED | PENDING/风险 | 具体牌号、硬度、-46~200°C温度范围、天然气兼容性和防爆性能 |
| 填料/防火绳 | 柔性石墨 | PENDING | 低泄漏、防火等级、压紧量和证书 |
| 螺柱/螺母 | A193 B7M/A194 2HM | PENDING | 低温冲击、硬度、NACE和MTC |

**第1项结论：** `38°C按2.00 MPa`，`200°C按对应温压表/实际压力`，两者分开计算。LCB问题来自`-46°C低温承压要求`，不是因为200°C。阀座和密封材料仍需确认是否覆盖-46~200°C。

## 2. 阀体壁厚及法兰

### 2.1 流道先做标准下限

API 6D本地整理表对NPS12/DN300、150/300/600级全径阀门给出的最小孔径是 `303 mm`。当前流道为 `303 mm`，所以：

```text
d_model = 303 mm
d_min   = 303 mm
d_model - d_min = 0 mm
```

这是“满足最小孔径数值”的初步结果，但没有加工公差和圆度余量，最终图纸不能简单按303.000 mm锁死。

### 2.2 圆筒承压壁厚理论下限

先用内压圆筒公式做筛选，不把它当作阀体最终壁厚：

```text
t = P × R / (S × E - 0.6P)
R = Di / 2 = 303 / 2 = 151.5 mm
```

按指南中的第一轮参考许用应力计算：

| 工况 | P | S | E | 理论t | 若加3 mm腐蚀/设计裕量 |
|---|---:|---:|---:|---:|---:|
| 低温/基准参考 | 2.00 MPa | 137 MPa | 1.00 | 2.23 mm | 5.23 mm |
| 高温保守参考 | 2.00 MPa | 79 MPa | 1.00 | 3.89 mm | 6.89 mm |
| 低温铸件敏感性 | 2.00 MPa | 137 MPa | 0.85 | 2.63 mm | 5.63 mm |
| 高温铸件敏感性 | 2.00 MPa | 79 MPa | 0.85 | 4.59 mm | 7.59 mm |

这里的 `E=0.85` 只是铸件/质量系数敏感性示例，不是最终取值。阀体最终厚度还必须满足：

1. ASME B16.34最小壁厚或项目受控壁厚要求；
2. 流道开孔、阀杆孔、下轴孔造成的局部削弱；
3. 座腔外圆、法兰根部和中腔过渡的局部应力；
4. 铸造质量、腐蚀裕量、加工余量和低温冲击要求；
5. 2 MPa或最终批准温压包络下的变形限制。

因此，`5.23~7.59 mm`只是压力理论下限，不能把阀体设计成这个厚度。当前模型的结构壁厚需要从剖面和局部应力重新确认。

### 2.3 法兰初算输入

当前12寸Class150 RF候选接口为：

| 项目 | 候选值 | 当前处理 |
|---|---:|---|
| 法兰外径 | 485 mm | 按ASME B16.5候选，待正式版本复核 |
| 法兰厚度 | 30.2 mm，不含RF | 待正式版本复核 |
| 螺栓中心圆 | 431.8 mm | 与模型基线一致，待图纸复核 |
| 螺栓孔 | 12×φ25.5，模型约φ25.4 | 待正式版本复核 |
| RF直径 | 381 mm | 待正式版本复核 |
| RF高度 | 约2 mm | 待正式版本复核 |

模型右端外径约 `482.187 mm`，与485 mm候选差 `-2.813 mm`。这说明模型不能直接当成标准法兰尺寸放行，需要用正式法兰表和剖面图核对。

**第2项结论：PENDING。** 通径下限已对上；压力理论壁厚可算，但最终壁厚和法兰承载仍缺标准最小壁厚、局部几何和材料许用值。

## 3. 阀盖、底盖、螺栓

### 3.1 先算压力分离力

先按垫片内径作为受压直径，计算压力把盖板顶开的理论分离力：

```text
A = π × Di² / 4
F = P × A
```

| 部位 | 垫片规格 | 受压直径Di | P=2.00 MPa分离力 | P=3.00 MPa候选试验分离力 |
|---|---|---:|---:|---:|
| 前盖 | φ115×φ105×3.2 | 105 mm | 17.32 kN | 25.98 kN |
| 底盖 | φ80×φ70×3.2 | 70 mm | 7.70 kN | 11.55 kN |

### 3.2 按现有螺栓数量分摊压力载荷

前盖当前为 `8×φ14`，底盖当前为 `6×φ14`。这里只算压力分离力，不包含垫片座压力和预紧损失：

| 部位 | 数量 | P=2.00 MPa每个螺栓压力分量 | P=3.00 MPa每个螺栓压力分量 |
|---|---:|---:|---:|
| 前盖 | 8 | 2.16 kN/个 | 3.25 kN/个 |
| 底盖 | 6 | 1.28 kN/个 | 1.92 kN/个 |

实际螺栓预紧力必须满足：

```text
总预紧力 ≥ 压力分离力 + 垫片最小座压力 × 垫片有效面积
```

还要检查螺柱拉应力、螺纹剪切、孔边距、盖板弯曲和法兰转角。由于目前缺少垫片压紧参数、盖板有效跨距、支承方式和材料许用应力，不能用压力分离力直接定板厚。

### 3.3 当前几何候选

| 部位 | 当前候选 | 结论 |
|---|---|---|
| 前盖 | 外径233；PCD191；8×φ14；4×φ20×13；2×φ10定位孔 | 可用于空间预装配，不可作为制造尺寸 |
| 底盖 | 外径188；PCD146；6×φ14；底部垫片φ80×φ70×3.2 | 可用于空间预装配，不可作为制造尺寸 |

**第3项结论：PENDING。** 压力分离力已经算出；板厚、螺栓最终规格和预紧力还要等盖板剖面、材料许用值和垫片数据。

## 4. 阀杆、下轴、轴承、扭矩

### 4.1 阀杆扭矩基线

当前输入 `T_work=1800 N·m`、设计校核倍数暂取 `2`：

```text
T_design = 2 × 1800 = 3600 N·m
τ = 16T / (πd³)
```

对有效计算直径 `d=60 mm`：

| 工况 | 扭矩 | 纯扭剪应力 | M=0时等效应力 |
|---|---:|---:|---:|
| 工作候选 | 1800 N·m | 42.44 MPa | 73.51 MPa |
| 设计校核候选 | 3600 N·m | 84.88 MPa | 147.02 MPa |

等效应力按 `σeq=√(σb²+3τ²)`，这里暂取弯曲应力 `σb=0`，所以只能作为纯扭基线。

如果只按3600 N·m并假设许用剪应力，所需实心圆直径为：

| 假设许用剪应力 | 计算直径 | 说明 |
|---:|---:|---|
| 60 MPa | 67.36 mm | 敏感性，不是最终材料许用值 |
| 80 MPa | 61.20 mm | 敏感性，不含键槽和弯矩 |
| 100 MPa | 56.81 mm | 敏感性，不含键槽和弯矩 |

所以 `Ø60` 只能保留为候选。键槽、扁方、轴肩和弯矩加入后，最小净截面通常会控制结果。

### 4.2 下轴和轴承载荷包络

全通径受压投影面积：

```text
A_bore = π × 303² / 4 = 72,106.6 mm²
F_pressure = 2 × A_bore = 144.21 kN
```

在3 MPa候选壳体试验压力下，压力载荷包络为 `216.32 kN`。这是球体、上轴、下轴和支承结构的总载荷包络，不能直接全部施加到单个轴承上。

如果暂按左右支承均分，只作敏感性参考：

```text
每个支承的压力分量 ≈ 144.21 / 2 = 72.11 kN
```

现有下部滑动轴承候选为 `φ70×φ65×50`。若把72.11 kN全部当作径向载荷，投影面积取 `65×50=3250 mm²`，则：

```text
p_bearing ≈ 72.11 / 3.25 = 22.19 MPa
```

若不均分、全部落在单个支承，敏感性值约为 `44.37 MPa`。这两个结果都只能用来筛选轴承材料和载荷路径，最终必须补充球体支承、止推垫和轴承摩擦的真实受力图。

### 4.3 驱动选型避免重复放大

当前建议按以下方式处理倍数，避免把2倍重复乘两次：

```text
T_open = 1800 N·m              工作/脱开扭矩候选
T_stem = 2 × T_open = 3600 N·m 阀杆强度校核候选
T_act  ≥ 2 × T_open = 3600 N·m 低温/高温驱动输出候选
```

拿到真实阀座摩擦、填料摩擦、轴承摩擦和压差扭矩后，再把 `1800 N·m` 替换成计算值。若实际扭矩高于1800 N·m，阀杆和蜗轮必须同步重算。

**第4项结论：PENDING。** 纯扭基线和压力载荷包络已算出；弯矩、键槽净截面、轴承允许面压、止推载荷和最终材料许用值未闭合。

## 5. 阀座、弹簧、密封

### 5.1 阀座几何初选

当前候选为：

```text
D3 = 364 mm       静态密封外径候选
D4 = 303 mm       静态密封内径候选
D1 = 333 mm       密封面外径候选
D2 = 303 mm       密封面内径候选
b  = (D1-D2)/2 = 15 mm
```

`D3=364 mm`与阀体座腔`364.595 mm`的名义直径差为：

```text
364.595 - 362.107 = 2.488 mm
径向名义间隙 = 2.488 / 2 = 1.244 mm
```

这个数只能作为模型配合参考，还要叠加制造公差、热膨胀、挤出间隙和金属防火面间隙。

### 5.2 阀座预紧力初算

按企业固定球阀规范参考的 `2.5 MPa` 预紧比压，若作用面积取静态密封环 `D3-D4`：

```text
A_seat = π/4 × (364² - 303²) = 31,955.5 mm²
F_seat = 2.5 × A_seat = 79.89 kN/每个阀座
```

BOM为两套阀座、72件弹簧，若每个阀座均布36件弹簧：

```text
F_spring ≈ 79.89 / 36 = 2.22 kN/每根弹簧
```

如果实际预紧作用面积只取密封面带 `D1-D2`，则：

```text
A_seal = π/4 × (333² - 303²) = 14,985.4 mm²
F_seal = 2.5 × A_seal = 37.46 kN/每个阀座
F_spring ≈ 37.46 / 36 = 1.04 kN/每根弹簧
```

这两个结果差异很大，说明最终必须从阀座剖面确认“2.5 MPa作用在哪个有效面积”，不能直接选2.22 kN或1.04 kN作为成品弹簧力。

### 5.3 弹簧需要的输入

现有弹簧候选为 `Inconel X-750，φ8×1.6×18×7，72件`。要把预紧力换成弹簧尺寸，至少还缺：

1. 弹簧外径/中径的准确含义；
2. 自由高度、安装高度和工作压缩量；
3. 有效圈数、端部形式和并紧高度；
4. -46°C、38°C、200°C剪切模量和许用剪应力；
5. 开关循环次数和疲劳验收条件。

公式为：

```text
F = k × δ
k = G × d_wire^4 / (8 × D_mean^3 × n_active)
```

当前只能先得到“每根弹簧目标力范围”，不能反推最终弹簧自由长度和圈数。

### 5.4 O形圈和填料初算

现有候选沟槽参数：

| O形圈 | 截面 | 槽深 | 槽宽 | 初算压缩率 |
|---|---:|---:|---:|---:|
| φ320×5.3 | 5.3 mm | 4.2 mm | 7.0 mm | 20.75% |
| φ311×3.55 | 3.55 mm | 2.8 mm | 4.8 mm | 21.13% |

公式：

```text
压缩率 = (O形圈截面直径 - 槽深) / O形圈截面直径
```

几何压缩率已算出，但材料还不能放行：

- 当前阀座DELVON候选温度范围不能覆盖-46~200°C；
- VITON/VITON AED必须补具体牌号、硬度和温度曲线；
- 天然气工况需要确认抗爆/快速减压性能；
- 防火要求不能只靠O形圈，必须配合金属防火面和石墨密封；
- 填料`φ75×φ65×5`和防火石墨绳`3×3`还需确定压紧量、层数和泄漏等级。

**第5项结论：PENDING/材料风险。** 阀座尺寸和O形圈沟槽可以做第一轮模型，但预紧力必须等剖面确认，弹簧必须等安装压缩量和材料数据，密封材料必须先解决温度范围。

## 6. 五项计算的并行安排

### 现在立即并行

1. `温压等级和材料依据`：确认2.00 MPa是否为设计压力，以及200°C额定压力。
2. `阀体壁厚及法兰`：先按303 mm、2.00 MPa做压力理论下限和法兰接口复核。
3. `阀盖、底盖、螺栓`：先算分离力、预紧力下限和螺栓载荷。
4. `阀杆、下轴、轴承、扭矩`：先用1800/3600 N·m做纯扭、压力载荷和轴承敏感性。
5. `阀座、弹簧、密封`：先按D3/D4、D1/D2和2.5 MPa做预紧力范围及O形圈压缩率。

### 必须回代的依赖

完成球体/阀座剖面后，回代：

- 阀体座腔和局部壁厚；
- 阀座有效受压面积；
- 弹簧单件目标力；
- 阀座摩擦扭矩；
- 90°旋转干涉；
- 阀杆弯矩和下轴支承载荷。

## 7. 当前总判定

| 计算包 | 第一轮结果 | 当前状态 |
|---|---|---|
| 温压等级和材料 | 38°C按2 MPa核对；200°C按对应温压表/实际压力；-46°C低温材料未闭合 | PENDING |
| 阀体壁厚及法兰 | 理论壁厚下限已算，法兰候选已列，局部强度未算 | PENDING |
| 阀盖、底盖、螺栓 | 前盖17.32 kN、底盖7.70 kN压力分离力已算 | PENDING |
| 阀杆、下轴、轴承、扭矩 | Ø60在3600 N·m下纯扭剪应力84.88 MPa；压力载荷144.21 kN包络已算 | PENDING |
| 阀座、弹簧、密封 | 预紧力范围1.04~2.22 kN/根、O形圈压缩率已算 | PENDING |

下一轮只需要补齐正式材料表、阀体/阀盖剖面、阀杆键槽、阀座剖面、弹簧安装尺寸和密封厂家数据，就可以把这些初算继续收敛为正式计算书。
