# Q347F 12寸 Class 150——第3H页：DEVLON代用牌号与V7继续设计规则

> **目的**：当前项目只有“DEVLON”材料名，拿不到完整牌号和厂家数据。为了不让整条设计链停住，本页引入一个**临时代用材料（Proxy）**，仅用于继续计算、布置和风险筛选，不等于最终采购牌号。
>
> **当前代用牌号：James Walker Devlon® V-API。** 选择理由：厂家当前官方页面明确说明该材料专为阀门/海工应用开发，并用于阀座插件。
>
> [← 返回主文档](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md) ｜ [← 第3G V6纠错](./Q347F_12in_Class150_第3G步_制造冻结校核_O形圈_DEVLON热配合_弹簧_轴向链_V6.md)

---

# 1. 状态定义：新增 P（Proxy）

```text
P = 临时代用材料/参数
```

用途：

```text
可以：继续计算、做SolidWorks草模、做敏感性分析、决定空间是否够
不可以：直接写入最终采购规范、材料证明、制造冻结图
```

一旦拿到订单真实DEVLON牌号：

```text
P参数全部替换
→ 自动重算热配合/允许温度/材料校核
→ 再决定是否冻结
```

---

# 2. 为什么临时选 Devlon V-API

James Walker当前官方产品资料说明：

- Devlon V-API 是专门为阀门行业开发的专有配方；
- 典型用途包括 valve seat inserts；
- 当前官方产品页给出的工作温度能力约为 `-50℃ ～ +176℃`。

所以当项目只写“DEVLON”而没有完整牌号时，V-API比普通尼龙或其它Devon系列更适合作为**阀座设计代理材料**。

但是本项目公司规范 `GFE-JS01-2025` 对“DEVLON”给出的企业适用温度仍是：

```text
-29℃ ～ +120℃
摩擦系数 μ = 0.08
```

因此V7规定：

> **计算温度包络仍以公司 -29～120℃ 为控制范围，不用 V-API 的 -50～176℃ 去覆盖公司规范。**

---

# 3. V7临时代用材料参数表

下列数值用于“继续设计”，不是最终采购规范。

| 参数 | V7临时值 | 状态/用途 |
|---|---:|---|
| 代用牌号 | James Walker Devlon V-API | P |
| 公司控制温度 | -29～120℃ | A；公司规范优先 |
| 厂家当前公开产品温度能力 | -50～176℃ | 外部交叉，不作为项目控制 |
| 摩擦系数 μ | 0.08 | A；公司规范 |
| 线膨胀系数 α，30～100℃ | 1.11×10^-4 /℃ | P；公开V-API技术数据 |
| 弹性模量 E | 约4.14 GPa | P |
| 抗压强度 | 约140 MPa | P；不是许用应力 |
| 抗压屈服强度 | 约91.2 MPa | P；不是许用应力 |
| 硬度 | Shore D 78～80 | P |
| 密度 | 约1.14 g/cm³ | P |
| 24h吸水率 | 约0.105% | P |
| 饱和吸水率 | 约3% | P；尺寸稳定性风险项 |

重要：

```text
抗压强度140 MPa
≠ 设计允许比压自动等于140 MPa
```

公司固定球阀计算程序/计算书的密封比压判据继续按公司口径执行；材料代理数据只做结构和热配合交叉。

---

# 4. 热膨胀敏感性：V7继续怎么算

F316金属支承圈取公开典型热膨胀系数：

```text
α316 ≈ 16.5×10^-6 /℃
```

DEVLON代理：

```text
αdev ≈ 111×10^-6 /℃
```

差值：

```text
Δα ≈ 94.5×10^-6 /℃
```

以当前DEVLON物理外径草模：

```text
OD ≈ φ331 mm
装配参考温度 T0 = 20℃
```

## 4.1 从20℃到+120℃

```text
ΔT = +100℃

相对F316的自由直径差：
ΔD ≈ Δα × D × ΔT
    ≈ 94.5×10^-6 × 331 × 100
    ≈ 3.13 mm
```

## 4.2 从20℃到-29℃

```text
ΔT = -49℃

相对F316的自由直径差：
ΔD ≈ 94.5×10^-6 × 331 × 49
    ≈ 1.53 mm
```

这两个数的正确理解不是“槽必须放3.13mm间隙”，而是：

> **不能再用0.1～0.2mm这种刚性紧套思路去冻结DEVLON保持结构。**

实际阀座插件是被金属结构捕获、承压、受球面接触和防挤出约束的，最终间隙必须由真实牌号、保持方式、压力、温度和制造公差一起决定。

---

# 5. 为了让整体链路继续，V7现在怎么画

## 5.1 DEVLON实体先保留“功能包络”

SolidWorks继续画：

```text
材料代理        Devlon V-API（备注：PROXY）
球侧工作面      R232.5
真实接触带      D9≈φ323.88 ～ D10≈φ327.13
物理ID草模      ≈φ323.8
物理OD草模      ≈φ331
轴向宽草模      ≈10 mm
```

但以下尺寸不要与金属硬绑定：

```text
DEVLON-metal radial fit = *_PENDING
DEVLON axial retaining clearance = *_PENDING
```

也就是说：

> **先画出“它占多大空间、在哪工作”，暂时不冻结“它到底紧多少/松多少”。**

## 5.2 第二O圈 φ311×3.55

规格仍然保留：

```text
VITON φ311×3.55
```

但因为它真实位置尚未从成熟12寸结构确认：

```text
O_RING_311_POSITION_PENDING
```

V7不再让这个槽阻塞后续阀杆、阀体、阀盖设计。

## 5.3 主O圈可以继续用

当前主O圈链继续保留：

```text
主O圈              φ320×5.3
D11导向OD           φ342
槽底OD              φ333.6
阀盖导向孔          φ342.4候选
```

这一链已经与公司20寸成熟结构的有效压缩量交叉验证，因此继续作为 C+ 草模链。

---

# 6. V7之后，哪些尺寸足够驱动整阀继续设计

阀体/阀盖真正需要的主要是“外包络和功能接口”，不是DEVLON最后0.x mm配合。

所以目前已经够用的接口是：

```text
流道                    φ303
球体                    φ465 / R232.5
阀座真实接触带          φ323.88～φ327.13
D11主活塞导向          φ342
主O圈导向孔            φ342.4候选
弹簧PCD                 φ362
支承圈大端              φ380候选
阀盖大端座孔            φ382候选
Wseat总空间包络         ≈58 mm
防火金属面正常间隙      0.9 mm候选
```

因此：

> **DEVLON牌号拿不到，不再阻塞第4步“阀杆/下支承/滑动轴承”以及第5～6步内部空间、阀体/阀盖主腔设计。**

---

# 7. V7需要保留的红线

以下事情暂时禁止：

```text
1. 在采购/BOM里把DEVLON正式改成“V-API”
2. 把-50～176℃写成项目设计温度
3. 把抗压强度140MPa直接当设计许用应力
4. 用3.13mm热膨胀差直接拍一个槽间隙
5. 冻结φ311×3.55第二O圈的真实位置
6. 冻结DEVLON保持槽/燕尾/压环尺寸
```

---

# 8. 当前结论

V7采取：

```text
真实材料：DEVLON（具体牌号未知）

临时代用材料：James Walker Devlon V-API
↓
公司温度范围 -29～120℃ 继续做控制
↓
用V-API典型CTE/力学数据做热配合和空间敏感性
↓
不冻结DEVLON最终保持间隙
↓
保留阀座外包络
↓
整阀设计继续向第4步推进
```

这是一种“**不断链，但不伪造最终材料**”的处理方法。

---

# 9. 下一步

现在不再等DEVLON资料，直接进入：

```text
第4步
球体压力载荷
→ 上/下支承反力
→ 阀杆候选φ60正式校核
→ 下支承轴
→ 滑动轴承
→ 回代球体上下接口
```

DEVLON真实牌号以后拿到，再回到本页替换 P 参数即可。
