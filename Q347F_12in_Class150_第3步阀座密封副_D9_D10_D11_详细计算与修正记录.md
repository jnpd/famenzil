# Q347F 12寸 Class 150——第3步阀座密封副 D9 / D10 / D11 详细计算与修正记录

> **定位**：本文件是主设计文档“第3步阀座密封副”的展开计算页。主文档只保留当前结论、状态和下一步；所有公式、来源、修正原因和依赖关系在这里展开。  
> **返回主线**：[← 返回《Q347F 12寸 Class150 球体设计计算底稿——从内到外设计主线》](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md)

---

# 1. 为什么单独拆这一页

这一步是整台固定球阀设计里非常关键的接口：

```text
球体 φ465 / 流道 φ303
        ↓
球面密封几何
        ↓
D9 / D10
        ↓
弹簧预紧 + 介质压力活塞力
        ↓
必须比压 / 实际工作比压
        ↓
D11 静压面外径
        ↓
33%中腔自泄压
        ↓
O形圈 + K1/K2/K3/K4
        ↓
DEVLON截面 / 金属阀座圈 / 阀座总宽
        ↓
阀体座腔
```

这里一旦把“密封面”“静压面”“流道”“阀座外圆”混成一个直径，后面阀座推力、扭矩、自泄压和座腔都会跟着错。

因此，本页采用公司规范术语，并把旧试算全部重新分类。

---

# 2. 本页依据优先级

本页按以下优先级使用资料：

```text
① 当前项目正式输入
② GFE-JS01-2025《BS系列固定球阀设计规范》
③ 公司提供的固定球阀计算书 Excel
④ 人工复算
⑤ 历史模型 / 旧BOM仅交叉检查
```

## 2.1 公司规范直接给出的规则

`GFE-JS01-2025` 对本步骤明确规定：

```text
1. 根据球体结构尺寸初步确定密封面内外径 D9、D10
2. 通过必须比压计算，确定阀座静压面外径 D11
3. 通过固定球阀计算程序确定最终尺寸
4. K1 = 2～3
   K2 = O形圈槽宽度
   K3 = 3～5
   K4 = 4～6
5. 中腔压力超过介质压力33%时，阀座应具有自泄压作用
6. ≥2"固定球阀采用圆柱弹簧预紧，预紧比压规定为2.5 MPa
```

公司规范还给出：

```text
DEVLON
适用压力等级：150LB～1500LB
适用温度：-29～120℃
摩擦系数：0.08
```

因此：

- `qpre = 2.5 MPa` 是公司现行设计规范明确值；
- `D11` 必须由计算得到，不能靠“往外加几毫米”确定；
- DEVLON 对 Class150 的压力等级适用，但项目温度如果超出 `-29～120℃`，材料适用性必须单独闭合。

## 2.2 公司固定计算书实际公式

本次提供的固定球阀计算书工作表：

```text
TMBV Calculation
```

关键单元格公式如下：

```text
E16  qmf = ROUND(MAX(0.1×P, 2), 0)
E17  Fc  = 0.25π(D1²-D2²)×qmf
E23  Q2  = Fsd×Ns
E24  Q1  = 0.25π(D3²-D4²)×P
E26  Qm  = Q1+Q2
E27  qm  = Qm / [0.25π(D1²-D2²)]
E31  m   = IF(Class<900, 1.4, 1.8)
E32  a   = Viton→0.4；NYLON→1.8；其他→3.5
E33  c   = Viton→0.6；NYLON→0.9；其他→1
E34  qb  = m(a+cP) / √[(D1-D2)/2]
E45  验收：qb ≤ qm ≤ [qm]
E46  ΔP1 = 1.33Q2 / [0.25π(D3²-D4²)]
```

计算书材料允许比压列表还明确列有：

```text
DEVLON [qm] = 140 MPa
```

> **重要**：这份 Excel 的 `a/c` 公式没有单独识别 `DEVLON`。它只识别 `Viton`、`NYLON`，其他材料会落到第三组系数。因此，DEVLON 的 `a=1.8、c=0.9` 不能写成“Excel明确规定”，只能作为当前V1按聚酰胺/尼龙组进行的临时映射，必须保留待公司确认标记。

---

# 3. 公司术语与固定计算书符号映射——本轮关键修正

公司规范和固定计算书使用了两套符号。重新对照 `GFE-JS01-2025` 第6～7页、第11页 `fig 5` 与固定计算书后，当前采用下面的映射：

| 物理意义 | 公司规范 | 固定计算书 | 当前项目 |
|---|---|---|---|
| 密封面内径 | D9 | D2 | D9 |
| 密封面外径 | D10 | D1 | D10 |
| 阀座静压面外径 | D11 | D3 | D11 |
| 静压面内边界 / 通道直径 | d | D4 | d |

因此：

```text
Excel D2 → 公司 D9
Excel D1 → 公司 D10
Excel D3 → 公司 D11
Excel D4 → 公司 d
```

## 3.1 为什么这次把 `D4=D9` 改掉

上一版曾根据示例计算书中：

```text
D4 = D2 = 51 mm
```

临时假设：

```text
D4 = D9
```

这个假设现在撤销。

原因有两个：

1. 公司规范第6页明确把 `d` 定义为“通道直径”，来源 API 6D；
2. 公司第11页 `fig 5` 同时画出 `φD11、φd、φD9、φD10` 四个不同符号，说明 `d` 与 `D9` 不是同一设计量。

公司提供的示例计算书是 `2 × 1.5` 的缩径阀，`D4=51 mm` 而其阀孔输入为38 mm，不能把“示例里 D4 恰好等于 D2”推广成所有规格的通用规则。

本项目为全通径 NPS12，因此当前人工复算统一采用：

```text
d = 303 mm
```

作为静压环内边界。

> **这是本轮最重要的修正。它会直接把 D11 的自泄压下限从上一版约φ359.5降到约φ339.5。**


# 4. 当前项目输入

当前第3步统一输入：

```text
公称尺寸：NPS 12 / DN300
压力等级：Class 150
设计压力：P = 2 MPa
球体材料：ASTM A182 F316
球体外径：Db = φ465 mm
球体半径：Rb = 232.5 mm
流道：d = φ303 mm
密封斜面角：α = 45°
实际斜面密封带：w = 4～6 mm
V1取：w = 5 mm
阀座材料：DEVLON
```

---

# 5. 第一步：由球体求密封接触中心 Dc

45°锥形密封面与球面接触时，第一轮几何关系：

```text
Dc = Db × cos45°
```

代入：

```text
Dc = 465 × 0.70710678
   ≈ 328.80 mm
```

所以：

```text
Dc ≈ φ328.80 mm
```

状态：`B 公式计算值`。

> 前提：真实阀座剖面采用当前45°锥面几何。若后续剖面不是这个几何关系，则 D9/D10 必须重算。

---

# 6. 第二步：由 w=5 mm 求 D9 / D10

定义：

```text
w  = 沿45°斜面量取的实际密封带宽
br = 密封带在径向方向的投影宽度
```

45°时：

```text
br = w × cos45°
```

V1：

```text
br = 5 × 0.70710678
   ≈ 3.5355 mm
```

以接触中心近似对称展开：

```text
D9  = Dc - br
D10 = Dc + br
```

得到：

```text
D9  ≈ 328.80 - 3.5355
    ≈ φ325.27 mm

D10 ≈ 328.80 + 3.5355
    ≈ φ332.34 mm
```

当前V1：

```text
D9  = φ325.27 mm
D10 = φ332.34 mm
```

### 为什么 φ303 不能当 D9

```text
φ303 = 流道控制尺寸
D9   = 球面实际密封区内径
```

两者物理意义不同。

旧试算把 `333/303` 直接当密封带边界，这一做法作废。

---

# 7. 第三步：密封面积 Aseal

采用计算书相同的环形投影面积：

```text
Aseal = π/4 × (D10²-D9²)
```

代入：

```text
Aseal
= π/4 × (332.34²-325.27²)
≈ 3651.55 mm²
```

圆整：

```text
Aseal ≈ 3652 mm²
```

---

# 8. 第四步：弹簧预紧力 Q2

## 8.1 为什么不用旧Excel的 qmf 公式

旧计算书采用：

```text
qmf = max(0.1P, 2)
```

但 2025 公司设计规范已经明确：

```text
≥2" 固定球阀圆柱弹簧预紧比压 = 2.5 MPa
```

因此当前项目采用较新的公司规范：

```text
qpre = 2.5 MPa
```

旧Excel的 `max(0.1P,2)` 仅作为历史计算逻辑记录，不再作为本项目预紧比压控制值。

## 8.2 每侧阀座总预紧力

```text
Q2 = qpre × Aseal
```

代入：

```text
Q2
= 2.5 × 3651.55
≈ 9128.88 N
≈ 9.13 kN / 每侧阀座
```

状态：`B，基于公司2.5 MPa规定`。

> 弹簧数量、单根弹簧力、自由高度、工作高度、疲劳寿命尚未冻结。`Q2` 是当前需要由全部弹簧共同提供的总工作预紧力。

---

# 9. 第五步：必须比压 qb

固定计算书实际公式：

```text
qb = m(a+cP) / √[(D10-D9)/2]
```

## 9.1 流体/压力等级系数 m

Excel公式：

```text
Class < 900 → m = 1.4
Class ≥ 900 → m = 1.8
```

本项目：

```text
Class150 → m = 1.4
```

这个值不再需要按“天然气普通/高密封”人为二选一；当前按公司计算书 Class 分档。

## 9.2 DEVLON 的 a、c 当前处理

固定计算书公式分支：

```text
Viton：a=0.4，c=0.6
NYLON：a=1.8，c=0.9
其他：a=3.5，c=1
```

Excel材料允许比压表虽然列出了 DEVLON=140 MPa，但 `a/c` 公式没有 DEVLON 专用分支。

因此当前V1暂按：

```text
DEVLON → NYLON / Polyamide组

a = 1.8
c = 0.9
```

状态：`C 待公司确认材料映射`。

> 如果公司正式计算程序对 DEVLON 采用其他 a/c，下面 qb、D11 都必须重算。

## 9.3 计算 qb

径向半宽：

```text
(D10-D9)/2
= (332.34-325.27)/2
= 3.535 mm
```

代入：

```text
qb
= 1.4 × (1.8+0.9×2) / √3.535
≈ 2.6806 MPa
```

所以：

```text
qb ≈ 2.68 MPa
```

Excel通常保留一位小数时可写：

```text
qb ≈ 2.7 MPa
```

---

# 10. 第六步：实际工作密封比压 qm

固定计算书的阀座受力链为：

```text
介质压力静压推力 Q1
+
弹簧总预紧力 Q2
=
总密封力 Qm
↓
除以密封面积 Aseal
↓
实际工作密封比压 qm
```

按本轮正确符号映射：

```text
Q1 = P × π/4 × (D11²-d²)

Qm = Q1 + Q2

qm = Qm / Aseal
```

验收条件：

```text
qb ≤ qm ≤ [qm]
```

计算书允许比压表中：

```text
DEVLON [qm] = 140 MPa
```

---

# 11. 必须比压条件反算 D11

最低密封条件：

```text
qm ≥ qb
```

代入：

```text
P × π/4 × (D11²-d²) + Q2
≥ qb × Aseal
```

反算：

```text
D11,min,sealing
= √[d² + 4(qb×Aseal-Q2)/(πP)]
```

当前：

```text
d     = 303 mm
qb    ≈ 2.680 MPa
Aseal ≈ 3652.10 mm²
Q2    ≈ 9130.25 N
P     = 2 MPa
```

得到：

```text
D11,min,sealing ≈ φ303.69 mm
```

这个数学下限甚至小于当前：

```text
D10 ≈ φ332.34 mm
```

因此它不具备直接结构意义，只说明一件事：

> **当前公司规定的2.5 MPa弹簧预紧已经非常接近最低必须比压，额定压力下“最低密封能力”不是 D11 的控制条件。**

---

# 12. 33%中腔自泄压——当前 D11 控制条件

公司规范规定：

```text
当中腔压力超过介质压力33%时，
阀座要有自泄压作用。
```

固定计算书 E46：

```text
ΔP1
= 1.33Q2 / [π/4 × (D11²-d²)]
```

本项目按：

```text
ΔP1 ≤ 0.33P
```

校核。

因为：

```text
P = 2 MPa
0.33P = 0.66 MPa
```

反算：

```text
D11,min,relief
= √[
    d²
    + 4×1.33Q2 / (π×0.33P)
  ]
```

代入：

```text
d  = 303 mm
Q2 = 9130.25 N
P  = 2 MPa
```

得到：

```text
D11,min,relief ≈ φ339.46 mm
```

所以：

```text
最低密封条件：约φ303.69 → 不控制
33%自泄压条件：约φ339.46 → 当前控制
```

---

# 13. D11 当前 V1 改为 φ342 mm

为了不贴着理论边界 `φ339.46`，并为后续 O 形圈和加工留出第一轮回代空间，当前圆整：

```text
D11 = φ342 mm
```

状态：

```text
C：V1设计候选
```

这里必须记录一次正式修正：

```text
旧 V1：D11 = φ362 mm  → 作废
新 V1：D11 = φ342 mm
```

作废原因不是“优化了20 mm”，而是：

```text
上一版错误使用 D4=D9≈325.27
本版按公司 fig5 恢复 d=通道直径=303
```

---

# 14. D11=342 的完整回代校核

## 14.1 静压环面积

```text
Astatic
= π/4 × (D11²-d²)
= π/4 × (342²-303²)
≈ 19756.69 mm²
```

## 14.2 介质压力推力 Q1

```text
Q1 = P × Astatic
   ≈ 39.51 kN
```

## 14.3 总密封力 Qm

```text
Qm = Q1 + Q2
   ≈ 39.51 + 9.13
   ≈ 48.64 kN
```

## 14.4 实际工作比压 qm

```text
qm = Qm / Aseal
   ≈ 13.32 MPa
```

校核：

```text
qb   ≈ 2.68 MPa
qm   ≈ 13.32 MPa
[qm] = 140 MPa
```

所以：

```text
2.68 < 13.32 < 140
```

当前人工复算满足计算书判据。

## 14.5 自泄压压差

```text
ΔP1
= 1.33Q2 / Astatic
≈ 0.615 MPa
```

公司33%目标：

```text
0.33P = 0.66 MPa
```

所以：

```text
0.615 < 0.660 MPa
```

当前V1通过。

---

# 15. 当前最终V1结果表

| 参数 | 当前值 | 状态 | 说明 |
|---|---:|---|---|
| 通道直径 d | **φ303 mm** | A | 公司规范/API 6D控制输入 |
| Dc 接触中心 | φ328.80 mm | B | 45°球面几何 |
| D9 密封面内径 | **φ325.27 mm** | B/C | 当前V1几何 |
| D10 密封面外径 | **φ332.34 mm** | B/C | 当前V1几何 |
| Aseal | **约3652.10 mm²** | B | 环形投影面积 |
| qpre | **2.5 MPa** | A | GFE-JS01-2025 |
| Q2 | **约9.13 kN/侧** | B | qpre×Aseal |
| m | **1.4** | B/公司计算书 | Class<900 |
| a/c | **1.8 / 0.9** | C | DEVLON暂按NYLON/Polyamide组，待确认 |
| qb | **约2.68 MPa** | B/C | 公司计算书公式 + 当前材料映射 |
| D11 必须比压理论下限 | φ303.69 mm | B/C | 不控制，且小于D10 |
| D11 自泄压下限 | **φ339.46 mm** | B/C | 当前控制条件 |
| **D11 V1** | **φ342 mm** | **C** | 向上圆整 |
| D11=342 时 qm | **约13.32 MPa** | B/C | `qb<qm<[qm]` |
| DEVLON [qm] | 140 MPa | 计算书参考 | 公司提供计算书材料表 |
| D11=342 时 ΔP1 | **约0.615 MPa** | B/C | 小于0.66 MPa |

---

# 16. 这次修正了哪些旧结论

## 16.1 作废：DEVLON厚12、嵌入8、外露4

原因：

```text
公司规范没有给这种固定厚度；
计算书也没有“DN300 → DEVLON厚度”的直接公式。
```

因此：

```text
DEVLON厚度 = D 待设计
嵌入深度 = D 待设计
```

要等 D11、O形圈、K1/K2/K3/K4 和真实保持结构建立后再确定。

## 16.2 D11不是“整个金属阀座圈外径”

公司规范的液压关键直径是：

```text
D11 = 阀座静压面外径
```

固定计算书 `D3` 也是：

```text
Seat static sealing outer diameter
```

所以：

```text
计算书 D3 ↔ 公司 D11
```

但：

```text
D11 ≠ 整个金属阀座圈最终外径
```

## 16.3 正式作废：D11=φ362 的上一版V1

上一版错误链：

```text
示例Excel D4=D2
↓
错误推广为本项目 D4=D9
↓
用325.27作为静压环内径
↓
把自泄压下限推到φ359.48
↓
圆整得到φ362
```

现在根据公司规范：

```text
d = 通道直径 = φ303
```

所以：

```text
D11,min,relief ≈ φ339.46
D11 V1          = φ342
```

## 16.4 2.5 MPa仍然不是“必须比压”

```text
2.5 MPa = 公司规定弹簧预紧比压 qpre
qb      = 公司固定计算书算出的最低必须密封比压
```

---

# 17. D11 与后续 O 形圈的直接关系

本轮 `D11=φ342` 确定后，可以把历史12寸 O 形圈 `φ320×5.3` 做几何回代。

公司第16页对 `d1=5.3 mm` 的动密封沟槽给出：

```text
径向沟槽深度 t = 4.4 mm
槽宽 K2 = 7 mm
```

若把 D11 作为活塞密封的外侧配合/压力边界，则沟槽底径第一轮为：

```text
Dg
= D11 - 2t
= 342 - 8.8
= φ333.2 mm
```

历史12寸清单存在：

```text
VITON O形圈 φ320×5.3
```

装到 `φ333.2` 槽底时，内径拉伸率：

```text
εstretch
= (333.2-320)/320
≈ 4.13%
```

Parker O-Ring Handbook 对大直径活塞槽说明，装配拉伸可接近5%；其欧洲手册给出活塞槽可拉伸到约6%。因此 `4.13%` 可作为当前V1的外部交叉校核。

> 具体VITON/FKM牌号、硬度、AED/RGD性能和温度范围仍未冻结。

---

# 18. 下一步：K1/K2/K3/K4 + 阀座导向外圆 + DEVLON保持结构

当前继续顺序：

```text
D11 = φ342
↓
O形圈 φ320×5.3（V1）
↓
槽底径 φ333.2（V1）
↓
K2 = 7
K1 / K3 / K4 取规范范围内候选
↓
核阀座大端外圆与阀盖内孔的2 mm规则
↓
建立DEVLON保持槽
↓
确定 tdev / edev
↓
得到金属阀座圈最终外径和 Wseat
```

---

# 19. 仍未闭合的风险

### R1：DEVLON温度范围

公司规范普通DEVLON：

```text
-29～120℃
```

项目如果要求超出该温度范围，材料必须重新确认。

### R2：DEVLON a/c 系数映射

固定计算书没有DEVLON专用 `a/c` 分支。

当前：

```text
DEVLON → 暂按NYLON/Polyamide
a=1.8
c=0.9
```

仍需公司计算程序确认。

### R3：D11与阀盖实际配合尺寸

当前把 D11 作为活塞密封外侧压力边界用于 O 形圈第一轮回代，是基于公司 fig5 + 固定计算书物理意义的V1解释。最终二维图仍需明确：

```text
阀盖座孔实际尺寸
阀座导向外圆
沟槽底径
各自公差
```

### R4：天然气快速降压

天然气工况如存在快速降压，应确认FKM是否需要AED/RGD等级。

---

# 20. 当前一句话结论

```text
D9  = φ325.27
D10 = φ332.34
d   = φ303
qpre= 2.5 MPa
Q2  ≈ 9.13 kN/侧
qb  ≈ 2.68 MPa

D11,min,relief ≈ φ339.46
D11 V1          = φ342

回代：
qm  ≈ 13.32 MPa
ΔP1≈ 0.615 MPa

下一步：
φ320×5.3 O形圈
→ K1/K2/K3/K4
→ 阀座导向/阀盖座孔
→ DEVLON完整保持截面
```

**[→ 继续《O形圈 / K1-K4 / DEVLON截面 V1详细计算》](./Q347F_12in_Class150_第3步阀座_O形圈_K1-K4_DEVLON截面_V1详细计算.md)**

**[← 返回主设计计算文档](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md)**