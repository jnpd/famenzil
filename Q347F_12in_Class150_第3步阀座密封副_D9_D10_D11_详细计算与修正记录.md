# Q347F 12寸 Class 150——第3步阀座密封副 D9 / D10 / D11 详细计算与修正记录

> **定位**：本文件是主设计文档“第3步阀座密封副”的展开计算页。主文档只保留当前结论、状态和下一步；所有公式、来源、修正原因和依赖关系在这里展开。  
> **返回主线**：[← 返回《Q347F 12寸 Class150 球体设计计算底稿——从内到外设计主线》](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md)

---

# 1. 为什么单独拆这一页

这一步是整台固定球阀设计里非常关键的接口：

```text
球体 φ465 / 流道 φ303
        ↓
球面密封几何
        ↓
D9 / D10
        ↓
弹簧预紧 + 介质压力活塞力
        ↓
必须比压 / 实际工作比压
        ↓
D11 静压面外径
        ↓
33%中腔自泄压
        ↓
O形圈 + K1/K2/K3/K4
        ↓
DEVLON截面 / 金属阀座圈 / 阀座总宽
        ↓
阀体座腔
```

这里一旦把“密封面”“静压面”“流道”“阀座外圆”混成一个直径，后面阀座推力、扭矩、自泄压和座腔都会跟着错。

因此，本页采用公司规范术语，并把旧试算全部重新分类。

---

# 2. 本页依据优先级

本页按以下优先级使用资料：

```text
① 当前项目正式输入
② GFE-JS01-2025《BS系列固定球阀设计规范》
③ 公司提供的固定球阀计算书 Excel
④ 人工复算
⑤ 历史模型 / 旧BOM仅交叉检查
```

## 2.1 公司规范直接给出的规则

`GFE-JS01-2025` 对本步骤明确规定：

```text
1. 根据球体结构尺寸初步确定密封面内外径 D9、D10
2. 通过必须比压计算，确定阀座静压面外径 D11
3. 通过固定球阀计算程序确定最终尺寸
4. K1 = 2～3
   K2 = O形圈槽宽度
   K3 = 3～5
   K4 = 4～6
5. 中腔压力超过介质压力33%时，阀座应具有自泄压作用
6. ≥2"固定球阀采用圆柱弹簧预紧，预紧比压规定为2.5 MPa
```

公司规范还给出：

```text
DEVLON
适用压力等级：150LB～1500LB
适用温度：-29～120℃
摩擦系数：0.08
```

因此：

- `qpre = 2.5 MPa` 是公司现行设计规范明确值；
- `D11` 必须由计算得到，不能靠“往外加几毫米”确定；
- DEVLON 对 Class150 的压力等级适用，但项目温度如果超出 `-29～120℃`，材料适用性必须单独闭合。

## 2.2 公司固定计算书实际公式

本次提供的固定球阀计算书工作表：

```text
TMBV Calculation
```

关键单元格公式如下：

```text
E16  qmf = ROUND(MAX(0.1×P, 2), 0)
E17  Fc  = 0.25π(D1²-D2²)×qmf
E23  Q2  = Fsd×Ns
E24  Q1  = 0.25π(D3²-D4²)×P
E26  Qm  = Q1+Q2
E27  qm  = Qm / [0.25π(D1²-D2²)]
E31  m   = IF(Class<900, 1.4, 1.8)
E32  a   = Viton→0.4；NYLON→1.8；其他→3.5
E33  c   = Viton→0.6；NYLON→0.9；其他→1
E34  qb  = m(a+cP) / √[(D1-D2)/2]
E45  验收：qb ≤ qm ≤ [qm]
E46  ΔP1 = 1.33Q2 / [0.25π(D3²-D4²)]
```

计算书材料允许比压列表还明确列有：

```text
DEVLON [qm] = 140 MPa
```

> **重要**：这份 Excel 的 `a/c` 公式没有单独识别 `DEVLON`。它只识别 `Viton`、`NYLON`，其他材料会落到第三组系数。因此，DEVLON 的 `a=1.8、c=0.9` 不能写成“Excel明确规定”，只能作为当前V1按聚酰胺/尼龙组进行的临时映射，必须保留待公司确认标记。

---

# 3. 公司术语与旧计算书符号映射

公司规范和固定计算书使用了两套符号。

| 物理意义 | 公司规范 | 固定计算书 | 当前项目写法 |
|---|---|---|---|
| 密封面内径 | D9 | D2 | D9 |
| 密封面外径 | D10 | D1 | D10 |
| 阀座静压面外径 | D11 | D3 | D11 |
| 阀座静压面内径 | 规范正文未单独给符号 | D4 | D4 |

因此当前统一采用：

```text
旧 D2 → 公司 D9
旧 D1 → 公司 D10
旧 D3 → 公司 D11
```

`D4` 必须单独保留，不能默认永远等于 D9。

公司提供的示例计算书中：

```text
D4 = D2 = 51 mm
```

因此本项目第一轮可以暂按同一种阀座静压面结构：

```text
D4 = D9
```

但这是**结构假设 C**，不是公司通用强制规则。取得12寸实际阀座剖面后必须复核。

---

# 4. 当前项目输入

当前第3步统一输入：

```text
公称尺寸：NPS 12 / DN300
压力等级：Class 150
设计压力：P = 2 MPa
球体材料：ASTM A182 F316
球体外径：Db = φ465 mm
球体半径：Rb = 232.5 mm
流道：d = φ303 mm
密封斜面角：α = 45°
实际斜面密封带：w = 4～6 mm
V1取：w = 5 mm
阀座材料：DEVLON
```

---

# 5. 第一步：由球体求密封接触中心 Dc

45°锥形密封面与球面接触时，第一轮几何关系：

```text
Dc = Db × cos45°
```

代入：

```text
Dc = 465 × 0.70710678
   ≈ 328.80 mm
```

所以：

```text
Dc ≈ φ328.80 mm
```

状态：`B 公式计算值`。

> 前提：真实阀座剖面采用当前45°锥面几何。若后续剖面不是这个几何关系，则 D9/D10 必须重算。

---

# 6. 第二步：由 w=5 mm 求 D9 / D10

定义：

```text
w  = 沿45°斜面量取的实际密封带宽
br = 密封带在径向方向的投影宽度
```

45°时：

```text
br = w × cos45°
```

V1：

```text
br = 5 × 0.70710678
   ≈ 3.5355 mm
```

以接触中心近似对称展开：

```text
D9  = Dc - br
D10 = Dc + br
```

得到：

```text
D9  ≈ 328.80 - 3.5355
    ≈ φ325.27 mm

D10 ≈ 328.80 + 3.5355
    ≈ φ332.34 mm
```

当前V1：

```text
D9  = φ325.27 mm
D10 = φ332.34 mm
```

### 为什么 φ303 不能当 D9

```text
φ303 = 流道控制尺寸
D9   = 球面实际密封区内径
```

两者物理意义不同。

旧试算把 `333/303` 直接当密封带边界，这一做法作废。

---

# 7. 第三步：密封面积 Aseal

采用计算书相同的环形投影面积：

```text
Aseal = π/4 × (D10²-D9²)
```

代入：

```text
Aseal
= π/4 × (332.34²-325.27²)
≈ 3651.55 mm²
```

圆整：

```text
Aseal ≈ 3652 mm²
```

---

# 8. 第四步：弹簧预紧力 Q2

## 8.1 为什么不用旧Excel的 qmf 公式

旧计算书采用：

```text
qmf = max(0.1P, 2)
```

但 2025 公司设计规范已经明确：

```text
≥2" 固定球阀圆柱弹簧预紧比压 = 2.5 MPa
```

因此当前项目采用较新的公司规范：

```text
qpre = 2.5 MPa
```

旧Excel的 `max(0.1P,2)` 仅作为历史计算逻辑记录，不再作为本项目预紧比压控制值。

## 8.2 每侧阀座总预紧力

```text
Q2 = qpre × Aseal
```

代入：

```text
Q2
= 2.5 × 3651.55
≈ 9128.88 N
≈ 9.13 kN / 每侧阀座
```

状态：`B，基于公司2.5 MPa规定`。

> 弹簧数量、单根弹簧力、自由高度、工作高度、疲劳寿命尚未冻结。`Q2` 是当前需要由全部弹簧共同提供的总工作预紧力。

---

# 9. 第五步：必须比压 qb

固定计算书实际公式：

```text
qb = m(a+cP) / √[(D10-D9)/2]
```

## 9.1 流体/压力等级系数 m

Excel公式：

```text
Class < 900 → m = 1.4
Class ≥ 900 → m = 1.8
```

本项目：

```text
Class150 → m = 1.4
```

这个值不再需要按“天然气普通/高密封”人为二选一；当前按公司计算书 Class 分档。

## 9.2 DEVLON 的 a、c 当前处理

固定计算书公式分支：

```text
Viton：a=0.4，c=0.6
NYLON：a=1.8，c=0.9
其他：a=3.5，c=1
```

Excel材料允许比压表虽然列出了 DEVLON=140 MPa，但 `a/c` 公式没有 DEVLON 专用分支。

因此当前V1暂按：

```text
DEVLON → NYLON / Polyamide组

a = 1.8
c = 0.9
```

状态：`C 待公司确认材料映射`。

> 如果公司正式计算程序对 DEVLON 采用其他 a/c，下面 qb、D11 都必须重算。

## 9.3 计算 qb

径向半宽：

```text
(D10-D9)/2
= (332.34-325.27)/2
= 3.535 mm
```

代入：

```text
qb
= 1.4 × (1.8+0.9×2) / √3.535
≈ 2.6806 MPa
```

所以：

```text
qb ≈ 2.68 MPa
```

Excel通常保留一位小数时可写：

```text
qb ≈ 2.7 MPa
```

---

# 10. 第六步：实际工作密封比压 qm

固定计算书的阀座受力链为：

```text
介质压力静压推力 Q1
+
弹簧总预紧力 Q2
=
总密封力 Qm
↓
除以密封面积 Aseal
↓
实际工作密封比压 qm
```

公式：

```text
Q1 = P × π/4 × (D11²-D4²)

Qm = Q1 + Q2

qm = Qm / Aseal
```

验收条件：

```text
qb ≤ qm ≤ [qm]
```

计算书允许比压表中：

```text
DEVLON [qm] = 140 MPa
```

---

# 11. 如果只按“必须比压”反算 D11

设计下限：

```text
qm ≥ qb
```

代入：

```text
P × π/4 × (D11²-D4²) + Q2
≥ qb × Aseal
```

反算：

```text
D11,min,sealing
= √[D4² + 4(qb×Aseal-Q2)/(πP)]
```

当前V1暂按示例结构：

```text
D4 = D9 = 325.27 mm
```

代入：

```text
D11,min,sealing ≈ φ325.91 mm
```

但：

```text
D11,min,sealing < D10
```

这说明：

> 在当前 `qpre=2.5 MPa` 下，弹簧预紧已经非常接近 `qb≈2.68 MPa`，所以“最低必须比压”本身并不控制本项目 D11 的实际尺寸。

因此，不能再使用此前“只从必须比压直接得到 D11≈337～340”的算法。该结果作废。

---

# 12. 第七步：33%中腔自泄压成为控制条件

公司规范规定：

```text
当中腔压力超过介质压力33%时，阀座要有自泄压作用。
```

固定计算书 E46 给出自泄压开启压差计算：

```text
ΔP1 = 1.33Q2 / [π/4 × (D11²-D4²)]
```

这里将公司规范的33%要求解释为设计校核：

```text
ΔP1 ≤ 0.33P
```

本项目：

```text
P = 2 MPa
0.33P = 0.66 MPa
```

于是：

```text
1.33Q2 / [π/4 × (D11²-D4²)] ≤ 0.66
```

反算：

```text
D11,min,relief
= √[D4² + 4×1.33Q2/(π×0.33P)]
```

仍暂按：

```text
D4 = D9 = 325.27 mm
```

得到：

```text
D11,min,relief ≈ φ359.48 mm
```

因此当前两个下限：

```text
必须比压下限：约 φ325.91 mm  → 不控制
33%自泄压下限：约 φ359.48 mm → 控制
```

---

# 13. 当前 V1 选 D11 = φ362 mm

为了不把尺寸卡在理论边界 `φ359.48`，第一轮圆整：

```text
D11 = φ362 mm
```

状态：`C V1设计候选`。

> 这不是最终制造冻结值。它依赖两个仍需确认的前提：`D4=D9` 的真实12寸剖面是否成立，以及 DEVLON 的 `a/c` 材料映射。

---

# 14. D11=362 的完整回代校核

## 14.1 静压环面积

```text
Astatic
= π/4 × (D11²-D4²)
```

代入：

```text
Astatic
= π/4 × (362²-325.27²)
≈ 19826.14 mm²
```

## 14.2 介质压力推力 Q1

```text
Q1 = P × Astatic
```

```text
Q1
= 2 × 19826.14
≈ 39652.28 N
≈ 39.65 kN
```

## 14.3 总密封力 Qm

```text
Qm = Q1 + Q2
```

```text
Qm
≈ 39.65 + 9.13
≈ 48.78 kN
```

## 14.4 实际工作比压 qm

```text
qm = Qm / Aseal
```

```text
qm
= 48781.17 / 3651.55
≈ 13.36 MPa
```

与必须比压、允许比压比较：

```text
qb   ≈ 2.68 MPa
qm   ≈ 13.36 MPa
[qm] = 140 MPa（公司固定计算书材料表）
```

所以：

```text
2.68 < 13.36 < 140
```

第一轮通过。

## 14.5 自泄压开启压差

```text
ΔP1
= 1.33Q2 / Astatic
```

```text
ΔP1
≈ 1.33×9128.88 / 19826.14
≈ 0.612 MPa
```

公司33%目标：

```text
0.33P = 0.66 MPa
```

比较：

```text
0.612 < 0.660 MPa
```

第一轮通过。

---

# 15. 当前最终V1结果表

| 参数 | 当前值 | 状态 | 来源/说明 |
|---|---:|---|---|
| Db 球体外径 | φ465 mm | C | 公司规范 F316、12"、≤CL300优选 |
| w 斜面密封带 | 5 mm | C | 4～6中值试算 |
| Dc 接触中心 | φ328.80 mm | B | 45°球面几何 |
| D9 密封面内径 | **φ325.27 mm** | B/C | 当前V1几何 |
| D10 密封面外径 | **φ332.34 mm** | B/C | 当前V1几何 |
| Aseal | **3651.55 mm²** | B | 环形投影面积 |
| qpre | **2.5 MPa** | A | GFE-JS01-2025 |
| Q2 | **9.13 kN/侧** | B | qpre×Aseal |
| m | **1.4** | B/公司计算书 | Class<900 |
| a/c | **1.8 / 0.9** | C | DEVLON暂按NYLON/Polyamide组，待确认 |
| qb | **2.68 MPa** | B/C | 公司计算书公式 + 当前材料映射 |
| D4 | **暂按φ325.27** | C | 参照示例计算书 D4=D2，待12寸剖面确认 |
| D11 必须比压下限 | φ325.91 mm | B/C | 不控制 |
| D11 自泄压下限 | **φ359.48 mm** | B/C | 当前控制条件 |
| **D11 V1** | **φ362 mm** | **C** | 圆整并留边界余量 |
| D11=362 时 qm | **13.36 MPa** | B/C | `qb<qm<[qm]` |
| DEVLON [qm] | 140 MPa | 计算书参考 | 公司提供计算书材料表 |
| D11=362 时 ΔP1 | **0.612 MPa** | B/C | 小于0.66 MPa |

---

# 16. 这次修正了哪些旧结论

## 16.1 作废：DEVLON厚12、嵌入8、外露4

原因：

```text
公司规范没有给这种固定厚度；
计算书也没有“DN300 → DEVLON厚度”的直接公式。
```

因此：

```text
DEVLON厚度 = D 待设计
嵌入深度 = D 待设计
```

要等 D11、O形圈、K1/K2/K3/K4 和真实保持结构建立后再确定。

## 16.2 作废：把 D3 直接定义成“金属阀座圈外径”

公司规范的第一关键外径是：

```text
D11 = 阀座静压面外径
```

固定计算书里的 `D3` 物理意义也是“Seat static sealing outer diameter”。

所以：

```text
计算书 D3 → 公司 D11
```

而不是“整个金属阀座圈最终外径”。

## 16.3 作废：D11≈337～340 的旧人工结果

原因：旧算法没有完整按公司计算书的：

```text
Q1 + Q2 → Qm → qm
```

处理，并遗漏了公司要求的33%中腔自泄压约束。

当前重新计算后：

```text
D11,min,sealing ≈ φ325.91
D11,min,relief  ≈ φ359.48
V1取 D11       = φ362
```

## 16.4 修正：2.5 MPa不是“必须比压”

```text
2.5 MPa = 公司规定的弹簧预紧比压 qpre
qb      = 由公司固定计算书公式计算的最低必须密封比压
```

当前：

```text
qpre = 2.5 MPa
qb    ≈ 2.68 MPa
```

两个量非常接近，但物理意义不同，不能混写。

---

# 17. D11 与后续尺寸的相关性

D11确定以后，才能继续向外长结构：

```text
D11 静压面外径
  ↓
确认 D4 / 静压环真实边界
  ↓
O形圈截面直径
  ↓
K2 = O形圈槽宽
  ↓
K1 / K3 / K4
  ↓
O形圈槽位置 + 导向段
  ↓
DEVLON保持槽 / 防挤出结构
  ↓
金属阀座圈最终外径
  ↓
阀座轴向总宽 Wseat
  ↓
阀盖/阀体座腔内径
```

同时必须回算：

```text
阀座压力推力
↓
实际密封比压
↓
阀座-球体摩擦力
↓
启闭扭矩
↓
阀杆 / 蜗轮箱
```

所以 D11 不是孤立尺寸，它是**阀座液压活塞效应与后续座腔尺寸的核心接口参数**。

---

# 18. 下一步执行顺序

当前不要直接去猜 DEVLON 厚度。下一步建议严格按以下顺序：

```text
1. 找12寸真实阀座剖面，确认 D4 是否等于 D9
2. 确定阀座O形圈截面直径（VITON具体规格）
3. 按公司第16页表得到 K2 / 槽深 / 导角 / 圆角
4. 按公司 fig5 布置 K1=2～3、K3=3～5、K4=4～6
5. 在 D11=362 基础上建立金属阀座圈V1剖面
6. 再确定 DEVLON 厚度、嵌入深度、保持结构和防挤出结构
7. 得到金属阀座圈最终外径和 Wseat
8. 回算 qm、自泄压、摩擦扭矩、弹簧参数
9. 传递给阀体/阀盖座腔设计
```

---

# 19. 仍未闭合的风险

目前有三个必须持续挂起的风险：

### R1：DEVLON温度范围

公司规范给：

```text
-29～120℃
```

如果项目正式设计温度超出这个范围，DEVLON不能仅凭“Class150可用”直接放行。

### R2：DEVLON a/c 系数映射

公司固定计算书没有DELVON专用 a/c 分支。

当前：

```text
DEVLON → 暂按 NYLON/Polyamide：a=1.8、c=0.9
```

必须由公司计算程序、历史受控计算书或设计负责人确认。

### R3：D4=D9

这是根据本次提供的示例计算书结构得到的第一轮假设，不是规范正文直接规定。

取得12寸阀座实际剖面后应优先确认。

---

# 20. 当前一句话结论

```text
当前第3步已经从“猜DEVLON厚度/猜阀座外径”
修正为公司真正的计算链：

D9 / D10
→ Aseal
→ qpre / Q2
→ qb
→ Q1 + Q2
→ qm
→ 33%自泄压
→ D11

当前V1：
D9  = φ325.27
D10 = φ332.34
D11 = φ362
```

下一步进入：

**D4真实边界确认 → O形圈 → K1/K2/K3/K4 → DEVLON截面 → 金属阀座圈最终外径 / Wseat。**

---

[← 返回主设计计算文档](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md)
