# Q347F 12寸 Class 150——第3步阀座密封副 D9 / D10 / D11 详细计算与修正记录

> **定位**：本文件是主设计文档“第3步阀座密封副”的展开计算页。主文档只保留当前结论、状态和下一步；所有公式、来源、修正原因和依赖关系在这里展开。  
> **返回主线**：[← 返回《Q347F 12寸 Class150 球体设计计算底稿——从内到外设计主线》](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md)

---

# 1. 这一页解决什么问题

```text
球体 φ465 / 全通径 φ303
        ↓
球面密封几何
        ↓
D9 / D10
        ↓
弹簧预紧 Q2
        ↓
必须比压 qb
        ↓
静压面积 D11 / D4
        ↓
实际工作比压 qm
        ↓
33%中腔自泄压
        ↓
D11 V1
```

这里最容易犯的错误，是把下面四类直径混在一起：

```text
整阀通道直径
密封面内/外径
阀座静压面内/外径
阀座/O形圈实际制造配合直径
```

因此本页必须优先服从公司规范和公司提供的固定球阀计算书，而不是靠外观猜尺寸。

---

# 2. 依据优先级

```text
① 当前项目正式输入
② GFE-JS01-2025《BS系列固定球阀设计规范》
③ 公司提供的固定球阀计算书 Excel
④ 人工复算
⑤ 20寸成熟结构 / 历史BOM，仅用于交叉检查
```

公司规范对本步骤明确规定：

```text
根据球体结构初定 D9、D10
→ 通过必须比压确定 D11
→ 固定球阀计算程序确定最终尺寸
```

同时规定：

```text
K1 = 2～3
K2 = O形圈槽宽度
K3 = 3～5
K4 = 4～6

中腔压力超过介质压力33%时要自泄压

≥2"固定球阀圆柱弹簧预紧比压 = 2.5 MPa
```

---

# 3. 公司规范与固定计算书的符号映射——最终采用版

公司计算书 `TMBV Calculation` 的关键输入/公式为：

```text
D3 = Seat static sealing outer diameter
D4 = Seat static sealing inner diameter
D1 = Seat sealing surface outer diameter
D2 = Seat sealing surface inner diameter

Q1 = 0.25π(D3²-D4²)P
Q2 = 弹簧总工作力
qm = (Q1+Q2) / [0.25π(D1²-D2²)]
qb = m(a+cP) / √[(D1-D2)/2]
ΔP1 = 1.33Q2 / [0.25π(D3²-D4²)]
```

和公司规范对应：

| 物理意义 | 公司规范 | 固定计算书 | 本项目写法 |
|---|---|---|---|
| 密封面内径 | D9 | D2 | D9 |
| 密封面外径 | D10 | D1 | D10 |
| 静压面外径 | D11 | D3 | D11 |
| 静压面内径 | fig5 局部标注 `d` | D4 | **D4**，为避免和全通径 d 混淆，本项目保留 `D4` 名称 |
| 整阀通道直径 | 基本参数 `d` | Valve Bore Design Diameter `d` | **d_flow** |

所以：

```text
Excel D2 → 公司 D9
Excel D1 → 公司 D10
Excel D3 → 公司 D11
Excel D4 → 公司 fig5 的静压面内径
```

## 3.1 特别重要：公司文件存在“d”符号重名

公司规范前文用：

```text
d = 通道直径
```

但第11页 `fig 5` 又在阀座局部使用 `φd`。

**不能因为字母一样，就认定两个 d 是同一条直径。**

公司提供的实际计算书已经给出了直接证据：

```text
Valve Bore Design Diameter d = 38 mm
D4 Seat static sealing inner diameter = 51 mm
D2 Seat sealing surface inner diameter = 51 mm
```

也就是：

```text
D4 = 51 ≠ 通道 d = 38
D4 = D2 = 51   （该公司样例结构）
```

因此曾短暂采用的：

```text
D4 = 通道φ303
→ D11≈φ342
```

**正式作废。**

这次修正不是经验判断，而是由公司计算书真实输入值直接推翻。

---

# 4. 当前项目输入

```text
公称尺寸：NPS 12 / DN300
压力等级：Class 150
设计压力：P = 2 MPa
介质：天然气
结构：固定球、全通径

球体：ASTM A182 F316
球体外径 Db = φ465 mm
球体半径 Rb = 232.5 mm
整阀通道 d_flow = φ303 mm

密封斜面角 α = 45°
实际斜面密封带 w = 4～6 mm
V1取 w = 5 mm

阀座材料：DEVLON
```

---

# 5. 密封接触中心 Dc

当前45°几何：

```text
Dc = Db × cos45°
```

代入：

```text
Dc = 465 × 0.70710678
   ≈ 328.80 mm
```

所以：

```text
Dc ≈ φ328.80 mm
```

状态：`B 公式计算值`。

---

# 6. 由 w=5 mm 求 D9 / D10

```text
br = w × cos45°
   = 5 × 0.70710678
   ≈ 3.5355 mm
```

以接触中心近似对称展开：

```text
D9  = Dc - br
D10 = Dc + br
```

得到：

```text
D9  ≈ φ325.27 mm
D10 ≈ φ332.34 mm
```

注意：

```text
φ303 = 整阀流道
D9≈φ325.27 = 密封面内径
```

二者不是同一个尺寸。

---

# 7. 密封面积 Aseal

```text
Aseal = π/4 × (D10²-D9²)
```

按未过早圆整的几何值：

```text
Aseal ≈ 3652.10 mm²
```

后文工程表达可写：

```text
Aseal ≈ 3652 mm²
```

---

# 8. 弹簧总预紧力 Q2

2025公司规范明确：

```text
qpre = 2.5 MPa
```

所以：

```text
Q2 = qpre × Aseal
   = 2.5 × 3652.10
   ≈ 9130 N
   ≈ 9.13 kN / 每侧阀座
```

状态：`B`。

这里的2.5 MPa是：

```text
弹簧预紧比压 qpre
```

不是下面的“必须比压 qb”。

---

# 9. 必须比压 qb

公司固定计算书：

```text
qb = m(a+cP) / √[(D10-D9)/2]
```

Class150：

```text
m = 1.4
```

固定计算书没有 DEVLON 专用 `a/c` 分支，因此当前V1暂按NYLON/Polyamide组：

```text
a = 1.8
c = 0.9
```

代入：

```text
qb ≈ 2.68 MPa
```

状态：`B/C`。

> `a=1.8、c=0.9` 仍需公司正式计算程序/设计负责人确认，不能写成“DEVLON公司最终定值”。

---

# 10. D4：当前怎么取

这是本轮真正的关键前置尺寸。

公司固定计算书样例中：

```text
D4 = D2 = 51 mm
```

即该成熟结构里：

```text
静压面内径 = 密封面内径
```

因此12寸第一轮按同一公司结构逻辑取：

```text
D4_V1 = D9
      ≈ φ325.27 mm
```

状态：`C 公司样例结构映射`。

这比“D4=通道φ303”有更直接的公司计算书依据，但仍不是通用标准公式；如果后续12寸正式阀座剖面显示 D4≠D9，D11必须立即重算。

---

# 11. 实际工作密封比压 qm

固定计算书：

```text
Q1 = P × π/4 × (D11²-D4²)

Qm = Q1 + Q2

qm = Qm / Aseal
```

验收：

```text
qb ≤ qm ≤ [qm]
```

公司计算书材料表：

```text
DEVLON [qm] = 140 MPa
```

---

# 12. 必须比压条件反算 D11

最低密封条件：

```text
qm ≥ qb
```

反算：

```text
D11,min,sealing
= √[D4² + 4(qb×Aseal-Q2)/(πP)]
```

代入：

```text
D4    ≈ 325.27
qb    ≈ 2.680
Aseal ≈ 3652.10
Q2    ≈ 9130.25
P     = 2
```

得到：

```text
D11,min,sealing ≈ φ325.91 mm
```

它小于 D10≈φ332.34，说明：

> 当前2.5 MPa弹簧预紧已经接近最低必须比压，**最低密封能力不是本项目 D11 的控制条件。**

---

# 13. 33%中腔自泄压——当前D11控制条件

公司规范要求中腔超过介质压力33%时能自泄压。

固定计算书：

```text
ΔP1
= 1.33Q2 / [π/4 × (D11²-D4²)]
```

本项目：

```text
P = 2 MPa
0.33P = 0.66 MPa
```

要求：

```text
ΔP1 ≤ 0.66 MPa
```

反算：

```text
D11,min,relief
= √[
    D4²
    + 4×1.33Q2/(π×0.33P)
  ]
```

代入当前 `D4=D9≈325.27`：

```text
D11,min,relief ≈ φ359.48 mm
```

所以：

```text
必须比压下限：约φ325.91 → 不控制
33%自泄压下限：约φ359.48 → 当前控制
```

---

# 14. D11当前V1：φ362 mm

为了不贴着理论边界359.48，当前向上圆整：

```text
D11 = φ362 mm
```

状态：`C V1候选`。

修正历史必须保留：

```text
第一轮错误猜测：D3≈362（无公式） → 作废

之后：
按公司计算书 + D4=D9公司样例结构
得到 D11≈362 → 有计算依据的V1

短暂错误版：
把D4误等同全通径303
得到D11≈342 → 作废
```

也就是说：

> **现在的φ362和最早“猜出来的362”数值碰巧接近，但来源完全不同。当前φ362是由公司计算书、自泄压条件和D4结构映射推出来的。**

---

# 15. D11=362完整回代

静压环面积：

```text
Astatic
= π/4 × (362²-325.27²)
≈ 19826.59 mm²
```

介质压力推力：

```text
Q1 = P × Astatic
   ≈ 39.65 kN
```

总密封力：

```text
Qm = Q1 + Q2
   ≈ 39.65 + 9.13
   ≈ 48.78 kN
```

实际工作比压：

```text
qm = Qm/Aseal
   ≈ 13.36 MPa
```

验收：

```text
qb   ≈ 2.68 MPa
qm   ≈ 13.36 MPa
[qm] = 140 MPa

2.68 < 13.36 < 140
```

自泄压：

```text
ΔP1
= 1.33Q2/Astatic
≈ 0.612 MPa
```

要求：

```text
0.612 < 0.660 MPa
```

通过当前人工复算。

---

# 16. 当前V1结果表

| 参数 | 当前值 | 状态 | 说明 |
|---|---:|---|---|
| 整阀通道 d_flow | **φ303 mm** | A | 全通径控制值 |
| Dc | φ328.80 | B | 45°几何 |
| D9 | **φ325.27** | B/C | 密封面内径 |
| D10 | **φ332.34** | B/C | 密封面外径 |
| Aseal | **约3652.10 mm²** | B | 环形投影面积 |
| qpre | **2.5 MPa** | A | 公司规范 |
| Q2 | **约9.13 kN/侧** | B | 弹簧总预紧 |
| qb | **约2.68 MPa** | B/C | DEVLON材料映射待确认 |
| D4 V1 | **φ325.27** | C | 公司计算书样例 D4=D2，当前映射D4=D9 |
| D11 必须比压下限 | φ325.91 | B/C | 不控制 |
| D11 自泄压下限 | **φ359.48** | B/C | 当前控制 |
| **D11 V1** | **φ362** | **C** | 向上圆整 |
| qm@362 | **约13.36 MPa** | B/C | 满足当前计算书判据 |
| ΔP1@362 | **约0.612 MPa** | B/C | `<0.66 MPa` |

---

# 17. D11与O形圈：不要再直接画等号

一个非常重要的新结论：

```text
D11 = 静压面外径
```

**不等于**：

```text
O形圈槽底径
O形圈安装台阶直径
阀盖某一段导向孔径
整个金属阀座圈最大外径
```

20寸总装剖面也能看到：阀座O形圈位于支承圈后部的独立台阶/沟槽位置，而不是直接由 D11 一条直径决定。

因此曾采用的：

```text
Dg = D11 - 2×4.4
```

**作废。**

O形圈槽底径必须等真实导向台阶/阀盖配合孔确定后再计算。

---

# 18. 下一步

继续进入第3B页：

```text
目标BOM两条O形圈：
φ320×5.3
φ311×3.55
        ↓
分别查公司沟槽表
        ↓
确定哪一条对应 fig5 主活塞O圈
        ↓
K1/K2/K3/K4
        ↓
阀盖座孔 / 支承圈导向台阶
        ↓
DEVLON保持结构
        ↓
tdev / edev / Wseat
```

**[→ 继续《O形圈 / K1-K4 / DEVLON截面 V1详细计算》](./Q347F_12in_Class150_第3步阀座_O形圈_K1-K4_DEVLON截面_V1详细计算.md)**

**[← 返回主设计计算文档](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md)**