# Q347F 12寸 Class150 第4C步：20寸原生支承/阀杆轴向证据与12寸V11坐标骨架

> **对象**：Q347F，NPS12 / DN300，Class150，天然气，固定球，全通径 φ303。  
> **本页作用**：纠正V9/V10对上部轴承功能的误判，并建立“球体上/下主支承—阀杆导向—双O圈—填料—连接盘—键轴”的第一版绝对轴向坐标骨架。  
> **原则**：本页把“原生几何实测”“12寸BOM固定规格”“公式计算”“系列化映射候选”严格分开。**没有直接证据的绝对Z坐标只标C，不进入制造冻结。**

---

# 1. 本轮证据源与主球面面级复核

本轮使用：

1. `20Q347F-300LB-04 阀杆.SLDPRT`
2. `20Q347F-300LB-08 前盖.SLDPRT`
3. `20Q347F-300LB-09 联接盘.SLDPRT`
4. `20ZQ347F-300LB 总装图.STEP`
5. 12寸目标BOM
6. 公司 `GFE-JS01-2025`

原生STEP可直接提取实体圆柱面、轴承/垫片/填料实体的轴向包络。

## 1.1 二次面级复核：20寸STEP主球面实际也是 R374 = φ748

公司当前规范对低碳奥氏体不锈钢球体给出：

```text
20" ≤CL300 → Sφ = 748
12" ≤CL300 → Sφ = 465
```

第一次快速读取时，STEP球体里发现了 `R373` 球面，因此一度怀疑当前装配STEP的球径是 `φ746`。本轮为了避免把局部球面误认成主球面，又用 OCC 对**每一个球面Face**做了面积与包络复核。

结果：

```text
主球面：R374
- 2个大面积半球面Face
- 单面面积约 468731 mm²
- 合计面积约 937462 mm²
- 球体整体包络约 ±374 mm

局部球面：R373
- 4个局部球面片
- 合计面积约 328494 mm²
- 主要位于上下接口/过渡区域
```

所以正确结论是：

```text
公司20"系列名义球径 = φ748
20寸STEP主球面       = R374 = φ748
两者一致              ✅
```

`R373` 不能再当作球体主球面。

因此V11后续系列化轴向映射统一采用：

```text
k = 465 / 748
  = 0.6216578
```

> **历史“φ746/φ748冲突、双比例映射”在V11正式提交前撤回，不进入当前有效主线。**

---

# 2. 20寸原生STEP把“谁是球体轴承、谁是阀杆轴承”说清楚了

以球心为：

```text
Y = 0
```

20寸上部的真实功能链是：

```text
球体上支承孔 φ150
↓
滑动轴承 φ150×φ145×55
↓
前盖下支承轴 φ145
```

同时，在这个支承轴内部还有另一条完全独立的链：

```text
阀杆 φ95
↓
止推垫 φ110×φ95×2
↓
滑动轴承 φ100×φ95×65
↓
前盖内孔 φ100
```

因此V9/V10旧说法：

```text
“φ70×φ65×50是12寸上部球体主支承轴承”
```

正式降为：

```text
H/R
```

12寸正确对应应为：

```text
球体上主支承：
球体孔 φ105
↓
φ105×φ100×30
↓
前盖支承轴 φ100

阀杆自身导向：
阀杆 φ65
↓
φ70×φ65×50
↓
前盖内轴承孔 φ70
```

---

# 3. 20寸原生几何坐标证据表

以下均为本轮STEP几何直接读取，不是比例猜值。

| 功能 | 20寸原生几何 | 原生Y范围 / 中心 |
|---|---:|---:|
| 球体上支承孔 | φ150 | `308～361` |
| 上球体轴承 | φ150×φ145×55 | `308～363`，中心 **335.5** |
| 前盖下支承轴 | φ145 | 约 `310～365` |
| 阀杆下部头/肩最大外径 | 约φ109 | `268～324` |
| 止推垫 | φ110×φ95×2 | `324～326` |
| 阀杆导向轴承 | φ100×φ95×65 | `326～391`，中心 **358.5** |
| 前盖轴承座孔 | φ100 | 约 `328～392` |
| 前盖阀杆普通间隙孔 | φ95.4 | `392～428.8`、`470.2～478.5` |
| 第一阀杆O圈槽根径 | φ103.8 | `429.4～435.6` |
| 第一建模O圈包络 | 约φ104 | `429～436.1` |
| 第二阀杆O圈槽根径 | φ103.8 | `463.4～469.6` |
| 第二建模O圈包络 | 约φ104 | `463～470.1` |
| 填料函外径 | φ105 | `478.5～485` |
| 柔性石墨填料 | φ105×φ95×5 | `478.5～483.5` |
| 联接盘 | 外包络φ350级 | `483～521` |
| 联接盘中心孔 | φ95.4 | `483～521` |
| 阀杆上键轴 | **φ90** | 约 `519～663` |
| 两条平键 | 25×14×140 | `525～665` |
| 球体下支承孔 | φ105 | `-366～-276` |
| 下球体轴承 | φ105×φ100×90 | `-370～-280`，中心 **-325** |
| 底盖支承轴 | φ100 | 约 `-370～-281.5` |

本轮又用 OCC/XCAF 对STEP装配实体逐组件复核，关键购买件包络直接得到：

```text
上球体轴承 φ150×φ145×55：Y=308～363
下球体轴承 φ105×φ100×90：Y=-370～-280
阀杆导向轴承 φ100×φ95×65：Y=326～391
止推垫 φ110×φ95×2：Y=324～326
填料 φ105×φ95×5：Y=478.5～483.5
键25×14×140：Y=525～665
```

这些位置不是截图估读，而是装配STEP中对应组件的变换后实体包络。

这张表得到三个非常重要的公司成熟结构事实：

1. 上、下球体反力中心接近对称，但不是强制 `a=b`；
2. 阀杆导向轴承与球体上支承轴承是**同轴套娃、功能不同**；
3. 阀杆O圈、外O圈、填料、轴承会在不同半径上**轴向重叠**，绝不能把所有零件长度简单相加。

---

# 4. V11重新计算球体上下主支承反力

## 4.1 12寸反力中心候选

20寸原生球体主支承轴承中心：

```text
a20 = +335.5 mm
b20 = 325.0 mm
```

统一按已经复核通过的主球面系列比例：

```text
k = 465/748 = 0.6216578
```

中心等比例映射：

```text
a12,center = 335.5×465/748
           = 208.57 mm

b12,center = 325×465/748
           = 202.04 mm
```

但这里还有一个不能忽略的问题：20寸轴承长度和12寸采购轴承长度**不是严格按0.6216578同比缩放**。

```text
上轴承：20寸 L=55 → 等比例应34.19，但12寸BOM实际 L=30
下轴承：20寸 L=90 → 等比例应55.95，但12寸BOM实际 L=50
```

所以 `208.57 / 202.04` 只能作为“中心系列映射值”，不能冒充加工坐标。为了看清这件事对反力的影响，V11又做两种端面保持敏感性：

```text
上支承中心可能约：206.47 ～ 210.66 mm
下支承中心绝对值可能约：199.06 ～ 205.01 mm
```

V11 CAD骨架先取中心系列值并圆整：

```text
a = 208.6 mm     C
b = 202.0 mm     C
```

同时给出几何敏感性范围，而不是把小数点后两位当制造精度。

在 `Fsupport≈164.692 kN` 下，上述端面保持组合得到的反力范围约：

```text
Ru ≈ 80.0 ～ 82.1 kN
Rl ≈ 82.6 ～ 84.7 kN
```

因此后面按中心系列值算出的 `Ru≈81.04 / Rl≈83.66 kN` 是合理的第一轮设计值；真正前盖/底盖轴向装配完成后再用实际中心坐标回算。

## 4.2 反力分配公式

设：

```text
F = 球体水平支承载荷
a = 球心到上轴承反力中心距离
b = 球心到下轴承反力中心距离
```

静力平衡：

```text
Ru = F×b/(a+b)
Rl = F×a/(a+b)
```

成熟结构比例：

```text
Ru/F = 325/(335.5+325)
     ≈ 0.492051

Rl/F = 335.5/(335.5+325)
     ≈ 0.507949
```

---

# 5. 两种压力面积口径都保留，不偷换概念

## 5.1 基础流道投影

```text
d = 303
P = 2 MPa

F303
= P×πd²/4
≈ 144.213 kN
```

得到：

```text
Ru303 ≈ 70.960 kN
Rl303 ≈ 73.253 kN
```

## 5.2 V9/V11继续保留的保守支承包络

当前工程包络：

```text
Deff = D4 ≈ 323.8
```

因此：

```text
Fsupport
= 2×π×323.8²/4
≈ 164.692 kN
```

对应：

```text
Ru ≈ 81.037 kN
Rl ≈ 83.655 kN
```

V9旧：

```text
Ru = Rl = 82.35 kN
```

改为：

```text
H
```

它只是早期 `a=b` 对称近似。

---

# 6. 真正两只“球体主支承轴承”的面压

## 6.1 上球体主支承

12寸BOM：

```text
φ105×φ100×30
```

所以：

```text
d = 100
L = 30
Ru = 81.037 kN
```

平均面压：

```text
pu
= 81037/(100×30)
≈ 27.01 MPa
```

基础φ303压力面积时：

```text
pu,303 ≈ 23.65 MPa
```

## 6.2 下球体主支承

12寸BOM：

```text
φ70×φ65×50
```

所以：

```text
d = 65
L = 50
Rl = 83.655 kN
```

平均面压：

```text
pl
= 83655/(65×50)
≈ 25.74 MPa
```

基础φ303口径：

```text
pl,303 ≈ 22.54 MPa
```

结论：

```text
保守包络：
上 ≈27.0 MPa
下 ≈25.7 MPa
```

两边设计需求非常接近，说明12寸BOM里的：

```text
100×30
与
65×50
```

很可能本来就是按接近相同面压进行系列化配置。

轴承材料目前只有：

```text
316+PTFE
```

没有供应商牌号/许用静压，所以这里只得到“需求”，不能宣布最终合格。

---

# 7. 球体上下孔V11纠错

## 7.1 上孔

20寸：

```text
上轴承名义长度 = 55
球体φ150圆柱支承孔有效长度 ≈53
```

比例：

```text
53/55 ≈0.9636
```

12寸上轴承长度30，第一轮有效圆柱接触长度：

```text
Lu,bore
≈30×53/55
≈28.9 mm      C
```

因此：

```text
BALL_UPPER_BORE_D = φ105   C+
有效圆柱段 ≈29 mm          C
```

总孔深还要加导角/装配过渡，暂不冻结。

## 7.2 下孔

20寸：

```text
下球体孔φ105圆柱段 = 90
下轴承长度         = 90
```

因此12寸更可靠的理解是：

```text
BALL_LOWER_BORE_D = φ70
有效圆柱支承段    = 50 mm
```

V9旧：

```text
φ70×52深
```

不能再解释成“50mm轴承必须完全塞在52mm圆柱段里”。

V11改成：

```text
有效圆柱轴承段 = 50 mm  C+
总加工深度（含倒角/底部过渡） ≥50，具体=? D
```

原 `52` 只保留为历史CAD总深候选 `H/C`。

---

# 8. φ65到底是什么：V11把“下支承轴”和“阀杆”分开

虽然两个地方都是65，但不是同一根轴。

```text
下球体支承：
底盖A182 F51支承轴颈 = φ65

上部阀杆：
A182 F51阀杆密封/导向主轴颈 = φ65
```

二者只是系列尺寸相同。

最重要的是：

```text
上球体主支承轴 ≠ 阀杆φ65
```

上球体主支承轴实际上是：

```text
前盖A105N+ENP的 φ100 支承轴
```

---

# 9. V11再次修正φ60：它很可能不是“虚拟筛选值”，而是实际上键轴

20寸原生阀杆直接读到：

```text
中部密封/导向轴 = φ95
上部键轴         = φ90
```

同时20寸BOM：

```text
键 = C25×140
```

原生键实体：

```text
25×14×140
Y≈525～665
```

公司键表：

```text
>85～95轴径 → 25×14
```

完全对得上20寸 `φ90 + 25×14`。

12寸已经明确：

```text
中部主轴颈 = φ65
BOM键       = 18×90
公司键表 >58～65 → 18×11
```

因此V11恢复一个更合理的物理候选：

```text
STEM_SEAL_GUIDE_D = φ65       C+
STEM_KEY_D        = φ60       C
KEY               = 18×11×90 C+
```

这意味着：

> `φ60` 不应再理解成“整根阀杆直径”，但也不应彻底当成不存在。它现在最合理的角色是**连接盘上方的实际键轴候选**。

V10：

```text
“默认不建立φ60物理台阶”
```

降为 `H/R`。

## 9.1 两段扭转都要保留

1800N·m：

```text
φ65主轴：
τ65 ≈33.38 MPa

φ60键轴：
τ60 ≈42.44 MPa
```

真正薄弱位置还要考虑：

```text
键槽削弱
根部圆角Kt
实际断裂位置
```

所以仍不能仅凭纯扭转宣布合格。

---

# 10. 18×11×90键重新按φ60键轴计算

1800N·m：

```text
T = 1,800,000 N·mm
d = 60 mm

Ft = 2T/d
   = 60,000 N
```

单键承担全部扭矩：

```text
τkey
= 60000/(18×90)
≈37.04 MPa
```

按半键高承压：

```text
σkey
= 60000/(5.5×90)
≈121.21 MPa
```

因此：

- V9的 `37.04 / 121.21 MPa` 恢复为 **V11当前φ60键轴计算值**；
- V10按φ65得到的 `34.19 / 111.89 MPa` 改为 **H**。

公司仍要求双键槽，通常装1键；12寸BOM数量写2键，这个冲突继续保持 `R/D`，强度按单键承担全部扭矩最保守。

---

# 11. 防吹出肩：20寸原生关系把φ74说明白了

20寸：

```text
阀杆下头最大OD ≈109
止推垫OD       =110
差≈1 mm
```

12寸：

```text
止推垫 = φ75×φ65×2
```

按同一成熟关系：

```text
Dshoulder ≈75-1
          ≈φ74 mm
```

所以：

```text
STEM_SHOULDER_OD = φ74   C+
```

而不是V10的约φ75。

同时：

```text
74 > 70
```

仍然无法穿过φ70阀杆导向轴承孔，机械防吹出成立。

2MPa吹出力：

```text
Fblow ≈6.637 kN
```

按实际φ74肩面可用环面积：

```text
A74-65
= π/4(74²-65²)
≈982.53 mm²

p_shoulder
≈6636.6/982.53
≈6.75 MPa
```

止推垫全环面积按φ75-φ65时仍为：

```text
≈1099.56 mm²
≈6.04 MPa
```

---

# 12. 20寸原生前盖把两道阀杆O圈位置也纠正了

20寸前盖：

```text
第一槽根：
φ103.8
Y=429.4～435.6

第二槽根：
φ103.8
Y=463.4～469.6
```

二槽之间真实金属/注脂区：

```text
463.4 - 435.6
= 27.8 mm
```

所以V10：

```text
双O圈中间金属带 = 4 mm
双槽局部包络 = 18 mm
```

正式改为 `H/R`。

20寸两道O圈中间明显存在：

```text
阀杆注脂/隔离空间
```

这与12寸BOM存在：

```text
1/4"NPT 加长阀杆注脂阀
```

完全一致。

---

# 13. 12寸双O圈径向槽保持V10正确值，轴向位置改为V11

12寸：

```text
阀杆 = φ65
O圈  = φ65×5.3 ×2
```

公司5.3截面动密封：

```text
h = 4.4
b = 7.0
z = 4
r1 = 0.6
r2 = 0.2
```

所以槽根径继续：

```text
Dgroove
=65+2×4.4
=φ73.8
```

原生20寸还显示：

```text
阀杆φ95
前盖普通孔φ95.4
槽根φ103.8
```

即：

```text
普通孔直径比阀杆大0.4
槽根相对阀杆半径高度=4.4
```

因此12寸V11可先画：

```text
前盖普通阀杆孔 ≈φ65.4   C
O圈槽根         φ73.8    B/C+
O圈槽宽         7.0      A
```

---

# 14. V11绝对Z坐标骨架——只用于SolidWorks骨架，不用于加工图

## 14.1 坐标映射精度说明

20寸主球面已经二次复核为：

```text
R374 = φ748
```

与公司球径表一致，因此本轮不再使用“746/748双比例”。但这些Z坐标仍然只是成熟结构映射候选，因为12寸购买轴承长度与20寸不是严格同比缩放。

所以本节规则是：

```text
系列中心映射 → 用 k=465/748
最终制造坐标 → 等12寸前盖/底盖真实装配后再冻结
```

### Z基准

```text
Z0 = 球心 = 0
向上为+
```

## 14.2 球体主支承中心

中心系列映射：

```text
上球体轴承中心 Zu ≈ +208.6 mm   C
下球体轴承中心 Zl ≈ -202.0 mm   C
```

端面保持敏感性范围：

```text
Zu ≈ +206.5 ～ +210.7 mm
Zl ≈ -205.0 ～ -199.1 mm
```

12寸购买件：

```text
上轴承L=30
下轴承L=50
```

按中心系列值做CAD第一轮轴承包络：

```text
上：约 +193.6 ～ +223.6
下：约 -227.0 ～ -177.0
```

这些范围用于骨架/干涉检查，**不作为加工图绝对Z尺寸**。

## 14.3 阀杆导向轴承/止推肩

20寸阀杆轴承中心：

```text
358.5
```

统一按465/748映射：

```text
358.5×465/748 = 222.86 mm
```

V11 CAD取：

```text
Z_STEM_BRG_C ≈ +222.9 mm     C
```

轴承L=50：

```text
约 +197.9 ～ +247.9
```

止推垫2mm紧接轴承下方：

```text
约 +195.9 ～ +197.9
```

因此防吹出肩上表面：

```text
Z_SHOULDER_TOP ≈ +195.9 mm   C
```

20寸下头轴向包络56mm，按465/748映射为：

```text
56×465/748 = 34.81 mm
```

V11取下驱动头轴向包络：

```text
≈34.8 mm   C
```

所以其下端约：

```text
Z_HEAD_BOTTOM ≈ +161.1 mm
```

这个结果与历史资料里出现过的 `35 mm` 数字相近，但二者**对象定义并不完全相同**，所以不能把这种相近当成独立验证。V11仅把 `≈35 mm` 作为驱动头轴向包装候选C；球体槽深、槽宽、啮合长度仍要由实际阀杆—球体接口校核后冻结。

## 14.4 第一阀杆O圈

20寸从阀杆轴承上端391到第一槽下端429.4：

```text
38.4 mm
```

按465/748映射：

```text
38.4×465/748 = 23.87 mm
```

以前盖阀杆轴承上端约247.9为基准，12寸第一槽：

```text
Z_O1_START ≈ +271.7 mm
Z_O1_END   ≈ +278.7 mm
```

槽宽仍直接用公司：

```text
7.0 mm
```

## 14.5 两O圈之间的注脂/隔离区

20寸真实净区：

```text
27.8 mm
```

按465/748映射：

```text
27.8×465/748 = 17.28 mm
```

V11 CAD：

```text
L_OO_LAND ≈17.3 mm   C
```

所以第二槽约：

```text
Z_O2_START ≈ +296.0 mm
Z_O2_END   ≈ +303.0 mm
```

这才是12寸 `1/4"NPT` 阀杆注脂结构应占用的中间区。

最终注脂孔轴向中心必须在前盖正式图中闭合，不把17.3当规范值。

## 14.6 第二O圈到填料

20寸：

```text
469.6 → 478.5
净距8.9
```

按465/748映射：

```text
8.9×465/748 = 5.53 mm
```

所以V11：

```text
填料自由件下边界约 +308.6 mm
```

填料：

```text
φ75×φ65×5
```

按当前跨规范10～15%压缩参考，CAD压后目标：

```text
4.4 mm
```

因此压紧面大约：

```text
Z_PACK_TOP ≈ +313.0 mm
```

## 14.7 联接盘、键轴：只给范围，不冻结

20寸联接盘厚度：

```text
38 mm
```

按465/748系列比例：

```text
38×465/748 = 23.62 mm
```

但12寸联接盘最终还受：

```text
ISO 5211
M12紧固
蜗轮接口
填料压紧
```

控制。

所以V11只给：

```text
t_adapter,CAD ≈23.6～24 mm   C/H-guide
最终 = ? D
```

如果CAD暂用24：

```text
联接盘上表面约 +336.6～337 mm
```

20寸键从联接盘上方约4mm开始，按465/748后约2.49mm。

12寸键长90，所以第一轮：

```text
KEY_START ≈ +339.1～339.5 mm
KEY_END   ≈ +429.1～429.5 mm
```

这只是装配骨架。

最终阀杆总长必须等：

```text
连接盘最终厚度
+ ISO5211
+ 蜗轮输入轴接口
```

一起闭合。

---

# 15. 外侧φ95 O圈：V10的“内孔φ103.4”方向需要纠正

20寸原生前盖外部存在：

```text
前盖外圆导向OD = φ150
外O圈槽底OD    ≈φ141.2
槽径向深度     =(150-141.2)/2=4.4
接口缠绕垫     =φ160×φ150
```

12寸BOM：

```text
外O圈      = φ95×5.3
缠绕垫     = φ115×φ105×3.2
上球轴承OD = φ105
```

功能系列非常清楚：

```text
20寸：145 →150 →160
12寸：100 →105 →115
```

所以V11认为前盖外侧正确方向更可能是：

```text
前盖外导向OD      = φ105         C+
外O圈开在前盖外圆 = 是            C+
```

如果继续沿用20寸成熟结构的4.4mm径向槽深：

```text
Dgroove_out
=105-2×4.4
=φ96.2 mm
```

所以：

```text
COVER_OUT_GUIDE_D = φ105
COVER_OUT_ORING   = φ95×5.3
COVER_OUT_GROOT   ≈φ96.2   C
GASKET             φ115×φ105×3.2 A
```

V10旧：

```text
“若采用内孔静槽，槽根φ103.4”
```

降为：

```text
H/R
```

最终外O圈槽深究竟采用公司静槽4.2还是成熟结构装配型4.4，要在阀体上部配合孔和装配方式确定后冻结；V11不把4.4冒充公司强制静密封值。

---

# 16. V11下支承轴弯曲回算

下球体反力改为：

```text
Rl ≈83.655 kN
```

继续用V9 CAD悬臂：

```text
e =27 mm   C
d =65
```

得到：

```text
M = Rl×e
  ≈2.259×10^6 N·mm

σb
=32M/(πd³)
≈83.78 MPa

τdirect
=4Rl/(πd²)
≈25.21 MPa

σeq
≈sqrt(σb²+3τ²)
≈94.47 MPa
```

V9旧：

```text
82.46 / 24.82 / 92.99 MPa
```

保留 `H`。

这仍未包含：

```text
肩根Kt
底盖过渡圆角
局部接触
温度许用
```

不能写“最终合格”。

---

# 17. V11当前可以直接给SolidWorks的骨架参数

```text
# 球体主支承
BALL_UP_BORE_D=105
UP_BRG=105×100×30
UP_COVER_JOURNAL_D=100
Z_UP_BRG_C≈+208.6

BALL_LOW_BORE_D=70
LOW_BRG=70×65×50
LOW_TRUNNION_D=65
Z_LOW_BRG_C≈-202.0

# 阀杆
STEM_SEAL_GUIDE_D=65
STEM_GUIDE_BRG=70×65×50
Z_STEM_GUIDE_BRG_C≈+222.9

THRUST_WASHER=75×65×2
STEM_SHOULDER_OD≈74
Z_SHOULDER_TOP≈+195.9

STEM_DRIVE_HEAD_AXIAL≈34.8
STEM_KEY_D≈60
KEY=18×11×90

# 阀杆双O圈
STEM_PLAIN_BORE≈65.4
STEM_ORING=65×5.3×2
STEM_ORING_GROOT=73.8
STEM_ORING_GW=7
Z_O1≈271.7~278.7
Z_O2≈296.0~303.0
OIL_LAND≈17.3

# 填料
PACK=75×65×5
PACK_T_INSTALL≈4.4
Z_PACK_BOTTOM≈308.6
Z_PACK_PRESS_FACE≈313.0

# 前盖外侧
COVER_OUT_GUIDE_D=105
COVER_OUT_ORING=95×5.3
COVER_OUT_GROOT≈96.2   # C，方向已修正，深度制造前再确认
GASKET=115×105×3.2

# 联接盘/顶部
ADAPTER_T_CAD≈23.6~24       # 仅草模
KEY_START≈339.1~339.5
KEY_END≈429.1~429.5
STEM_TOTAL_LEN=?       # 仍不冻结
```

---

# 18. 本轮正式修正清单

| V9/V10旧理解 | V11处理 |
|---|---|
| 第一次快速读到R373→怀疑主球面φ746 | **H/R并撤回**；面级复核确认主球面R374=φ748，与公司表一致 |
| 上下球体都用φ70×φ65×50 | **H/R**；上球体实际用φ105×φ100×30 |
| φ105×φ100×30只是“外导向轴承” | **H/R**；它是球体真正上主支承轴承 |
| Ru=Rl=82.35 | **H**；V11为81.04/83.66 |
| φ60默认不存在 | **H/R**；V11恢复为上部键轴候选 |
| 键按φ65算34.19/111.89 | **H**；V11按φ60为37.04/121.21 |
| 防吹出肩≈φ75 | 调整为 **≈φ74 C+** |
| 双O圈中间仅4mm | **H/R**；原生20寸净区27.8，12寸CAD约17.3 |
| 双O圈局部18mm | **H/R** |
| 外φ95 O圈若内孔槽根φ103.4 | **H/R**；更合理为前盖外圆槽，V11槽底≈φ96.2 C |
| 下轴应力82.46/24.82/92.99 | **H**；新反力下83.78/25.21/94.47 |
| 下球孔φ70×52为固定圆柱深 | **H/C**；有效轴承圆柱段50，总加工深度待闭合 |

---

# 19. V11还不允许制造冻结的项目

```text
上/下反力中心最终Z
前盖所有绝对Z制造尺寸
联接盘最终厚度
ISO5211顶法兰规格
蜗轮真实输入接口
阀杆总长
φ60键轴的最终长度/公差
外φ95 O圈最终槽深4.2还是4.4
轴承316+PTFE真实材料牌号/允许面压
下支承肩根Kt与正式强度
```

这些不阻塞继续进入前盖/连接盘草模，但在二维加工图前必须关闭。

---

# 20. V11一句话结论

> **第4步现在第一次把“球体主支承”和“阀杆导向”真正分开了：上球体由前盖φ100轴+φ105×φ100×30轴承支承，下球体由底盖φ65轴+φ70×φ65×50轴承支承；阀杆自身另用φ70×φ65×50导向。按20寸成熟反力中心比例，12寸保守164.69kN横向载荷分到上/下约81.04/83.66kN，轴承平均面压约27.0/25.7MPa。φ60则恢复为上部键轴候选，而不是整根阀杆直径。**
