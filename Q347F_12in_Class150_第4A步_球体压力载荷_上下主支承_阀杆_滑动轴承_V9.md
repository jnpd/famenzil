# Q347F 12寸 Class 150——第4A步：球体压力载荷、上下主支承、阀杆与滑动轴承 V9

> **定位**：承接第3I/V8。第3步阀座已经达到功能冻结，可以进入第4步。  
> **本页目标**：把 `2 MPa → 球体水平压力载荷 → 上下主支承反力 → φ65主支承轴颈/φ70轴承座 → φ60有效扭矩截面 → 滑动轴承面压` 串起来。  
> **重要边界**：公司 `GFE-JS01-2025` 没有在现有文本里给出“球体水平流体推力”的明确专用公式，所以本页对球体水平载荷采用**双包络工程法**：`φ303流道投影`作为基础值，`D4≈φ323.8`投影作为当前支承设计包络。后一项是工程设计包络，不冒充公司规范公式。

[← 返回主文档](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md)

---

# 1. 本页新增的状态

```text
A = 项目/BOM/公司规范明确值
B = 直接公式计算
C = 当前工程设计候选
C+ = 多条证据交叉支持的候选
D = 暂缺前置条件
H = 历史值
R = 冻结前必须关闭的风险
```

---

# 2. 先把12寸BOM的支承结构读清楚

12寸目标BOM在**底盖组**明确给出：

```text
底盖                  A182 F51
止推垫                316+PTFE  φ65×φ20×2
滑动轴承              316+PTFE  φ70×φ65×50
球体                  A182 F316
```

同一份BOM在**阀杆/前盖组**又明确给出：

```text
阀杆                  A182 F51
滑动轴承              316+PTFE  φ105×φ100×30
止推垫                316+PTFE  φ75×φ65×2
滑动轴承              316+PTFE  φ70×φ65×50
前盖                  A105N+ENP
O形圈                 VITON AED φ65×5.3 ×2
填料                  柔性石墨  φ75×φ65×5
平键                  GB/T1096 C型 18×90
```

因此V9得到一个很强的结构证据：

> **φ65不是历史猜测，而是12寸BOM反复出现的主支承/密封轴颈系列。**

同时：

```text
φ70×φ65×50
```

上下各有一只，适合把它们定义成**球体附近的上下主径向支承轴承候选**。

上部另有：

```text
φ105×φ100×30
```

它先作为上部外侧导向/支承轴承保留；它和内侧φ65轴承到底如何分担载荷，要等阀杆/前盖轴向几何完成后再分配，当前不硬猜。

---

# 3. 最大关闭压差的当前处理

项目已确认：

```text
设计压力 P = 2.0 MPa
```

但“最大关闭压差 ΔPmax”尚未另给。

为了不断链，V9采用：

```text
ΔP_V9 = 2.0 MPa
```

状态：

```text
C = 以设计压力作为当前最大关闭压差的保守包络
```

以后项目数据表如果给出实际 `ΔPmax < 2.0 MPa`，所有支承载荷按比例下降。

---

# 4. 球体水平压力载荷：双包络

## 4.1 基础值：按φ303流道投影

公式：

```text
A_bore = π d² / 4
F_bore = ΔP × A_bore
```

代入：

```text
d = 303 mm
ΔP = 2 N/mm²
```

得到：

```text
A_bore
= π×303²/4
≈ 72106.62 mm²

F_bore
≈ 144213.24 N
≈ 144.21 kN
```

这个就是以前的：

```text
≈144.2 kN
```

它继续保留为基础参考值。

## 4.2 V9支承设计包络：按D4≈φ323.8投影

第3.5步V8已经把内侧金属活塞边界落到：

```text
D4 ≈ φ323.8 mm
```

由于公司现有规范没有给“球体水平推力专用有效直径”，V9为了支承系统不低估，暂把：

```text
D_eff,V9 = D4 = 323.8 mm
```

作为**支承设计投影直径包络**。

公式：

```text
A_eff = π D_eff² / 4
F_env = ΔP × A_eff
```

代入：

```text
A_eff
= π×323.8²/4
≈ 82346.20 mm²

F_env
≈ 164692.40 N
≈ 164.69 kN
```

与φ303基础值相比：

```text
(164.69/144.21 - 1)×100%
≈ 14.20%
```

所以V9当前采用：

```text
球体水平支承设计载荷 F_support = 164.69 kN   C
```

而：

```text
144.21 kN
```

继续作为历史/基础比较值，不删除。

> 注意：`D_eff=323.8` 是当前工程包络，不是公司GFE规范明文给出的球体推力公式。最终可由公司固定球阀计算程序或FEA/压力场验证替换。

---

# 5. 上下主支承反力

定义：

```text
Ru = 上主支承水平反力
Rl = 下主支承水平反力

a = 球心到上主径向轴承反力中心距离
b = 球心到下主径向轴承反力中心距离
```

一般静力关系：

```text
Ru + Rl = F_support

Ru = F_support × b/(a+b)
Rl = F_support × a/(a+b)
```

## 5.1 V9设计意图：上下主轴承尽量对称

12寸BOM上下都给了同一规格：

```text
φ70×φ65×50
```

因此V9把：

```text
a ≈ b
```

设为**设计目标**，而不是把它当成已经测出来的事实。

如果做到：

```text
a = b
```

则：

```text
Ru = Rl = F_support/2

Ru = Rl
≈ 82346.20 N
≈ 82.35 kN
```

当前正常支承计算值：

```text
Ru,V9 ≈ 82.35 kN
Rl,V9 ≈ 82.35 kN
```

状态：B/C。

## 5.2 单个轴承局部容量再做一个“全载荷包络”

为了防止实际 `a/b`、装配公差、局部接触导致某一侧承载偏高，V9另保留：

```text
R_local,env = F_support = 164.69 kN
```

这个值**不是预期实际反力**，只用于后面询问轴承供应商或做局部结构强度上限校核。

---

# 6. φ70×φ65×50主滑动轴承面压需求

轴承平均投影面压：

```text
p = R / (d_journal × L)
```

当前：

```text
d_journal = 65 mm
L = 50 mm
```

## 6.1 对称设计工况

```text
p_nom
= 82346.20/(65×50)
≈ 25.34 MPa
```

所以：

```text
p_nom ≈ 25.34 MPa
```

## 6.2 单侧全载荷容量包络

```text
p_full
= 164692.40/(65×50)
≈ 50.67 MPa
```

所以：

```text
p_full ≈ 50.67 MPa
```

由于当前BOM只有：

```text
材料 = 316+PTFE
```

没有具体轴承厂家/衬层牌号/静载许用面压，所以本轮**不宣称“轴承合格”**。

V9冻结的是“轴承必须提供的性能需求”：

```text
正常对称工况需要承受 ≈25.34 MPa平均投影面压

局部全载荷包络达到 ≈50.67 MPa
```

最终采购/制造冻结时，供应商数据必须覆盖选定的设计判据。

---

# 7. 上部φ105×φ100×30轴承的需求量级

BOM还有：

```text
φ105×φ100×30
```

如果它承担一半水平载荷：

```text
p100,nom
= 82.346 kN/(100×30)
≈ 27.45 MPa
```

如果把全载荷作为局部容量包络：

```text
p100,full
= 164.692 kN/(100×30)
≈ 54.90 MPa
```

所以：

```text
≈27.45 MPa / ≈54.90 MPa
```

但这只说明量级。它是不是主承载轴承、和φ65轴承如何分载，当前保持D，等前盖/阀杆截面完成后确定。

---

# 8. 下支承轴：φ65现在可以从“历史猜测”升级

底盖组：

```text
底盖 A182 F51
+
φ70×φ65×50轴承
+
球体
```

说明V9最自然的结构链是：

```text
底盖F51下支承轴颈 ≈ φ65
      ↓
316+PTFE轴承 ID φ65 / OD φ70 / L50
      ↓
球体下部轴承孔 ≈ φ70
```

所以：

```text
下支承轴颈 d_lower = φ65         C+
球体下轴承孔 D_lower_bore = φ70 C+
```

## 8.1 球体下孔深度纠错

旧主文档有：

```text
φ70×40深，R4
```

但BOM轴承本身长度就是：

```text
50 mm
```

因此：

```text
40 mm深
```

无法完整容纳50mm轴承，应降为：

```text
H/R = 历史错误/过早草模
```

最低几何要求：

```text
H_lower ≥ 50 mm
```

此前20→12推演台账已有：

```text
50 mm轴承 + 约2 mm入口/过渡
→ 下孔总深约52 mm
```

因此V9 CAD候选改为：

```text
球体下轴承孔 = φ70 × 52深   C
```

R4继续作为加工/应力过渡候选，最终由根部局部强度和刀具冻结。

---

# 9. 下支承轴第一轮弯曲校核

还缺精确底盖剖面，所以真正根部到轴承载荷合力点的悬臂距离 `e` 还没有制造尺寸。

但BOM给了：

```text
轴承长度 = 50
止推垫厚 = 2
```

如果第一轮把轴承合力看作作用在长度中点：

```text
L/2 = 25 mm
```

并把2mm止推垫/界面厚度加入第一轮空间量级：

```text
e_V9 ≈ 25 + 2 = 27 mm
```

状态：C，仅用于敏感性，不是最终底盖尺寸。

## 9.1 对称反力工况

```text
Rl = 82.346 kN
d = 65 mm
e = 27 mm
```

弯矩：

```text
M = Rl × e
  ≈ 2.223×10^6 N·mm
```

圆轴外纤维简化弯曲应力：

```text
σb = 32M/(πd³)
≈ 82.46 MPa
```

平均直接剪应力量级：

```text
τ = 4R/(πd²)
≈ 24.82 MPa
```

为了给第一轮一个保守组合量级，暂写：

```text
σeq,simple = √(σb² + 3τ²)
≈ 92.99 MPa
```

注意：弯曲最大应力与横向剪切最大值在圆截面上并不严格同点，因此这个 `≈93 MPa` 是**简化偏保守量级**，不能代替FEA和根部应力集中。

当前：

```text
φ65下支承轴
对称主工况简化等效应力量级 ≈93.0 MPa
```

## 9.2 单侧全载荷包络

同样取：

```text
R = 164.692 kN
e = 27 mm
d = 65 mm
```

得到：

```text
σb ≈ 164.93 MPa
τ  ≈ 49.63 MPa
σeq,simple ≈ 185.99 MPa
```

也就是：

```text
≈186 MPa
```

这是局部极端容量包络，不是正常预计载荷。

## 9.3 V9对底盖几何提出的要求

为了不让悬臂弯曲快速上升，V9给底盖/下轴第一轮包装目标：

```text
e_target ≈27 mm
e_design 尽量 ≤30 mm
```

如果后续底盖几何使：

```text
e > 30 mm
```

必须重新计算φ65轴颈，不能继续沿用本页结果。

---

# 10. 阀杆：把“φ60”和BOM的“φ65”统一起来

12寸BOM同时给了：

```text
φ70×φ65×50 轴承
φ75×φ65×2 止推垫
φ65×5.3 O形圈
φ75×φ65×5 填料
```

所以实际阀杆/主支承密封轴颈存在：

```text
φ65
```

这一系列是有BOM证据的。

而以前计算中的：

```text
φ60
```

更适合继续作为：

> **阀杆最小有效/净扭矩截面保守计算直径。**

因此V9不再写成“整根阀杆就是φ60”，而改成：

```text
阀杆主支承/密封名义轴颈 ≈ φ65   C+
阀杆保守有效扭转直径     = φ60   C
```

这样BOM和强度计算不再互相冲突。

---

# 11. φ60有效截面扭转敏感性

实心圆轴简化：

```text
τ = 16T/(πd³)
```

取保守有效直径：

```text
d_eff = 60 mm
```

已有：

```text
T =1500 N·m → τ≈35.37 MPa
T =1800 N·m → τ≈42.44 MPa
```

V9新增两个扭矩敏感性，不代表正式扭矩已经变成这些值：

```text
1.5×1800 = 2700 N·m
→ τ≈63.66 MPa

2×1800 = 3600 N·m
→ τ≈84.88 MPa
```

公司规范原文要求：

```text
“阀门强度设计按杆材料许用应力的1.5倍校核”
```

同时公司还要求通过固定球阀计算程序确定阀杆上/下扁方或键槽尺寸。

由于这句话的公司内部接受口径和F51在正式设计温度下的许用应力尚未取得，本页不擅自把它改写成某个安全系数公式。

所以φ60当前结论：

> **作为保守有效净截面继续设计没有问题；最终合格仍需公司计算程序 + F51温度许用值 + 最小净截面/键槽联合校核。**

---

# 12. 上部键：18×90与φ60/φ65系列互相对上

12寸BOM：

```text
GB/T1096 C型平键 18×90
```

公司固定球阀规范键表对：

```text
轴径 >58～65 mm
```

给出：

```text
键 b×h = 18×11
```

所以V9可以把当前键理解成：

```text
18×11×90
```

其中：

```text
18×90 = BOM已给
h=11   = 公司轴径系列表对应值
```

这对 `φ60～φ65` 阀杆系列是很强的交叉验证。

## 12.1 单键最不利简化校核

公司规范说上部可设计双键槽，但除特殊情况装配时只装一个键，因此强度敏感性先按**一只键承担全部扭矩**。

切向力：

```text
Ft = 2T/d
```

暂取：

```text
d = 60 mm
b = 18 mm
h = 11 mm
L = 90 mm
```

在1800 N·m：

```text
Ft = 60.0 kN

键剪应力量级
τkey = Ft/(bL)
≈ 37.04 MPa

键侧面挤压应力量级
σkey = 2Ft/(hL)
≈ 121.21 MPa
```

在2×1800=3600 N·m敏感性：

```text
Ft = 120 kN
τkey ≈ 74.07 MPa
σkey ≈ 242.42 MPa
```

键材料BOM为A29 1045，但当前没有项目正式温度下允许剪切/挤压值，所以这里只形成应力需求，不写“最终通过”。

---

# 13. 阀杆装配间隙

公司固定球阀规范明确要求：

```text
阀杆与球体之间
阀杆与阀盖之间

均留 1～3 mm 足够间隙
```

目的是防止阀杆顶住球体增加额外扭矩。

V9 SolidWorks建议先用中值：

```text
G_stem,CAD = 2.0 mm
```

状态：C。

制造冻结时必须保证公差叠加后仍满足公司 `1～3 mm` 功能范围。

---

# 14. V9当前可以直接画的第4步骨架

| 字段 | V9值 | 状态 |
|---|---:|---|
| 最大关闭压差暂用 | 2.0 MPa | C；等于设计压力的当前包络 |
| φ303基础球体压力载荷 | 144.21 kN | B/H参考 |
| V9支承有效投影径 | φ323.8 | C；取当前D4作工程包络 |
| V9球体水平支承载荷 | **164.69 kN** | B/C |
| 上下主支承设计目标 | **a≈b** | C |
| 上主反力 | **82.35 kN** | B/C；a=b |
| 下主反力 | **82.35 kN** | B/C；a=b |
| 单侧局部容量包络 | 164.69 kN | C；非预期实际反力 |
| 上/下内侧主轴承 | **φ70×φ65×50** | A/C+；BOM上下各1 |
| 主轴承正常面压 | **25.34 MPa** | B/C |
| 主轴承全载荷包络面压 | **50.67 MPa** | B/C |
| 上外侧轴承 | φ105×φ100×30 | A；功能/分载待定 |
| 下支承轴颈 | **φ65** | C+ |
| 球体下轴承孔 | **φ70** | C+ |
| 球体下孔最低深度 | **≥50** | B |
| 球体下孔CAD候选 | **φ70×52深** | C |
| 旧φ70×40深 | 作废为H/R | H/R |
| 下支承第一轮e | **≈27 mm** | C |
| e包装目标 | **≤30 mm** | C |
| φ65下轴正常简化等效应力量级 | **≈93 MPa** | B/C；未含Kt |
| φ65下轴全载荷简化等效包络 | **≈186 MPa** | B/C/R；未含Kt |
| 阀杆主支承/密封轴颈 | **φ65** | C+ |
| 阀杆保守有效扭转直径 | **φ60** | C |
| φ60 τ@1800 | **42.44 MPa** | B |
| φ60 τ@2700敏感性 | **63.66 MPa** | B |
| φ60 τ@3600敏感性 | **84.88 MPa** | B |
| 键 | **18×11×90** | C+；BOM+公司系列表 |
| 单键τ@1800 | **37.04 MPa** | B/C |
| 单键挤压@1800 | **121.21 MPa** | B/C |
| 单键τ@3600敏感性 | **74.07 MPa** | B |
| 单键挤压@3600敏感性 | **242.42 MPa** | B |
| 阀杆—球体/阀盖间隙 | 公司1～3；CAD先取2.0 | A/C |

---

# 15. V9还不能制造冻结的地方

现在第4步已经不是“没有尺寸”，而是剩下面这些签核点：

```text
① 真正a、b：
   等上下主轴承在总装中的中心位置定完后测量

② 真正e：
   底盖φ65轴颈根部到φ70×φ65×50轴承合力中心

③ 316+PTFE具体轴承厂家/衬层：
   需要静载许用面压，至少覆盖项目选定的25.34/50.67MPa判据

④ F51正式温度许用应力：
   用于阀杆、底盖下支承轴最终强度

⑤ φ65轴根圆角/应力集中：
   R4暂为候选，最终需局部强度/FEA

⑥ φ105×φ100×30上外侧轴承实际作用：
   需前盖/阀杆剖面闭合

⑦ 1800 N·m正式定义：
   决定最终校核扭矩到底取1800、1.5×还是项目另值
```

---

# 16. 下一步

V9之后继续：

```text
第4B
阀杆分段几何
φ100外支承段
φ65密封/内支承段
φ60有效净截面
防吹出肩
O圈/填料/前盖接口
↓
第4C
底盖φ65下轴
φ70×φ65×50轴承
止推垫
φ58×5.3 O圈
φ80×φ70×3.2缠绕垫
6×M12×55
完整轴向尺寸链
↓
回代a/b/e
↓
重新计算支承反力、轴弯曲、轴承面压
```

> **V9阶段结论**：`φ65主支承轴颈 + φ70×φ65×50上下主轴承 + φ60保守有效扭转截面` 已经形成一条能继续设计的主链。最重要的纠错是：旧的 `φ70×40深` 球体下孔不再有效，V9改为 `最低≥50，CAD候选52深`。
