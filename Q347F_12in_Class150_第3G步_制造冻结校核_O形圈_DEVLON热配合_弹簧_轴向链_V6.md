# Q347F 12寸 Class 150——第3G页：制造冻结校核与 V6 纠错

> **定位**：承接第3F。第3F第一次把阀座轴向链闭合到 `Wseat≈58 mm`，但本轮继续核对 O 形圈安装规则、20寸原生 SolidWorks 轴向结构、DEVLON 热膨胀敏感性以及 X-750 弹簧后，发现 **V5 的部分内部轴向站位不能作为制造候选继续使用**。
>
> **当前有效版本：V6。**
>
> [← 返回主文档](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md) ｜ [← 第3F页（历史V5）](./Q347F_12in_Class150_第3F步_阀座轴向尺寸链_DEVLON保持_第二O圈_弹簧安装高度_Wseat_V5.md)

---

# 1. 一句话结论

当前阀座不是“全都没算好”，而是已经分成三类：

| 状态 | 当前项目 | 结论 |
|---|---|---|
| 🟢 可继续画 V6 草模 | D9/D10、D11、主O圈径向链、弹簧PCD、大端外径 | 保留 |
| 🟡 可画候选但不能下制造图 | Wseat≈58、DEVLON宽约10、弹簧安装高约15.6 | 继续校核 |
| 🔴 暂时禁止冻结 | 第二O圈真实位置、DEVLON热配合、石墨绳槽、V5内部轴向站位 | 必须修正/补资料 |

最重要的纠错：

```text
V5的 A0/A1/.../A7 内部站位
以及
“主O圈 → 石墨 → 后部弹簧区”的顺序

不再作为当前有效制造链。
```

20寸原生结构更接近：

```text
球体
↓
DEVLON软密封
↓
弹簧作用区
↓
导向 / 防火石墨过渡区
↓
主活塞O圈
↓
后导向
```

因此 V6 保留 `Wseat≈58 mm` 作为**总空间包络**，但内部站位重新开放，不再硬填数字。

---

# 2. 当前仍然有效的径向主链

```text
球体球面           R232.5

真实软密封接触带
D9                 ≈ φ323.88
D10                ≈ φ327.13

D11主静压/导向OD   = φ342

主O圈              φ320×5.3
主O圈槽底OD        = φ333.6
阀盖主导向孔       = φ342.4（候选）

36×φ8.5弹簧孔/侧
弹簧PCD            = φ362

支承圈大端OD       = φ380（候选）
阀盖大端孔         = φ382（候选）
```

这部分继续作为 V6 草模的主骨架。

---

# 3. 冻结项1：φ320×5.3 主O圈——本轮由“风险”提升为“暂通过”

公司 `GFE-JS01-2025` 对截面 `5.3 mm` 给出：

```text
静密封槽深 = 4.2 mm
槽宽       = 7 mm
z          = 4 mm
r1         = 0.6 mm
r2         = 0.2 mm
```

当前：

```text
O圈自由ID = 320
槽底OD    = 333.6
```

安装拉伸：

```text
S = (333.6-320)/320
  = 4.25%
```

Trelleborg O-Ring Catalogue（2024版）对活塞外密封给出：
- 动态：O圈内径拉伸 2%～5%
- 静态：O圈内径拉伸 2%～8%
并要求拉伸后按截面缩小量重新校核压缩率。

来源：
https://www.trelleborg.com/seals/-/media/tss-media-repository/tss_website/pdf-and-other-literature/catalogs/o_ring_gb_en.pdf

因此：

```text
4.25%
```

落在静态 2%～8% 范围内，也没有超过动态5%的常用上限。

## 3.1 拉伸后截面不能仍按5.3直接算

Trelleborg 对 `S>3%` 给出：

```text
R = 0.56 + 0.59S - 0.0046S²
```

S=4.25：

```text
R ≈ 2.984%
```

所以拉伸后的有效截面：

```text
d_eff
= 5.3 × (1-2.984%)
≈ 5.142 mm
```

当前径向槽高：

```text
h
= (342.4-333.6)/2
= 4.4 mm
```

有效压缩率：

```text
ε
= (5.142-4.4)/5.142
≈ 14.43%
```

## 3.2 和20寸成熟结构直接对比

20寸成熟结构：

```text
O圈自由ID      = 510
槽底OD         = 520.2
阀盖孔         = 532
```

安装拉伸：

```text
(520.2-510)/510
= 2.0%
```

按同一Trelleborg公式，拉伸后的有效截面约：

```text
6.879 mm
```

20寸实际径向槽高：

```text
(532-520.2)/2
= 5.9 mm
```

有效压缩率：

```text
≈14.23%
```

对比：

```text
20寸成熟结构：≈14.23%
12寸V6：      ≈14.43%
```

两者几乎一致。

### 当前结论

```text
φ320×5.3
槽底 φ333.6
导向OD φ342
阀盖孔 φ342.4
```

可以从“未确认风险”提升为：

> **C+：强交叉验证通过，可用于V6草模；最终仍需配合公差、VITON具体牌号/硬度确认。**

Parker O-Ring Handbook 也明确指出，活塞槽中 O 圈拉伸超过2%～3%时必须考虑截面缩小，欧洲手册给出的活塞槽装配拉伸可到约6%，与上述结论方向一致。

Parker：
https://www.parker.com/content/dam/Parker-com/Literature/O-Ring-Division-Literature/ORD-5700.pdf

---

# 4. 冻结项2：φ311×3.55 第二O圈——尺寸本身能闭合，但“位置”不能冻结

12寸BOM明确存在：

```text
VITON φ311×3.55 ×2
```

但项目20→12对应表也明确写着：

> 12寸阀座密封组多出一条 O 形圈规格，需结合装配位置确认。

所以“有这个O圈”是确定的，但“它一定装在DEVLON内孔下面”不是确定的。

## 4.1 V5位置只做室温几何检查

V5假设：

```text
金属spigot OD = φ323.6
O圈槽底OD     = φ318.0
DEVLON ID     = φ323.8
O圈           = φ311×3.55
```

自由ID拉伸：

```text
S
= (318-311)/311
≈ 2.25%
```

按Trelleborg `S≤3%` 公式：

```text
R = 0.01 + 1.06S - 0.1S²
```

得到截面缩小约：

```text
R ≈ 1.89%
```

有效截面：

```text
d_eff ≈ 3.483 mm
```

径向槽高：

```text
h = (323.8-318)/2
  = 2.9 mm
```

有效压缩率：

```text
ε ≈16.74%
```

所以：

> **单看20℃室温几何，V5这个位置是能装得下的。**

但这还不等于可以冻结。

---

# 5. 为什么第二O圈位置必须退回：DEVLON热膨胀问题

公司当前规范只写：

```text
DEVLON
适用温度 -29～120℃
```

但没有给本项目具体 DEVLON 牌号，也没有给热膨胀系数。

James Walker 当前官方页面说明 Devlon V-API 是阀座专用材料，公开性能范围为约 `-50～+176℃`，但本项目并没有证据表明实际采购牌号已经锁定为 V-API。

官方产品页：
https://www.jameswalker.biz/our-solutions/our-products/engineered-thermoplastics/devlon-materials/devlon-v-api

为了判断V5结构是否敏感，可暂用一份公开的 Devlon V-API 旧技术数据做**敏感性示例**：
- 30～100℃线膨胀系数约 `1.11×10^-4 /℃`

注意：这个数只用于风险判断，不作为本项目最终材料数据。

AISI 316/316L 的典型20～100℃热膨胀系数约 `16×10^-6/K`（Outokumpu公开数据）。

如果从20℃估算到公司DEVLON范围：

### 到 -29℃

```text
ΔT = -49℃
```

DEVLON相对F316的额外收缩量（以直径约323.8计）：

```text
ΔDdiff
≈ (111-16)×10^-6 × 49 × 323.8
≈ 1.51 mm
```

而V5原本给的：

```text
DEVLON ID - metal spigot OD
= 323.8 - 323.6
= 0.2 mm（直径间隙）
```

1.51 mm远大于0.2 mm。

这意味着：

> **如果实际材料热膨胀接近V-API公开典型值，V5“DEVLON紧套φ323.6金属spigot”的结构在低温端会有明显干涉风险。**

### 到 +120℃

同样做敏感性：

```text
ΔT = +100℃
相对直径膨胀约 ≈ 3.08 mm
```

这又会改变保持结构、O圈压缩和DEVLON约束状态。

### 当前结论

V5这条：

```text
spigot φ323.6
root φ318
DEVLON ID φ323.8
第二O圈槽位置 2.5～7.3
```

正式降级为：

```text
H/R = 历史室温几何候选
```

**禁止作为制造图尺寸。**

现在第二O圈应保持：

```text
φ311×3.55 = 已确认BOM
真实安装位置 = ?
槽底直径 = ?
配合对象 = ?
```

直到拿到：

```text
① 项目最终设计温度上下限
② DEVLON具体牌号/厂家材料数据
```

再重新定。

---

# 6. 冻结项3：3×3防火石墨绳——BOM确认，沟槽尺寸没有公司依据

12寸BOM和20寸成熟BOM都存在：

```text
防火石墨绳 3×3
```

因此：

```text
材料/规格 = A
```

但目前公司固定球阀规范没有给：

```text
石墨槽宽
石墨槽深
预压缩量
径向位置
轴向位置
```

第3F曾用：

```text
4 mm轴向包装预留
```

只是空间占位。

所以V6状态：

```text
3×3石墨绳规格 = A
石墨槽几何     = D
```

不能因为绳子是3×3就直接画3×3槽，也不能套用阀杆石墨填料10%～15%压缩规则；那是另一种填料结构。

---

# 7. 冻结项4：X-750弹簧——几何基本对上，但热处理决定能否放行

目标BOM：

```text
Inconel X-750
Do = φ8
d  = φ1.6
H0 = 18
Nt = 7
72只/台 = 36只/侧
```

真实密封面积对应：

```text
Q2 ≈ 4.146 kN/侧
单簧目标 F ≈ 115.18 N
```

若按常见闭口端近似：

```text
有效圈 Na ≈ Nt-2 ≈ 5
```

取 X-750 室温剪切模量：

```text
G ≈ 77.2 GPa
```

弹簧刚度：

```text
k = G d^4 / (8 Dm^3 Na)

Dm = 8-1.6 = 6.4
```

得到：

```text
k ≈ 48.25 N/mm
```

所需压缩量：

```text
δ = 115.18/48.25
  ≈ 2.39 mm
```

安装高度：

```text
Hinstall
= 18-2.39
≈ 15.61 mm
```

因此第3F的：

```text
15.5 mm
```

并不是乱取，和7总圈/约5有效圈能对上。

V6建议草模更新为：

```text
Hinstall ≈ 15.6 mm
```

## 7.1 为什么仍不能制造冻结

弹簧指数：

```text
C = Dm/d = 6.4/1.6 = 4
```

Wahl系数约：

```text
Kw ≈ 1.4038
```

115.18N时最大修正剪应力：

```text
τ ≈ 643 MPa
≈ 93.3 ksi
```

Special Metals X-750官方资料显示，不同线材状态和热处理的室温剪切比例极限差别很大，例如约：
- 54 ksi
- 67.5 ksi
- 90.5 ksi
- 93.5 ksi

其中某一 Spring Temper + 1350°F/16h 状态约93.5ksi。

官方资料：
https://www.specialmetals.com/documents/technical-bulletins/inconel/inconel-alloy-x-750.pdf

因此当前115N工况已经接近某些热处理状态的比例极限。

如果防火时阀座再走约0.9mm，按当前线性刚度估计：

```text
Ffire ≈ 158.6 N/只
τfire ≈ 886 MPa
       ≈ 128.5 ksi
```

所以：

> **弹簧几何可以继续画，但必须让公司弹簧计算程序/供应商把X-750线材状态和热处理一起确认。**

当前状态：

```text
φ8×1.6×18×7 = A/C
36只/侧       = A/B
Hinstall≈15.6 = C
热处理         = D
最终允许应力   = D
```

---

# 8. 冻结项5：真正的轴向浮动量

公司要求：

```text
正常：
DEVLON密封

火灾：
DEVLON失效后
阀座继续向球体移动
金属防火面接球

正常金属防火间隙：
0.8～1.0 mm
```

当前草模：

```text
gfire = 0.9 mm
```

所以阀座系统至少必须能够提供：

```text
≥0.9 mm
```

的功能行程，否则火灾时金属面根本接不到球。

但这个0.9mm：

```text
≠ 最终总轴向浮动量
```

最终还要包含：
- 制造公差；
- O圈摩擦/拖曳；
- DEVLON热膨胀；
- 弹簧压缩；
- 球体/阀座装配偏差；
- 自泄压开启需要的实际运动。

因此当前：

```text
功能最小行程 ≥ 0.9 mm       B/C
最终设计浮动量 = ?           D
```

---

# 9. V5轴向站位为什么必须撤回

20寸原生 SolidWorks 几何显示大致顺序：

```text
球侧软密封
x≈ -278.8 ～ -265.8

弹簧孔/弹簧作用区
约 x≈ -297.8 ～ -278.8

主导向/过渡区
继续向后

主O圈槽
约 x≈ -324 ～ -316.6

后导向
```

也就是说成熟结构的功能顺序更像：

```text
DEVLON
→ 弹簧
→ 石墨/导向过渡
→ 主O圈
→ 后导向
```

而第3F V5曾把：

```text
DEVLON
→ 前部金属
→ K4
→ 主O圈
→ K3
→ 石墨
→ 后导向/弹簧
```

作为第一次包装。

两者不一致。

因此V6正式撤回：

```text
A0=0
A1=10
A2=22
A3=27
A4=34
A5=38
A6=42
A7=58
```

作为制造轴向站位的资格。

这些数字只保留在第3F作为“第一次包装尝试”的历史记录。

---

# 10. Wseat=58现在到底还能不能用

可以，但含义改变：

```text
Wseat≈58 mm
```

当前只表示：

> **12寸金属阀座支承圈的第一轮总轴向空间包络。**

它来自20寸成熟结构总宽约91mm的系列空间交叉，也能容纳当前10mm级DEVLON、约15.6mm弹簧安装高度、主O圈7mm槽及必要肩部。

但：

```text
58 ≠ 已冻结制造宽度
```

V6状态：

```text
Wseat ≈ 58 mm = C（总包络）
内部各站位   = D（重新布置）
```

---

# 11. 现在SolidWorks应该怎么画

为了不返工，V6只画“稳定骨架”。

## 可以画

```text
球体 R232.5 / φ465
流道 φ303

D9≈φ323.88
D10≈φ327.13

D11/主导向OD φ342
主O圈槽底 φ333.6
主O圈槽宽7
阀盖主导向孔 φ342.4

36×φ8.5
PCD φ362

支承圈大端 φ380
阀盖大端孔 φ382

Wseat总包络先取58

DEVLON：
球侧 R232.5
物理ID/OD先按当前草模
轴向宽只做约10占位
```

## 暂时不要画死

```text
φ311×3.55第二O圈槽的位置
第二O圈槽底直径
DEVLON与金属spigot最终间隙
3×3石墨绳真实槽
内部A0～A7轴向站位
X-750弹簧最终热处理
最终阀座浮动量
```

SolidWorks里建议把这些待定槽先做成：

```text
独立草图 / 独立特征
名称加 _PENDING
```

不要和主体关键旋转特征绑死。

---

# 12. 现在只剩两个最重要的外部输入

后面不用再让设计人员自己推公式。

只需要从项目数据表/订单/材料证明里找到：

```text
① 最低设计温度 / 最高设计温度

② DEVLON完整牌号
   例如是否明确写 Devlon V-API / JW DV-API
   以及厂家材料数据表
```

这两个一拿到，就可以正式做：

```text
DEVLON热胀冷缩
→ 第二O圈位置
→ DEVLON保持间隙
→ 石墨槽
→ 最终浮动量
→ 轴向站位
→ Wseat制造候选
```

---

# 13. V6状态总表

| 项目 | 当前值 | V6状态 |
|---|---:|---|
| D9 | φ323.88 | 🟢 B/C |
| D10 | φ327.13 | 🟢 B/C |
| D11 | φ342 | 🟢 C |
| 主O圈 | φ320×5.3 | 🟢 A/C |
| 主O圈槽底 | φ333.6 | 🟢 C+ |
| 主O圈有效拉伸 | 4.25% | 🟢 通过外部手册交叉 |
| 主O圈有效压缩 | 14.43% | 🟢 与20寸14.23%高度吻合 |
| 弹簧PCD | φ362 | 🟢 C |
| 支承圈大端 | φ380 | 🟢/🟡 C |
| 阀盖大端孔 | φ382 | 🟢/🟡 C |
| Wseat | ≈58 | 🟡 仅总包络 |
| DEVLON宽 | ≈10 | 🟡 仅包装候选 |
| 第二O圈规格 | φ311×3.55 | 🟢 A |
| 第二O圈真实位置 | ? | 🔴 D/R |
| 第二O圈V5 spigot链 | 作废为制造候选 | 🔴 H/R |
| 石墨绳规格 | 3×3 | 🟢 A |
| 石墨槽 | ? | 🔴 D |
| 弹簧安装高 | ≈15.6 | 🟡 C |
| X-750热处理 | ? | 🔴 D |
| 防火功能最小行程 | ≥0.9 | 🟡 B/C |
| 最终阀座浮动量 | ? | 🔴 D |
| V5 A0～A7轴向站位 | 撤回 | 🔴 H |

---

# 14. 下一步

下一页不要再盲目加尺寸。

下一步应是：

```text
确定项目设计温度 + DEVLON牌号
↓
热配合计算
↓
重定第二O圈
↓
确定石墨槽
↓
确定真实轴向浮动量
↓
重排V6轴向站位
↓
得到V7制造候选截面
```

在这之前，当前V6已经足够用于 **SolidWorks结构草模/干涉验证**，但不用于出制造图。