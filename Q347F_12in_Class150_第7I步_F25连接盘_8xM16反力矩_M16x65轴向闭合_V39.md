# Q347F 12寸 Class150——第7I步：F25连接盘 / 8×M16反力矩 / M16×65轴向闭合 V39

> **定位**：V37把上部驱动标准接口收敛到 ISO 5211 F25；V38证明φ60键轴按单个18×11×90键承担1800N·m仍可继续CAD。V39开始把“连接盘”本体真正画出来：关闭F25孔阵列、8×M16的执行器壳体反力矩、以及M16×65螺柱的轴向长度预算。
>
> **核心结论**：ISO 5211:2026现行F25接口为 `d1≥φ300 / d2≤φ200 / PCDφ254 / 8×M16 / 普通间隙孔φ17.5`，8孔相对主轴起始角22.5°。1800N·m若最保守地完全通过8颗M16孔组承担壳体反力矩，每颗切向力仅约1.77kN；即使只按4颗有效也约3.54kN/颗。M16×65在扣除2型螺母和2~3牙露出后，剩余夹持+锚固预算约42.6~45.3mm；配合当前24mm连接盘厚度，给蜗轮侧锚固/法兰留下约18.6~21.3mm，轴向链自洽。

[← 总导航](./00_Q347F_12in_Class150_文档导航_从这里开始.md)  
[← 数字化总装骨架总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)  
[← V37 ISO5211 F25](./Q347F_12in_Class150_第7G步_连接盘_ISO5211_F25_8xM16与1800Nm驱动接口_V37.md)  
[← V38 φ60阀杆/单键传扭](./Q347F_12in_Class150_第7H步_φ60阀杆_18x11x90单键传扭_键槽净截面_V38.md)

---

# 1. ISO 5211版本口径升级到2026现行版

当前现行标准：

```text
ISO 5211:2026
Industrial valves — Part-turn actuator attachments
```

2026版F25主尺寸继续为：

```text
d1 min = φ300

d2 max = φ200

d3 / PCD = φ254

thread = M16
clearance hole = φ17.5
n = 8
```

所以V37使用的F25主体判断不变，只把标准版本更新为2026。

---

# 2. F25孔阵列方向也可直接关闭

ISO 5211对F25~F40给出：

```text
α/2 = 22.5°
```

8孔圆周间隔：

```text
360/8=45°
```

因此第一孔中心相对主X/Y基准轴：

```text
22.5°
```

后续孔：

```text
22.5
67.5
112.5
157.5
202.5
247.5
292.5
337.5°
```

SolidWorks变量：

```text
F25_HOLE_QTY=8
F25_HOLE_PCD=254
F25_HOLE_D=17.5
F25_HOLE_START_ANGLE=22.5deg
F25_HOLE_STEP=45deg
```

状态：`A/STD`。

---

# 3. φ300是标准最小接口落脚外径，不是随便放大的装饰圆盘

F25：

```text
d1 min=φ300
```

其几何意义之一是保证M16螺栓/螺母或头部周围有最低支承落脚空间。

当前：

```text
R_outer=150
R_bolt=254/2=127
```

螺栓中心到外缘：

```text
150-127=23 mm
```

2026版普通间隙孔：

```text
φ17.5 → r=8.75
```

孔边到标准最小外缘净肉：

```text
23-8.75
=14.25 mm
```

所以：

```text
ADAPTER_OD_CAD=300 mm
```

可以直接作为F25标准最小CAD外径。

最终外形可因：

```text
铸/锻件圆角
吊装/扳手空间
厂家底座外形
局部加强
```

做得更大，但不得无依据小于标准接口要求。

---

# 4. 连接盘旧“20寸缩比外径”不能再控制12寸

20寸原生连接盘外包络约：

```text
φ350级
```

若简单乘465/748：

```text
≈φ217.6
```

这会小于当前F25：

```text
φ300 min
```

因此新增永久规则：

```text
20寸连接盘外径线性缩放
→ H/R-for-current-F25-interface
```

12寸连接盘必须由当前F25接口重新长出来，而不是把20寸连接盘等比例缩小。

---

# 5. 1800N·m的执行器壳体反力矩由8孔组承担

阀杆键传递：

```text
执行器输出轴 → 键 → φ60阀杆
```

同时执行器壳体会通过安装法兰向连接盘施加等大反向反力矩。

当前：

```text
T=1,800,000 N·mm
R_bolt=127 mm
n=8
```

如果忽略摩擦，最保守地把反力矩全部按螺栓孔组切向承载：

```text
F_each
=T/(n×R)
=1,800,000/(8×127)
≈1771.65 N
≈1.77 kN/颗
```

---

# 6. 即使只让一半螺栓有效，载荷仍不大

考虑装配间隙、孔位误差，不给8颗完美均载信用，做4颗有效筛查：

```text
F_each_4
=1,800,000/(4×127)
≈3543.3 N
≈3.54 kN/颗
```

所以：

```text
F25_BOLT_TANGENTIAL_8≈1.77kN
F25_BOLT_TANGENTIAL_4≈3.54kN
```

这是“无摩擦、直接孔壁/螺栓承担反力矩”的保守几何筛查。

实际预紧法兰可通过摩擦传递部分反力矩，因此真实螺栓剪切通常不会比该假设更差。

---

# 7. M16螺栓/螺柱剪切应力粗筛

M16粗牙P=2的有效拉应力面积约：

```text
As≈157 mm²
```

如果错误/保守地把螺纹有效面积直接作为剪切面积：

8颗均载：

```text
τ≈1771.7/157
≈11.3 MPa
```

4颗有效：

```text
τ≈3543.3/157
≈22.6 MPa
```

M16光杆面积：

```text
A=π×16²/4≈201.1mm²
```

对应：

```text
8颗均载≈8.8MPa
4颗有效≈17.6MPa
```

当前结论：

> 从1800N·m反力矩造成的纯切向载荷看，8×M16没有出现几何上一算就偏小的问题。最终仍需叠加预紧、弯矩、冲击及材料/温度要求。

---

# 8. M16×65轴向预算

12寸BOM：

```text
M16×65 双头螺柱 ×8
M16 2型六角螺母 ×8
```

GB/T 6175-2016 M16 2型螺母：

```text
P=2mm
m≈15.7~16.4mm
```

沿用公司统一露出：

```text
2~3牙
→4~6mm
```

所以65mm总长扣除螺母和露牙：

```text
L_grip_plus_anchor
=65-m-protrusion
```

最小：

```text
65-16.4-6
=42.6mm
```

最大：

```text
65-15.7-4
=45.3mm
```

得到：

```text
F25_STUD_GRIP_PLUS_ANCHOR≈42.6~45.3mm
```

---

# 9. 24mm连接盘厚度第一次得到独立支持

V11历史几何比例候选：

```text
ADAPTER_T_CAD≈24mm
```

过去只是：

```text
20寸38mm×465/748≈23.6mm
```

现在用M16×65重新检查。

若连接盘轴向被夹持厚度：

```text
24mm
```

则剩余给：

```text
蜗轮侧法兰厚度 / 螺柱有效锚固
```

的空间：

```text
42.6~45.3-24
≈18.6~21.3mm
```

即：

```text
GEAR_SIDE_GRIP_OR_ENGAGE_SPACE≈18.6~21.3mm
```

这个结果在几何上是合理的。

因此：

```text
ADAPTER_T_CAD=24
```

从原来的：

```text
C/H-guide
```

升级为：

```text
C
```

因为现在有第二条独立的M16×65轴向长度证据。

最终厂家蜗轮法兰厚度到位后再制造冻结。

---

# 10. ISO 5211本身也说明24mm这个数量级合理，但不冒充最终厚度

ISO 5211 F25表中存在24mm级的最小轴向接口尺寸要求。

V39只把它作为：

```text
标准数量级交叉证据
```

不把“24”偷换成“本项目连接盘最终厚度=24”。

因为真实连接盘还要承担：

```text
STEM_COVER→ADAPTER 4×M12×50
填料压紧
φ60键轴穿过
蜗轮厂家止口
8×M16落脚区
```

所以：

```text
ADAPTER_T_FINAL=? D
```

不变。

---

# 11. F25中心止口φ200只能作为最大包络

标准：

```text
d2 max=φ200
```

不能写成：

```text
实际止口一定=φ200
```

当前正确参数：

```text
F25_SPIGOT_D_MAX=200
GEARBOX_SPIGOT_D_FINAL=?
ADAPTER_RECESS_D_FINAL=?
```

为了草模防干涉可以建立：

```text
ENV_F25_CENTERING_MAX=φ200
```

但不在零件上切死φ200止口，除非厂家接口图确认。

---

# 12. F25_VENDOR_PLACEHOLDER第一版

SolidWorks建议新增：

```text
11_F25_GEARBOX_PLACEHOLDER.SLDPRT
```

只建立：

```text
底面接口外径包络≥φ300
8×φ17.5孔
PCDφ254
起始角22.5°
中心止口最大包络φ200
φ60输入轴/孔功能轴线
```

不要建立：

```text
真实蜗轮箱壳体高度
手轮位置
减速比
真实输入孔
真实键槽方向
```

这些全部由厂家模型替换。

---

# 13. 当前SolidWorks变量

```text
# ISO 5211:2026 F25
ISO5211_REV=2026
ISO5211_FLANGE_CAD=F25
F25_OD_MIN=300
F25_SPIGOT_D_MAX=200
F25_BOLT_PCD=254
F25_BOLT_QTY=8
F25_BOLT_SIZE=M16
F25_CLEAR_HOLE_D=17.5
F25_HOLE_START_ANGLE=22.5
F25_HOLE_STEP=45

# ADAPTER CAD
ADAPTER_OD_CAD=300
ADAPTER_T_CAD=24
ADAPTER_T_FINAL=?

# REACTION TORQUE SCREEN
F25_REACTION_TORQUE=1800000
F25_BOLT_RADIUS=127
F25_BOLT_TANGENTIAL_8=1771.65
F25_BOLT_TANGENTIAL_4=3543.3
F25_M16_THREAD_AREA≈157
F25_M16_SHEAR_8≈11.3
F25_M16_SHEAR_4≈22.6

# M16x65 AXIAL STACK
F25_STUD_L=65
F25_NUT_M_MIN=15.7
F25_NUT_M_MAX=16.4
F25_STUD_PROTRUSION_MIN=4
F25_STUD_PROTRUSION_MAX=6
F25_GRIP_PLUS_ANCHOR_MIN=42.6
F25_GRIP_PLUS_ANCHOR_MAX=45.3
GEAR_SIDE_GRIP_OR_ENGAGE_MIN≈18.6
GEAR_SIDE_GRIP_OR_ENGAGE_MAX≈21.3

# VENDOR FINAL
GEARBOX_SPIGOT_D_FINAL=?
GEARBOX_FLANGE_T_FINAL=?
GEARBOX_INPUT_BORE_FINAL=?
GEARBOX_KEYWAY_FINAL=?
```

---

# 14. V39当前结论

当前连接盘草模可以正式采用：

```text
F25
φ300接口外径
8×φ17.5
PCDφ254
22.5°起始
厚24mm CAD
```

其中：

```text
φ300 / φ254 / 8×M16 / φ17.5 / 22.5° = 标准接口
24mm = CAD候选，不是制造冻结
```

M16×65长度也与24mm连接盘形成合理轴向闭环。

---

# 15. 下一步 V40

下一步把连接盘放回Z轴：

```text
填料压紧面
↓
STEM_COVER上表面
↓
4×M12×50
↓
24mm ADAPTER
↓
F25上安装面
↓
φ60键轴 / 18×90键
↓
F25 gearbox placeholder
```

目标：关闭：

```text
Z_ADAPTER_BOTTOM_CAD
Z_ADAPTER_TOP_CAD
Z_F25_INTERFACE_CAD
KEY_START/END的新口径
阀杆总长CAD
```

这样上部驱动链就能第一次真正装进SolidWorks总装，而不是漂在空间里。

> **V39一句话结论：F25连接盘现在已经从“一个24mm厚的猜测圆盘”升级成有标准孔阵列、反力矩计算和M16×65轴向长度共同约束的可建模部件。**