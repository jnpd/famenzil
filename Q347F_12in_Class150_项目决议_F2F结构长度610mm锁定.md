# Q347F 12寸 Class150——项目决议：F2F结构长度 610 mm 锁定

> **状态：A / 项目设计输入。**  
> **适用对象：Q347F，NPS12 / DN300，Class150，固定球，全通径。**

---

# 1. 当前项目正式采用

```text
VALVE_F2F = 610 mm
```

因此以球心 `O=(0,0,0)`、X轴为流道方向：

```text
X_END_FACE_L = -305 mm
X_END_FACE_R = +305 mm
```

这两个端面作为当前数字化总装骨架的左右法兰端面基准。

---

# 2. 权限等级

本项目明确规定：

```text
F2F = 610 mm
状态 = A / 项目设计输入
```

其优先级高于后续检索到的其他结构长度表、其他阀型尺寸、API 6D其他适用图表或短型/长型备选数据。

也就是说：

```text
项目已锁定610
↓
后续所有阀体 / 阀盖 / 阀座 / 法兰 / XYZ骨架
均围绕610闭合
```

---

# 3. 其他结构长度如何处理

如果后续标准资料出现：

```text
356 mm
838 mm
或其他数值
```

统一处理为：

```text
STD-REF / 差异说明
```

不得自动覆盖当前610 mm项目输入。

除非项目方明确重新下达结构长度变更，否则：

```text
禁止修改 VALVE_F2F = 610
```

---

# 4. 对V18的覆盖规则

V18中原先把610写成：

```text
C/STD-candidate
```

从本项目决议起正式升级为：

```text
VALVE_F2F = 610 mm       A
X_END_FACE_L = -305 mm   B/A
X_END_FACE_R = +305 mm   B/A
```

V18中 `356短型配置` 只保留历史/参考，不再作为本项目SolidWorks可切换主配置。

---

# 5. 后续计算永久使用

后续统一使用：

```text
F2F = 610
HALF_F2F = 305
```

第5/6/7步涉及的：

- 左右阀体—阀盖接口；
- 阀座轴向包装；
- 中法兰；
- 端部法兰；
- 阀体承压壁；
- SolidWorks总装骨架；
- 工程图结构长度；

全部基于该值继续。

> **一句话结论：本项目结构长度就是610 mm，不再因为后续检索到其他标准表值自动改动。**
