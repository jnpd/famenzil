# Q347F 12寸 Class150——第4E步：20寸前盖/底盖原生 SolidWorks 结构与坐标核对 V13

> **定位**：承接第4C V11与第4D V12。本页直接读取用户提供的20寸成熟原生 SolidWorks 零件：`20Q347F-300LB-07 底盖.SLDPRT`、`20Q347F-300LB-08 前盖.SLDPRT`，目标不是按比例缩成12寸，而是提取**谁和谁一体、哪个面负责定位、轴承/密封/法兰如何分层、原生坐标如何组织**，再回写12寸数字化骨架。
>
> **永久规则**：20寸原生模型只提供结构拓扑、成熟制造习惯与交叉检查；12寸制造尺寸仍以12寸BOM、公司规范、计算结果和12寸公差链为准。

[← 第4C V11](./Q347F_12in_Class150_第4C步_前盖剖面_Z坐标骨架_轴向下限_V11.md)  
[← 第4D V12](./Q347F_12in_Class150_第4D步_底部主支承_Z坐标骨架_反力中心_V12.md)  
[→ 数字化总装骨架总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)

---

# 1. 本轮原生文件与解析边界

```text
20Q347F-300LB-07 底盖(2).SLDPRT
20Q347F-300LB-08 前盖(2).SLDPRT
```

两份文件均属于 SolidWorks 2015+ 的 modern chunk 格式。本轮以只读方式提取：

```text
配置名称/自定义属性
DisplayLists 三角化几何
预览图
特征元数据
```

重要限制：

- DisplayLists 可以可靠用于识别当前保存配置的外形、圆柱面、端面、孔位和相对轴向站位；
- 它不是正式制造尺寸API，最终制造尺寸仍要回到公司图纸/参数化特征/正式公差；
- 前盖当前保存的活动几何是 `焊前图`，所以其精加工孔槽不能把焊前尺寸当最终尺寸；
- 本页对20寸读取值统一记为 `REF20/H` 或结构证据 `C+`，禁止直接同比例缩放。

---

# 2. 原生配置与身份核对

## 2.1 底盖

原生文件存在：

```text
默认
焊前图
```

当前可读 DisplayLists 对应成熟底盖实体外形。

属性中读到：

```text
产品名称：底盖
图号：20"-Q347F-300LB-31
材料：A105+ENP
```

但另一个 `型式` 自定义字段仍写成：

```text
NPS12 CL300
```

这与：

```text
文件名20Q347F-300LB
图号20"
原路径DN500/20寸系列
```

冲突。

因此新增风险：

```text
R13-01 = 底盖自定义属性存在陈旧/错填字段
```

处理原则：

> 文件身份以文件名、图号、系列路径和实际装配上下文为主；`NPS12 CL300`字段不得作为尺寸来源。

## 2.2 前盖

原生属性一致：

```text
产品名称：前盖
图号：20"-Q347F-300LB-08
材料：A105+ENP
型式：NPS20 CL300
```

存在配置：

```text
默认
焊前图
```

当前保存的活动 DisplayLists 对应：

```text
焊前图
```

而 `默认` 配置预览能够看到精加工后的多级内孔/环槽。

因此：

```text
R13-02 = 前盖当前活动DisplayLists不能直接提供默认精加工配置的全部孔槽制造尺寸
```

---

# 3. 最大结构结论：底盖与下支承轴颈是一体结构

原生底盖不是：

```text
底盖
+
独立下支承轴
```

而是一个连续实体：

```text
法兰盘
↓
定位台阶/密封圆柱
↓
轴肩
↓
下支承轴颈
```

即：

> **下支承轴颈是底盖零件本体的一部分。**

这条结构拓扑证据升级为 `C+`。

因此12寸V12原来的文字链：

```text
球体φ70下孔
↓
φ70×φ65×50轴承
↓
F51 φ65下支承轴颈
↓
底盖
```

应进一步精确为：

```text
球体 φ70 下孔
↓
316+PTFE φ70×φ65×50轴承
↓
底盖本体一体加工的 F51 φ65 下支承轴颈
```

SolidWorks零件策略：

```text
05_LOWER_SUPPORT
```

不应拆成“底盖+单独轴”两个零件，而应优先建成：

```text
07_BOTTOM_COVER.SLDPRT
内部包含 LOWER_JOURNAL_D=65 特征
```

除非后续12寸正式零件图明确反证。

---

# 4. 20寸底盖原生轴向结构读取

20寸原模型自身轴向为源文件 `Y` 轴。下面数值只用于识别成熟结构关系。

## 4.1 主要站位

| REF20站位 | 原生Y约值 mm | 结构解释 | 状态 |
|---|---:|---|---|
| B20-0 | -278.0 | 轴颈最上端/靠球体侧端面 | REF20 |
| B20-1 | -281.464 | 端部倒角/过渡结束 | REF20 |
| B20-2 | -370.0 | 主轴颈下端肩面 | REF20 |
| B20-3 | -371.732 | 肩部过渡 | REF20 |
| B20-4 | 约-414 ~ -421 | O圈槽功能区 | REF20 |
| B20-5 | -437.5 | 定位肩/法兰上部基准 | REF20 |
| B20-6 | -433.0 | 法兰内侧端面邻近台阶 | REF20 |
| B20-7 | -465.0 | 法兰外侧端面 | REF20 |

> 这些是20寸原模型坐标，不是12寸Z坐标，不允许直接代入12寸。

## 4.2 主要直径

```text
最上端轴颈：约 φ96
主支承轴颈：φ100
下一导向/密封圆柱：φ105
局部定位肩：约φ115.4
法兰名义OD：约φ188
```

这些数值只作为20寸成熟结构参考。

---

# 5. 底盖O圈槽得到一条非常强的成熟结构证据

在20寸底盖 φ105 外圆附近识别出环槽：

```text
相邻基准外圆 OD ≈ 105
槽底 OD ≈ 96.2
```

因此径向槽深：

```text
h
= (105-96.2)/2
≈ 4.4 mm
```

轴向直线槽底约5.4mm，加两侧圆角/过渡后完整功能宽度约：

```text
b ≈ 7.0 mm
```

所以成熟20寸底盖原生结构给出：

```text
O圈槽深 ≈ 4.4 mm
O圈槽宽 ≈ 7.0 mm
```

这与公司当前 `5.3 mm` 截面O圈的动密封槽：

```text
深4.4
宽7.0
```

高度一致。

结论：

> **底部支承/底盖的转动或相对运动密封，优先采用5.3截面O圈动槽口径具有原生成熟结构支持。**

这条不等于12寸O圈名义直径已经冻结，但槽截面规则可提升为强交叉证据。

---

# 6. 20寸底盖法兰结构参考

原生模型读取到：

```text
法兰OD ≈ φ188
法兰轴向厚度 ≈ 32 mm
6个通孔
单孔≈φ22
孔中心半径≈73
PCD≈φ146
圆周60°均布
```

另外识别到中心小孔：

```text
约φ14盲孔/工艺孔
```

这些只用于理解成熟底盖：

```text
一体轴颈
↓
密封/定位台阶
↓
法兰端面
↓
圆周紧固
```

12寸底盖法兰OD、PCD、孔径和厚度必须重新按12寸结构、载荷和连接件设计，不按比例缩放。

---

# 7. 前盖原生结构：确认“中空套筒 + 法兰 + 多级内孔”拓扑

前盖不是实心轴类件，而是：

```text
靠球体侧中空套筒/伸入段
↓
多级内孔
↓
外部法兰
↓
连接盘/执行器侧
```

默认精加工配置预览可明确看到：

```text
中央贯穿/多级孔
多道环形台阶/沟槽
外圆导向段
法兰螺栓孔
侧向工艺/螺纹孔
```

这与V10/V11建立的：

```text
Rail-A：φ65阀杆附近轴承/止推/O圈/填料
Rail-B：φ95/φ100/φ105外导向和外密封
Rail-C：φ105/φ115连接盘/垫片
```

结构理解是一致的。

因此：

> **V11“不同半径功能轨道可以轴向嵌套，但同一径向轨道不能互相穿插”的方法，获得20寸原生前盖拓扑支持。**

---

# 8. 前盖当前焊前图可读取的参考包络

当前活动 DisplayLists 为 `焊前图`，读取到：

```text
下部套筒粗外径 ≈ φ156
套筒粗轴向长度 ≈ 126 mm
法兰粗外径 ≈ φ239
法兰粗厚度 ≈ 57 mm
下部粗内孔 ≈ φ89.4
中部粗内孔 ≈ φ109.8
上部短粗孔 ≈ φ111
```

这些只能记作：

```text
REF20-ROUGH
```

禁止用于12寸制造尺寸，也禁止拿它们验证V10的：

```text
φ65 / φ70
φ75
φ95 / φ100 / φ105
```

是否“相等”。因为焊前图本来就保留后加工余量。

---

# 9. 前盖精加工结构当前可以确认什么、不能确认什么

## 可以确认

```text
1. 前盖是中空结构；
2. 内部存在多级同轴孔/槽；
3. 轴承、O圈、填料功能应在不同Z站位组织；
4. 外导向与内阀杆功能可以在不同半径轨道嵌套；
5. 法兰端负责前盖与上部连接结构的轴向定位/紧固。
```

## 当前不能从活动DisplayLists硬填

```text
φ70精加工轴承孔的真实起止Z
两道φ65×5.3 O圈的原生20寸对应槽绝对站位
填料函绝对Z
φ100/φ105外导向轴承的最终加工站位
φ95外O圈到底开在前盖外圆还是对偶件内孔
```

因此V11的：

```text
Z_U0_ABS
```

仍不能仅凭这一个前盖零件完全关闭。

---

# 10. 原生坐标说明：20寸零件看起来保存了“装配上下文坐标”

本轮读取：

```text
前盖主要位于原生Y正方向：约 +299.5 ~ +482.5
底盖主要位于原生Y负方向：约 -278 ~ -465
```

而20寸成熟球体已知：

```text
R20 = 374 mm
```

从量级看，前盖和底盖分别位于球体上下两侧，原生零件很可能保留了总装上下文位置。

这对我们非常有价值，但当前只记：

```text
C = 具有装配坐标上下文的强迹象
```

不能直接宣布：

```text
源文件Y=0 = 球心O
```

因为还缺20寸球体或总装源文件作为共同原点证据。

---

# 11. 对V12下支承模型的正式修正

V12继续保留：

```text
Z_L0_ABS = 12寸球体φ70下轴承孔口
Z_RL     = Z_L0_ABS + 25
b        = -(Z_L0_ABS+25)
```

但是装配关系文字升级为：

```text
BALL lower bore φ70
↓
LOWER_BRG φ70×φ65×50
↓
BOTTOM_COVER integral journal φ65
```

因此SolidWorks Mate应表达为：

```text
球体φ70孔轴线
=
下轴承OD轴线

下轴承ID轴线
=
底盖一体φ65轴颈轴线

底盖本体
=
阀体底部安装面/定位圆
```

而不是建立：

```text
底盖 ↔ 独立下支承轴
```

这一条会直接减少一个错误零件层级和一组错误Mate。

---

# 12. 为什么这两个零件仍不能单独关闭绝对Z

V11/V12真正需要：

```text
Z_U0_ABS
Z_L0_ABS
```

它们分别定义在：

```text
上部：止推/上主支承与球体关系
下部：球体下轴承孔口
```

而前盖/底盖单零件只告诉我们：

```text
支承件自身的台阶位置
```

要把它们转换成：

```text
相对球心O的绝对Z
```

仍需要共同基准：

```text
20寸球体.SLDPRT
或20寸总装.SLDASM
```

因此下一关闭门是：

> **用20寸球体/总装验证“源Y=0是否就是球心O”，并读取球体上下孔口相对球心的真实站位。**

这一步关闭后，才可以把20寸成熟拓扑转成12寸的绝对Z设计约束。

---

# 13. 本轮给12寸数字化设计模型新增的规则

```text
RULE-13-01
下支承φ65轴颈属于BOTTOM_COVER零件本体，C+

RULE-13-02
球体下孔φ70 → φ70×φ65×50轴承 → 底盖一体φ65轴颈

RULE-13-03
底盖O圈槽截面7×4.4获得20寸原生结构交叉支持

RULE-13-04
前盖采用中空多级同轴孔 + 法兰拓扑

RULE-13-05
V11 Rail-A/Rail-B/Rail-C分轨建模方式继续有效

RULE-13-06
前盖焊前图尺寸只作粗包络，禁止作为12寸精加工尺寸

RULE-13-07
20寸源坐标疑似装配上下文坐标，但必须由球体/总装关闭原点
```

---

# 14. 下一步主线

```text
V13：前盖/底盖结构拓扑确认  ← 当前完成
↓
读取20寸球体或总装共同坐标
↓
关闭20寸球心O与上下支承站位
↓
把成熟拓扑映射为12寸约束，而不是尺寸缩放
↓
关闭 Z_U0_ABS / Z_L0_ABS
↓
回算 a / b / Ru / Rl
↓
第5步：内部整体空间
↓
阀体 / 阀盖
```

如果暂时没有20寸球体/总装，也不停止：可以继续推进12寸内部径向包络、底盖一体轴颈草模和SolidWorks骨架参数；只把绝对Z保持为D。

---

# 15. V13变更记录

## 2026-08-21

- 直接读取20寸底盖/前盖原生SolidWorks文件；
- 确认底盖与下支承轴颈为一体结构；
- 读取20寸底盖主轴颈、密封台阶、法兰与紧固拓扑；
- 由原生底盖几何得到O圈槽约 `宽7 / 深4.4` 的强交叉；
- 确认前盖为“中空套筒 + 多级内孔 + 法兰”结构；
- 发现前盖当前活动配置为焊前图，禁止把粗加工孔径当制造尺寸；
- 记录底盖自定义属性 `NPS12 CL300` 与20寸文件身份冲突；
- 修正12寸SolidWorks零件树：φ65下支承轴颈优先属于底盖零件本体；
- 明确关闭绝对Z仍需20寸球体或总装共同坐标证据。
