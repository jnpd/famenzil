# Q347F 12寸 Class150——第4F步：20寸总装共同坐标 / 球心与上下支承关闭 V14

> **定位**：承接第4E V13。本页不做20寸→12寸比例缩放，而是把20寸成熟总装中已经读出的共同坐标证据正式沉淀，建立“球心 O → 上止推/上轴承 → 下轴承 → 前盖/底盖”的坐标关闭方法。
>
> **当前结论等级**：20寸总装坐标只记作 `REF20 / C / C+` 结构证据；12寸 `Z_U0_ABS`、`Z_L0_ABS` 在没有直接量到12寸球体相应功能面前，仍保持 `D/C`，禁止为了凑对称而硬填。

[← 第4E V13：20寸前盖/底盖原生结构坐标核对](./Q347F_12in_Class150_第4E步_20寸前盖底盖原生SolidWorks结构坐标核对_V13.md)  
[→ 数字化总装骨架总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)  
[→ 总导航](./00_Q347F_12in_Class150_文档导航_从这里开始.md)

---

# 1. 为什么必须做这一页

V11/V12已经把上、下支承的**局部尺寸链**算清楚：

```text
上部：
Z_RU = Z_U0_ABS + 27

a = Z_U0_ABS + 27

下部：
Z_RL = Z_L0_ABS + 25

b = -(Z_L0_ABS + 25)
```

现在真正缺的不是“50 mm轴承多长”，而是：

```text
Z_U0_ABS = 上止推垫靠球体侧端面，相对球心O的真实绝对Z
Z_L0_ABS = 球体φ70下轴承孔口，相对球心O的真实绝对Z
```

只要这两个坐标关闭，就能立即得到：

```text
a / b
↓
Ru / Rl
↓
前盖、底盖相对球心的真实站位
↓
内部整体空间
↓
阀体 / 阀盖
```

---

# 2. 上一轮已经从20寸总装读到的共同坐标证据

本轮承接的原生文件：

```text
20Q347F-300LB 总装图(3).SLDASM
20Q347F-300LB-03 球体(2).SLDPRT
```

已确认：

```text
20寸总装中的球体组件变换矩阵 = 单位矩阵
```

这是一条非常重要的证据，说明：

> **球体组件坐标系与20寸总装坐标系高度一致，20寸总装很可能就是围绕球体基准组织装配。**

但要注意：

```text
“球体组件变换矩阵为单位矩阵”
≠
“球体零件Origin已经证明等于理论球心O”
```

后者仍需要用球体球面/流道轴/上下结构共同确认。

---

# 3. 当前已读到的20寸参考位置

上一轮从20寸成熟总装中读到：

| REF20 ID | 对象 | 总装参考坐标 | 当前解释 | 状态 |
|---|---|---:|---|---|
| `REF20-U-THRUST` | 上止推垫 | 约 `+324 mm` | 上部功能件参考位置 | REF20/C |
| `REF20-U-BRG` | 上主滑动轴承 | 约 `+326 mm` | 上主支承参考位置 | REF20/C |
| `REF20-L-BRG` | 下主滑动轴承 | 约 `-370 mm` | 下主支承参考位置 | REF20/C |

这些坐标目前只允许用于：

```text
① 判断上下件确实位于球体两侧；
② 判断20寸零件保存了装配上下文；
③ 建立“哪个件先定位、哪个件再相对定位”的成熟装配链；
④ 检查上下结构明显不是简单镜像尺寸复制。
```

禁止用于：

```text
12寸坐标 = 20寸坐标 × 12/20
```

也禁止直接写成：

```text
12寸 Z_U0_ABS = +324×比例
12寸 Z_L0_ABS = -370×比例
```

---

# 4. 为什么20寸三个参考坐标还不能直接关闭12寸Z

原因有三个。

## 4.1 “参考位置”必须继续落到明确功能面

必须确认每个值到底代表：

```text
零件Origin？
组件插入点？
轴承端面？
轴承中心？
止推垫端面？
几何包围盒代表点？
```

只有落到明确的工程功能面，坐标才可以进入尺寸链。

## 4.2 20寸球体Origin仍要和理论球心O做一次闭环

最低验证条件：

```text
FLOW_AXIS 穿过 Origin
STEM_TRUNNION_AXIS 穿过 Origin
理论球面中心 = Origin
```

如果三条同时成立，可以把：

```text
20寸总装原点 = 20寸球心 O20
```

升级为 `C+`。

## 4.3 12寸必须按自己的球体功能面重建

12寸当前基线：

```text
BALL_OD = φ465
BALL_R  = R232.5
BORE_D  = φ303
上驱动接口 = 50×70×35
下轴承孔 = φ70×52深
上下主轴承 = φ70×φ65×50
```

所以12寸真正要量/定的是自己的：

```text
球心O → 上部止推起始面
球心O → 下部φ70轴承孔口
```

20寸只负责告诉我们成熟结构应该怎么组织，不负责替12寸填数。

---

# 5. V14正式定义的“坐标关闭门”

## GATE-14A：关闭20寸球心原点

必须同时满足：

```text
A1. 球体球面理论中心落在总装Origin
A2. 流道轴穿过总装Origin
A3. 上下支承/阀杆轴穿过总装Origin
A4. 球体组件变换矩阵仍为单位矩阵
```

通过后记录：

```text
O20 = (0,0,0)
STATUS = C+
```

## GATE-14B：关闭20寸上主支承功能面

至少读取：

```text
上止推垫靠球体侧端面 Z20_U0
上轴承起点 Z20_UB0
上轴承终点 Z20_UB1
上轴承中心 Z20_RU = (Z20_UB0+Z20_UB1)/2
```

并检查：

```text
Z20_UB1 - Z20_UB0 = 轴承实际长度
```

## GATE-14C：关闭20寸下主支承功能面

至少读取：

```text
球体下轴承孔口 Z20_L0
下轴承外端 Z20_LB0
下轴承内端 Z20_LB1
下轴承中心 Z20_RL = (Z20_LB0+Z20_LB1)/2
```

并检查：

```text
Z20_LB1 - Z20_LB0 = 轴承实际长度
```

## GATE-14D：转成12寸约束，而不是转成比例尺寸

只继承：

```text
谁先定位
↓
谁和谁接触
↓
轴承从哪一侧装入
↓
哪个端面决定反力中心
↓
前盖/底盖怎样承接支承件
```

12寸数值由自己的零件尺寸链重新形成。

---

# 6. 12寸最终需要关闭的两条绝对尺寸

## 6.1 上部

定义不变：

```text
Z_U0_ABS = 上止推垫靠球体侧端面的绝对Z
```

局部链：

```text
止推垫 2
↓
上主轴承 50
↓
双O圈区 18
↓
填料压后 4.4
```

所以：

```text
Z_RU = Z_U0_ABS + 27
```

## 6.2 下部

定义不变：

```text
Z_L0_ABS = 球体φ70下轴承孔口绝对Z
```

下轴承：

```text
φ70×φ65×50
```

所以：

```text
Z_RL = Z_L0_ABS + 25
```

12寸球体下孔总深候选为：

```text
52 mm
```

因此孔底：

```text
Z_L0_ABS + 52
```

轴承内端：

```text
Z_L0_ABS + 50
```

保留：

```text
2 mm
```

孔底余量候选。

---

# 7. 一旦Z关闭，支承反力立即重算

当前总横向设计载荷：

```text
F_support = 164.69 kN
```

定义：

```text
a = Z_U0_ABS + 27
b = -(Z_L0_ABS + 25)
```

真实反力：

```text
Ru = 164.69 × b/(a+b)  kN
Rl = 164.69 × a/(a+b)  kN
```

检查：

```text
Ru + Rl = 164.69 kN
```

另外保留对称误差检查：

```text
SUPPORT_SYM_ERROR
= (Z_U0_ABS+27) - (-(Z_L0_ABS+25))
```

但：

> **`SUPPORT_SYM_ERROR=0` 只是理想检查项，不是设计强制条件。真实装配尺寸优先。**

不得为了得到 `Ru=Rl` 而人为修改真实Z坐标。

---

# 8. SolidWorks里真正要建立的基准

建议总装骨架至少建立：

```text
BALL_CENTER_O
FLOW_AXIS
STEM_TRUNNION_AXIS

PLN_U0      = 上止推垫球体侧基准面
PLN_RU      = 上轴承反力中心面
PLN_L0      = 下轴承孔口基准面
PLN_RL      = 下轴承反力中心面

PLN_TOP_COVER_MATE
PLN_BOTTOM_COVER_MATE
```

方程式：

```text
PLN_U0.Z = Z_U0_ABS
PLN_RU.Z = Z_U0_ABS + 27

PLN_L0.Z = Z_L0_ABS
PLN_RL.Z = Z_L0_ABS + 25
```

这样后续前盖、底盖、阀体都围绕骨架装，而不是零件互相“拖着装”。

---

# 9. 本轮新增的装配关系规则

```text
RULE-14-01
20寸球体组件在总装中为单位变换矩阵，作为共同坐标强证据，但球体Origin=理论球心仍需几何闭环。

RULE-14-02
20寸上止推≈+324、上主轴承≈+326、下主轴承≈-370，只记REF20参考坐标，不做比例缩放。

RULE-14-03
12寸Z_U0_ABS必须由12寸球体上部功能面/前盖尺寸链关闭。

RULE-14-04
12寸Z_L0_ABS必须由12寸球体φ70下孔口直接关闭。

RULE-14-05
下部链固定为：球体φ70孔 → φ70×φ65×50轴承 → 底盖一体φ65轴颈。

RULE-14-06
上下反力由真实a/b计算，不预设Ru=Rl。

RULE-14-07
绝对Z未关闭时允许继续做径向包络和草模，但禁止冻结前盖/底盖轴向制造尺寸。
```

---

# 10. 当前状态表

| 项目 | 当前状态 | 能否进入SolidWorks草模 | 能否制造冻结 |
|---|---|---|---|
| 球心O定义 | 已定义 | 是 | 是，待与源模型闭环 |
| X流道轴 | 已定义 | 是 | 是 |
| Z阀杆/支承轴 | 已定义 | 是 | 是 |
| φ65主轴颈 | C+ | 是 | 暂否 |
| φ70×φ65×50上下轴承 | A | 是 | 可作为采购/设计基线 |
| 下部“底盖一体轴颈”拓扑 | C+ | 是 | 待12寸正式图确认 |
| `Z_U0_ABS` | D/C | 用变量占位 | 否 |
| `Z_L0_ABS` | D/C | 用变量占位 | 否 |
| `a/b` | 依赖Z | 公式占位 | 否 |
| `Ru/Rl` | 依赖a/b | 公式占位 | 否 |
| 前盖轴向总高 | 未冻结 | 可做包络 | 否 |
| 底盖轴向总高 | 未冻结 | 可做包络 | 否 |

---

# 11. 下一步最小动作

不是继续堆更多零件尺寸，而是完成下面4个测量/确认：

```text
① 20寸球体理论球心是否 = 总装Origin
② 20寸上轴承参考坐标到底对应端面还是中心
③ 20寸下轴承参考坐标到底对应端面还是中心
④ 12寸球体上止推功能面、下φ70孔口相对球心O的直接站位
```

只要第④项得到，12寸就可以直接关闭：

```text
Z_U0_ABS
Z_L0_ABS
```

并立刻重算：

```text
a
b
Ru
Rl
```

然后进入：

```text
第5步：内部整体空间
↓
阀体 / 阀盖
↓
法兰 / 紧固
↓
SolidWorks总装骨架冻结
```

---

# 12. V14变更记录

## 2026-08-21

- 承接上一轮20寸总装/球体读取结果；
- 正式记录球体组件单位变换矩阵这一共同坐标证据；
- 记录20寸上止推约+324、上主轴承约+326、下主轴承约-370的参考位置；
- 明确三组20寸坐标仅用于拓扑/装配链验证，禁止比例缩放为12寸；
- 建立GATE-14A~14D坐标关闭门；
- 继续保持12寸 `Z_U0_ABS / Z_L0_ABS` 为未冻结变量；
- 明确一旦绝对Z关闭，即重算 `a/b/Ru/Rl` 并进入第5步内部整体空间。
