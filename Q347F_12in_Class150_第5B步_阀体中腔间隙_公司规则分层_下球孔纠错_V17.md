# Q347F 12寸 Class150——第5B步：阀体中腔间隙 / 公司规则分层 / 下球孔纠错 V17

> **定位**：承接 V16《内部整体空间统一包络》。本页解决两个问题：  
> 1. 固定球阀的球体—阀体中腔间隙，在没有本型号直接公司数值时怎样正确进入 SolidWorks；  
> 2. 纠正 V15/V16 中继续沿用的“下球孔 φ70×52 固定深度”旧理解。  
> **原则**：固定球阀公司规范 `GFE-JS01-2025` 为主；浮动球阀 `GFE-JS02-2025` 只能作为跨结构敏感性参考，不得冒充固定球阀强制尺寸。

[← 总导航](./00_Q347F_12in_Class150_文档导航_从这里开始.md)  
[← V16：内部整体空间统一包络](./Q347F_12in_Class150_第5A步_内部整体空间_球体阀座上下支承统一包络_V16.md)  
[← 当前有效 V11：20寸原生支承/阀杆轴向证据](./Q347F_12in_Class150_第4C步_20寸原生支承_阀杆轴向证据_12寸V11坐标骨架.md)  
[→ 数字化总装骨架总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)

---

# 1. 先固定“规则权限等级”

本项目以后涉及尺寸来源，按以下优先级：

```text
A0  客户/项目数据表
↓
A1  12寸目标BOM / 已确认采购件
↓
A2  公司固定球阀规范 GFE-JS01-2025
↓
B/C 公式计算 / 当前12寸几何闭合
↓
C+  20寸成熟固定球结构交叉证据
↓
P-XREF 同公司其他阀型参考（例如浮动球阀 GFE-JS02）
↓
H   历史值
```

因此：

> **同公司浮动球阀的“中腔间隙1.5～6mm”不能直接升级成12寸固定球阀的A/C制造值。**

它目前只能用于：

```text
SolidWorks敏感性配置
空间裕量检查
第6步阀体设计前的方案筛选
```

---

# 2. 本项目结构身份再次确认：NPS12 Class150 = 枢轴固定

公司 `GFE-JS01-2025` 当前规则：

```text
CL150～CL900：
NPS ≤ 12" → 枢轴固定结构
NPS ≥ 14" → 支撑板固定结构
```

本项目：

```text
NPS12
Class150
```

所以：

```text
TRUNNION_SUPPORT_TYPE = 枢轴固定    A
```

这意味着公司规范中针对“支撑板”的：

```text
支撑板外圆—阀体中腔间隙 0.8～2mm
球体—支撑板局部间隙 0.2mm
```

**不是本12寸枢轴固定结构的球体—阀体主球面间隙规则。**

不能偷换成：

```text
BALL_BODY_CLR_RAD = 0.8～2
```

---

# 3. 固定球阀规范当前能直接给我们的阀体规则

公司固定球阀规范明确：

```text
阀体最小壁厚：查 ASME B16.34
并适当增加 3～5mm
若标准查不到，则按 ASME VIII-1 UG-27 设计计算
```

所以第6步承压边界必须写成：

```text
T_B1634 = ASME B16.34 对当前NPS/Class/材料/温度要求的最小壁厚

BODY_WALL_TARGET
≥ T_B1634 + 3～5 mm
```

当前仓库还没有可直接追溯到表号/页码的 `T_B1634` 数值，因此：

```text
T_B1634 = ?                D
BODY_WALL_TARGET = ?       D
```

但设计规则本身已经关闭：

```text
BODY_WALL_POLICY = B16.34 + 3～5mm    A-policy
```

这不会阻塞参数化建模。

---

# 4. 球体—阀体中腔间隙为什么现在不硬猜一个值

V16定义：

```text
BALL_OD = 465
BALL_R  = 232.5

BODY_CAVITY_D_MIN
= 465 + 2×BALL_BODY_CLR_RAD
```

固定球阀公司规范当前没有直接给出：

```text
BALL_BODY_CLR_RAD = 某个固定数值
```

所以制造冻结状态继续保持：

```text
BALL_BODY_CLR_RAD = ?    D
```

这是正确的工程状态，不是计算遗漏。

因为真实间隙至少还受：

```text
加工误差
装配偏差
球体/支承实际偏心
温度变化
铸造与机加工方式
拆装路径
```

控制。

---

# 5. 用同公司浮动球阀规范建立“三档敏感性配置”

同公司 `GFE-JS02-2025` 对浮动球阀给出：

```text
阀体中腔毛坯直径
= 球体直径 + 2×间隙量

间隙量一般 1.5～6mm
装配需要时可以适当加大
```

这不是本项目固定球阀制造规则，但非常适合用作：

```text
P-XREF / CAD sensitivity
```

因此 SolidWorks 不选一个“伪最终值”，而建立三个配置。

## 5.1 CLR-LOW

```text
BALL_BODY_CLR_RAD = 1.5 mm    P-XREF
BODY_CAVITY_D     = 465+2×1.5
                  = 468 mm
```

## 5.2 CLR-MID

```text
BALL_BODY_CLR_RAD = 3.0 mm    P-XREF
BODY_CAVITY_D     = 465+2×3
                  = 471 mm
```

## 5.3 CLR-HIGH

```text
BALL_BODY_CLR_RAD = 6.0 mm    P-XREF
BODY_CAVITY_D     = 465+2×6
                  = 477 mm
```

三档表：

| 配置 | 径向间隙 | 中腔功能直径 | 状态 | 用途 |
|---|---:|---:|---|---|
| `CLR_1P5` | 1.5 | φ468 | P-XREF | 紧凑极限筛查 |
| `CLR_3P0` | 3.0 | φ471 | P-XREF | 中间场景草模 |
| `CLR_6P0` | 6.0 | φ477 | P-XREF | 宽松装配筛查 |

**三档都不是最终制造冻结值。**

---

# 6. 推荐 SolidWorks 做法：用配置，不要把3mm写成“最终”

骨架变量：

```text
BALL_OD = 465

# 由配置表驱动
BALL_BODY_CLR_RAD = 1.5 / 3.0 / 6.0

BODY_CAVITY_D_FUNC
= BALL_OD + 2*BALL_BODY_CLR_RAD
```

配置名建议：

```text
00_SKELETON_CLR_1P5
00_SKELETON_CLR_3P0
00_SKELETON_CLR_6P0
```

第一轮建模和干涉检查可以把：

```text
CLR_3P0
```

设为默认显示配置，但其状态仍是：

```text
P-XREF
```

不是 `C`、更不是 `A`。

---

# 7. V17关键纠错：下球孔“52mm”不能再当固定有效圆柱深度

当前有效V11已经通过20寸成熟下支承结构重新确认：

```text
20寸下球体孔有效圆柱段 = 90
20寸下球体轴承长度     = 90
```

12寸采购轴承：

```text
LOWER_BRG = φ70×φ65×50
```

所以12寸更可靠的当前理解：

```text
BALL_LOWER_BORE_D              = φ70     C+
BALL_LOWER_BORE_EFFECTIVE_L    = 50 mm   C+
```

而：

```text
总加工深度（含导角/底部过渡） ≥ 50 mm
```

具体值目前：

```text
BALL_LOWER_BORE_TOTAL_DEPTH = ?    D
```

旧值：

```text
φ70×52深
```

从V17开始正式降为：

```text
52 mm = H/C-history
```

它最多只能作为历史CAD总深候选，不能再解释成“有效圆柱轴承段=52”。

---

# 8. 因此 V15/V16 的 `Z=-175` 孔底也要降级

当前下球体主轴承中心骨架：

```text
Z_LOWER_BRG_CENTER ≈ -202.0 mm    C
```

轴承长度：

```text
L = 50
```

所以轴承名义范围继续有效：

```text
Z_LOWER_BRG_OUT = -227.0 mm    C
Z_LOWER_BRG_IN  = -177.0 mm    C
```

如果定义：

```text
Z_LOWER_BORE_MOUTH_CAD = -227.0 mm
```

那么**有效圆柱支承段**应到：

```text
-227 + 50 = -177 mm
```

这与轴承内端一致。

但总加工孔底由于还有：

```text
导角
底部过渡
刀具退刀/圆角
```

所以：

```text
Z_LOWER_BORE_BOTTOM = ?     D
```

旧V15/V16用52mm推出来的：

```text
Z_LOWER_BORE_BOTTOM = -175 mm
```

正式改为：

```text
-175 mm = H/C-history
```

禁止继续作为当前SolidWorks硬尺寸。

---

# 9. 上球孔也采用同样的“有效圆柱段 / 总加工深度”分层

20寸成熟结构：

```text
上轴承L = 55
球体φ150有效圆柱支承孔 ≈53
```

比例：

```text
53/55 ≈0.9636
```

12寸上轴承：

```text
φ105×φ100×30
```

第一轮有效圆柱接触长度：

```text
BALL_UPPER_BORE_EFFECTIVE_L
≈30×53/55
≈28.9 mm     C
```

因此：

```text
BALL_UPPER_BORE_D           = 105      C+
BALL_UPPER_BORE_EFFECTIVE_L ≈ 28.9     C
BALL_UPPER_BORE_TOTAL_DEPTH = ?        D
```

以后上下孔统一使用：

```text
有效支承圆柱长度
≠
总加工孔深
```

这能避免孔底导角/过渡把轴承设计逻辑搞乱。

---

# 10. 更新后的下部功能空间

V16下部包络保留：

```text
下球体轴承：φ70×φ65×50
中心：Z≈-202.0
轴承外端：Z≈-227.0
轴承内端：Z≈-177.0
```

但是改成：

```text
下球体φ70有效圆柱支承段：50 mm   C+
下球孔总加工深度：>=50，具体D
总孔底绝对Z：D
```

所以第5步真正需要阀体避让的是：

```text
轴承/轴颈的功能圆柱包络
```

不是一个虚假的固定 `-175` 孔底面。

---

# 11. V17后当前可以直接加入SolidWorks的变量

```text
# ========================
# BODY CAVITY SENSITIVITY
# ========================
BALL_OD = 465

# config 1
CLR_1P5 = 1.5
BODY_CAVITY_D_1P5 = 468

# config 2
CLR_3P0 = 3.0
BODY_CAVITY_D_3P0 = 471

# config 3
CLR_6P0 = 6.0
BODY_CAVITY_D_6P0 = 477

# 最终制造值仍未冻结
BALL_BODY_CLR_RAD_FINAL = ?
BODY_CAVITY_D_FINAL     = ?

# ========================
# BODY WALL POLICY
# ========================
T_B1634 = ?
BODY_WALL_ADD = 3~5
BODY_WALL_TARGET >= T_B1634 + BODY_WALL_ADD

# ========================
# BALL SUPPORT BORES
# ========================
BALL_UPPER_BORE_D = 105
BALL_UPPER_BORE_EFFECTIVE_L = 28.9
BALL_UPPER_BORE_TOTAL_DEPTH = ?

BALL_LOWER_BORE_D = 70
BALL_LOWER_BORE_EFFECTIVE_L = 50
BALL_LOWER_BORE_TOTAL_DEPTH = ?   # >=50

LOWER_BRG_CENTER_Z = -202.0
LOWER_BRG_Z_OUT    = -227.0
LOWER_BRG_Z_IN     = -177.0
LOWER_BORE_MOUTH_Z = -227.0       # CAD骨架候选
LOWER_BORE_BOTTOM_Z = ?
```

---

# 12. 历史值状态更新

| 旧项目 | 旧值 | V17状态 | 当前替代 |
|---|---:|---|---|
| 下球孔固定深度 | 52 | H/C | 有效圆柱50；总深D |
| 下球孔固定孔底Z | -175 | H/C | `Z_BOTTOM=?` D |
| 球体—阀体固定间隙 | 未知 | 继续D | 三档P-XREF敏感性 |
| 中腔功能直径 | 未知 | 继续D-final | φ468/471/477三档分析 |
| 阀体壁厚 | 未知 | 继续D | `B16.34 + 3~5` A-policy |

---

# 13. 现在第5步已经关闭到什么程度

已经可以确定：

```text
中央运动主包络 = R232.5球面
上/下主轴承不增加中央最大径向包络
阀杆功能链向上突破球体包络
左右阀座需要独立X向延伸空间
上下球孔有效支承段已与采购轴承匹配
```

已经可以参数化但还不能制造冻结：

```text
球体—阀体中腔间隙
阀体中腔直径
阀体承压壁厚
上下球孔总加工深度
左右阀座真实外端X
```

---

# 14. 下一关闭门

现在不需要用户补资料就可以继续做：

```text
第5C / 第6步预处理
↓
把公司“B16.34 +3~5mm”变成阀体承压边界参数
↓
继续从现有20寸成熟总装提取：
  左右阀座/阀盖真实X向层级
  上前盖与阀体接口层级
  下底盖与阀体接口层级
↓
只继承拓扑/比例关系
↓
建立12寸阀体第一版参数化外壳
```

真正需要用户新增资料的门，目前只有：

```text
1. 要冻结ASME B16.34最小壁厚具体数值时：需要可追溯的B16.34对应表/页；
2. 要冻结316+PTFE轴承许用面压时：需要供应商材料数据；
3. 要出加工图时：需要12寸正式前盖/底盖/阀体接口制造依据或公司系列图。
```

在此之前继续推进不需要用户操作。

---

# 15. V17变更记录

## 2026-08-22

- 明确 NPS12 Class150 按公司规则属于枢轴固定结构；
- 明确支撑板 `0.8~2 / 0.2mm` 规则不适用于本项目球体主球面—阀体间隙；
- 将浮动球阀公司规范的 `1.5~6mm` 仅作为跨结构 P-XREF；
- 建立 φ468 / φ471 / φ477 三档中腔敏感性配置；
- 固定阀体壁厚政策为 `ASME B16.34最小值 + 3~5mm`，具体标准值仍D；
- 正式纠正下球孔：有效圆柱段50mm，旧52mm降为H/C；
- 旧 `Z_LOWER_BORE_BOTTOM=-175` 降为H/C，当前孔底保持参数化D；
- 增加上球孔有效圆柱段约28.9mm，总加工深度保持D。
