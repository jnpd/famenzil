# Q347F 12寸 Class150——第4C步：前盖剖面、Z坐标骨架与轴向下限 V11

> **承接**：[第4B V10：阀杆完整分段 / 前盖内外接口 / 防吹出链](./Q347F_12in_Class150_第4B步_阀杆完整分段_前盖内外接口_防吹出链_V10.md)  
> **同步总账**：[数字化总装骨架：尺寸参数 / 装配关系 / 空间坐标总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)  
> **本页目标**：不再把前盖看成一堆零散BOM，而是第一次建立“从球体侧到蜗轮侧”的Z向功能轨道。先算出**不能互相重叠的轴向下限**和局部Z站位，再等待真实前盖台阶把它们整体平移到球心O的绝对Z坐标。

---

# 1. 为什么V11先算“下限”，不直接拍一个前盖总高

V10已经明确：

```text
内侧主轴承     φ70×φ65×50
止推垫         φ75×φ65×2
阀杆O圈        φ65×5.3 ×2
双O圈局部包络  7+4+7 = 18
填料           φ75×φ65×5
填料安装厚     ≈4.4
外导向轴承     φ105×φ100×30
外侧O圈        φ95×5.3
缠绕垫         φ115×φ105×3.2
```

V10同时警告：不能把所有数字简单相加，因为不同半径的功能可以轴向重叠。

V11进一步把这个原则细化：

> **同一根φ65阀杆附近、占据相同径向空间的功能件不能重叠；不同半径轨道可以重叠。**

因此先划分“径向功能轨道”。

---

# 2. 前盖剖面分成三条径向功能轨道

## Rail-A：阀杆内侧功能轨道

围绕：

```text
φ65主轴颈
```

包含：

```text
φ75×φ65×2止推垫
φ70×φ65×50内侧轴承
两道φ65×5.3 O圈
φ75×φ65填料
```

这些零件都直接占据 `r≈32.5~37.5 mm` 范围，原则上不能在同一个Z截面互相穿插。

## Rail-B：前盖外导向/外静密封轨道

围绕：

```text
φ95外密封边界
φ100前盖外导向轴颈
φ105×φ100×30外导向轴承
```

它位于更外层半径，可以与Rail-A的一部分轴向重叠。

## Rail-C：连接盘/垫片接口轨道

```text
φ115×φ105×3.2缠绕垫
连接盘
键轴/蜗轮接口
```

这是最外层/最上部接口，不应拿它去增加中腔的每一段长度。

---

# 3. Rail-A为什么不能互相重叠——用直径直接证明

## 3.1 内轴承 vs 阀杆O圈

内轴承：

```text
ID = 65
OD = 70
```

它在同一Z截面占据：

```text
r = 32.5 ~ 35.0 mm
```

阀杆O圈工作在φ65轴上，前盖内孔动密封槽根径：

```text
φ73.8
```

即O圈/沟槽功能区扩展到：

```text
r = 36.9 mm
```

如果O圈槽和φ70轴承座在相同Z位置，沟槽根径73.8已经大于轴承座孔70，几何会直接互相切穿。

所以：

```text
Z(BRG_IN) ∩ Z(STEM_ORING) = ∅
```

这是几何约束，不是经验猜测。

## 3.2 内轴承 vs 填料

填料：

```text
ID = 65
OD = 75
```

占据：

```text
r = 32.5 ~ 37.5 mm
```

它与轴承：

```text
r = 32.5 ~ 35.0
```

明显重叠，因此：

```text
Z(BRG_IN) ∩ Z(PACKING) = ∅
```

## 3.3 O圈区 vs 填料区

O圈沟槽根半径：

```text
73.8/2 = 36.9 mm
```

填料外半径：

```text
75/2 = 37.5 mm
```

二者径向只差：

```text
37.5 - 36.9 = 0.6 mm
```

它们不可能在同一Z截面各自保持独立完整功能，所以：

```text
Z(STEM_ORING) ∩ Z(PACKING) = ∅
```

结论：

> **Rail-A 的止推垫、内轴承、双O圈区、填料必须形成前后串联的Z向功能链。**

---

# 4. V11采用的第一版功能顺序

V10已经给出下一步从球体侧到蜗轮侧的放入顺序：

```text
球体侧
↓
φ75×φ65×2 止推垫
↓
φ70×φ65×50 内侧主轴承
↓
两道 φ65×5.3 O圈
↓
φ75×φ65×5 填料
↓
更外侧前盖/连接盘/蜗轮侧
```

V11把它升级成**第一版轴向拓扑候选** `C`。

注意：

- 这个顺序可以用于CAD骨架；
- 不是最终制造图；
- 若20寸原生前盖剖面或公司正式图反证，允许整体重排，但必须记录修正。

---

# 5. 定义局部Z0——先解决相对站位

暂定义：

```text
Z0_local = 止推垫靠球体侧端面
```

这个 `Z0_local` 现在**还不是球心O的绝对Z坐标**。

它只是前盖Rail-A的局部起点。

从这里开始向 `+Z`（蜗轮方向）累计。

---

# 6. Rail-A第一轮轴向站位计算

## 6.1 止推垫

```text
THRUST_T = 2 mm
```

所以：

```text
Z0 = 0
Z1 = 2
```

止推垫区：

```text
[0, 2]
```

## 6.2 内侧主轴承

```text
BRG_IN_L = 50 mm
```

如果轴承紧接止推垫：

```text
Z2 = Z1 + 50
   = 52 mm
```

轴承区：

```text
[2, 52]
```

轴承反力中心位于长度中点：

```text
Z_RU_local
= 2 + 50/2
= 27 mm
```

所以V11得到一个非常重要的新关系：

```text
上主支承反力中心
= Z0_absolute + 27 mm
```

只要以后确定局部Z0相对球心O的绝对坐标，上支承力臂 `a` 就能直接得到。

## 6.3 双O圈区

V10：

```text
槽1宽 = 7
金属带 = 4
槽2宽 = 7
```

局部包络：

```text
18 mm
```

因此：

```text
Z3 = 52 + 18
   = 70 mm
```

双O圈区：

```text
[52, 70]
```

细分：

```text
槽1：52 ~ 59
金属带：59 ~ 63
槽2：63 ~ 70
```

状态：`C`。

## 6.4 填料区

V10安装目标：

```text
PACK_T_INSTALL ≈ 4.4 mm
```

所以：

```text
Z4
= 70 + 4.4
= 74.4 mm
```

填料区：

```text
[70, 74.4]
```

---

# 7. V11得到第一个真正有用的“前盖内侧轴向下限”

Rail-A当前不能重叠的功能总长度：

```text
L_A,min
= 止推垫2
+ 内轴承50
+ 双O圈区18
+ 填料安装厚4.4

= 74.4 mm
```

因此：

```text
L_A,min = 74.4 mm   B/C
```

这不是“前盖总高74.4”。

它的正确含义是：

> **从止推垫球体侧端面开始，到填料压后外端面结束，当前Rail-A至少需要74.4mm连续轴向功能空间。**

而实际结构还需要：

```text
防吹出肩本体厚度
轴承端部倒角/装配余量
必要金属隔离带
填料压紧件/压盖
前盖顶部连接结构
```

所以最终前盖高度必然：

```text
> 74.4 mm
```

但现在不拍具体总高。

---

# 8. Rail-B：外导向轴承与φ95 O圈也不能简单重叠

## 8.1 外导向轴承

```text
OD = 105
ID = 100
L  = 30
```

## 8.2 φ95外O圈候选内孔槽

V10候选：

```text
φ95密封圆柱
5.3截面静槽深4.2
槽根径 = φ103.4
槽宽 = 7
```

如果槽开在外部配合件内孔，并且与φ105外轴承座在相同Z截面：

```text
轴承座半径 = 105/2 = 52.5
O圈槽根半径 = 103.4/2 = 51.7
```

两者之间只剩：

```text
52.5 - 51.7 = 0.8 mm
```

即：

```text
仅0.8mm径向金属
```

这不是一个可以轻率冻结的制造结构。

所以在V10“内孔开槽候选”成立时，V11增加约束：

```text
Z(BRG_OUT) ∩ Z(COVER_ORING_GROOVE) = ∅
```

即外轴承区和外O圈槽区应轴向错开。

状态：`C/R`，第4C真实剖面必须关闭。

---

# 9. Rail-B局部轴向下限

外轴承：

```text
30 mm
```

外O圈槽：

```text
7 mm
```

若按V10当前“内孔开槽”候选且二者不能重叠：

```text
L_B,min
= 30 + 7
= 37 mm
```

所以：

```text
L_B,min = 37 mm   C
```

这同样不是前盖总高。

因为Rail-B位于更外半径，它可以与Rail-A的74.4mm在Z方向部分重合。

因此不能写成：

```text
74.4 + 37 = 前盖高度
```

正确理解：

```text
Rail-A至少74.4
Rail-B至少37
两条轨道可以并行嵌套
```

于是仅从功能轨道下限看：

```text
前盖主动功能区轴向长度
至少 ≥ max(74.4, 37)
= 74.4 mm
```

仍然不包含最终结构余量。

---

# 10. 缠绕垫3.2mm为什么单独处理

缠绕垫：

```text
φ115×φ105×3.2
```

它位于连接接口端面，是“端面压紧件”，不是长圆柱导向件。

所以它记录为：

```text
接口轴向厚度 = 3.2 mm
```

但不直接与Rail-A/Rail-B全部相加。

第4C后续要确定的是它的压紧端面 `Z_GASKET`，而不是拿3.2凑前盖高度。

---

# 11. V11局部Z站位表

以：

```text
Z0_local = 止推垫球体侧端面
```

为0：

| Station | 功能面 | Z_local mm | 状态 |
|---|---|---:|---|
| Z0 | 止推垫球体侧端面 | 0 | C |
| Z1 | 止推垫外端 / 内轴承起点 | 2 | B/C |
| Z2 | 内轴承终点 / 双O圈区起点 | 52 | B/C |
| Z2a | 上主轴承反力中心 | 27 | B/C |
| Z3 | 双O圈区终点 / 填料起点 | 70 | B/C |
| Z4 | 填料压后终点 | 74.4 | B/C |

对应SolidWorks变量：

```text
Z_STEM_THRUST_IN      = Z0_ABS
Z_STEM_THRUST_OUT     = Z0_ABS + 2

Z_BRG_IN_START        = Z0_ABS + 2
Z_BRG_IN_END          = Z0_ABS + 52
Z_RU                  = Z0_ABS + 27

Z_ORING_ZONE_START    = Z0_ABS + 52
Z_ORING_ZONE_END      = Z0_ABS + 70

Z_PACK_START          = Z0_ABS + 70
Z_PACK_END            = Z0_ABS + 74.4
```

其中唯一还缺的是：

```text
Z0_ABS = ?
```

也就是止推垫球体侧端面相对球心O的绝对Z。

---

# 12. 一旦Z0_ABS确定，V9上支承反力立即从“假对称”升级

球心：

```text
Z(O)=0
```

上主轴承反力中心：

```text
Z_RU = Z0_ABS + 27
```

因此：

```text
a = Z0_ABS + 27
```

下部第4D完成后会得到：

```text
b
```

然后V9的真实反力：

```text
Ru = 164.69 × b/(a+b) kN
Rl = 164.69 × a/(a+b) kN
```

不再永久假设82.35/82.35。

---

# 13. 第4C现在真正还缺什么

要把 `Z0_ABS` 关闭，必须知道球体上部与前盖之间的真实轴向几何：

```text
球体上驱动区域的实际上端基准面
阀杆防吹出肩在球体/前盖中的位置
公司要求的阀杆—球体1~3mm间隙具体落在哪两个面
前盖靠中腔的第一定位台阶
```

当前主账只有：

```text
上驱动接口 50×70×35（C）
STEM_GAP = 2.0（公司1~3中值）
```

但缺少“35深从哪一个绝对Z面开始”的真实剖面证据。

所以本页不伪造 `Z0_ABS`。

下一步证据优先级：

```text
12寸真实前盖/球体二维图
↓
20寸原生SolidWorks前盖剖面（只取拓扑和站位关系）
↓
12寸BOM尺寸链
↓
公司规范
↓
工程候选
```

---

# 14. V11可以立即进入数字化总装骨架的结果

新增：

```text
Rail-A最小连续轴向功能空间 = 74.4 mm
Rail-B条件性最小功能空间   = 37 mm

Z0_local = 0
Z1 = 2
Z_RU_local = 27
Z2 = 52
Z3 = 70
Z4 = 74.4

上主支承反力中心：
Z_RU = Z0_ABS + 27
```

新增装配约束：

```text
内轴承区不能与双O圈区重叠
内轴承区不能与填料区重叠
双O圈区不能与填料区重叠

在φ95外O圈采用内孔槽候选时：
外O圈槽区不能与φ105外轴承座区重叠
```

---

# 15. V11冻结等级

## 可以进入SolidWorks骨架

```text
Rail-A功能顺序（第一版C）
0 / 2 / 27 / 52 / 70 / 74.4 局部Z站位
Rail-A最小轴向空间74.4
Z_RU = Z0_ABS+27
外O圈槽与外轴承区的非重叠约束（当前槽方向候选成立时）
```

## 不能制造冻结

```text
Z0_ABS
前盖总高
防吹出肩厚度
前盖顶部壁厚/压盖结构
外O圈最终槽方向
连接盘绝对Z
键轴伸出长度
```

---

# 16. 下一步 V12 / 第4D

V11之后有两条紧邻任务：

```text
A. 找到/还原前盖靠球体侧真实基准 → 关闭Z0_ABS → 得到a

B. 对底盖做同样的局部Z轨道 → 得到b
```

完成后：

```text
a/b真实坐标
↓
上下支承反力回算
↓
前盖/底盖局部强度
↓
内部整体空间
```

这时第4步才真正从“零件规格链”进入“总装尺寸链”。

---

# 17. V11变更记录

## 2026-08-21

- 建立前盖三条径向功能轨道；
- 用直径冲突证明Rail-A各功能区不能轴向重叠；
- 首次得到Rail-A轴向功能下限 `74.4 mm`；
- 建立局部站位 `0 / 2 / 27 / 52 / 70 / 74.4`；
- 得到 `Z_RU = Z0_ABS + 27`；
- 对外O圈槽/外轴承座做0.8mm径向余量冲突检查，要求第4C轴向错位；
- 明确继续缺口是 `Z0_ABS`，不以猜值补齐。
