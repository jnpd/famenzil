# Q347F 12寸 Class150——第5D步：API 6D侧装式结构长度 / 610与838口径关闭 V19

> **定位**：V18建立了610mm长型骨架，但随后在API 6D表C.2中又读到NPS12 / Class150 / RF = 838mm。V19专门关闭这个冲突。  
> **结论**：610与838都属于API 6D有效值，但对应不同结构：**610属于仅侧装式（Side Entry only）长型球阀；838属于顶装式/通用顶装口径。** 本项目为铸钢两片式枢轴固定、侧装分体结构，因此当前结构长度采用610mm。  
> **状态**：API 6D侧装式标准值A级；项目若另有数据单覆盖，则由项目数据单优先。

[← V18 阀体/阀盖参数化接口](./Q347F_12in_Class150_第5C步_阀体阀盖参数化接口_中法兰与XYZ骨架_V18.md)  
[→ 数字化总装骨架总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)

---

# 1. 为什么会同时出现610和838

API 6D表C.2对球阀结构长度不是只给一张表，而是按结构类型区分。

本轮查到：

```text
NPS12 / DN300
Class150
RF
```

在不同结构表中存在：

```text
Side Entry only / 仅侧装式：
Long Pattern = 24.00 in = 610 mm
Short Pattern = 14.00 in = 356 mm

Top Entry / 顶装式相关表：
RF A = 33.00 in = 838 mm
```

所以：

```text
610 ≠ 错
838 ≠ 错
```

错误只会发生在：

```text
没先判断阀体结构类型
就直接拿表中任一NPS12数字
```

---

# 2. 本项目结构身份

公司 `GFE-JS01-2025` 对**铸钢固定球阀**规定：

```text
CL150~CL600
NPS≤20 → 两片式、枢轴固定结构
NPS≥22 → 三片式、支撑板固定结构
```

本项目：

```text
BODY_MAT = ASTM A216 WCB
CLASS = 150
NPS = 12
```

因此：

```text
BODY_CONSTRUCTION = 两片式
BALL_SUPPORT_TYPE = 枢轴固定
ENTRY_TYPE = 侧装 / 分体式 Side Entry
```

状态：

```text
A / C+
```

20寸成熟Q347F总装同样呈现左右体盖/中央阀体分体拓扑，为侧装结构提供交叉证据。

---

# 3. API 6D侧装式NPS12 Class150结构长度

当前侧装式表：

| 项目 | RF | BW | RTJ |
|---|---:|---:|---:|
| Long Pattern / Full Bore | **610** | 635 | 622 |
| Short Pattern | **356** | 502 | 368 |

本项目当前端连接按RF骨架管理，因此：

```text
F2F_API6D_SIDE_LONG_RF = 610 mm    A-STD
F2F_API6D_SIDE_SHORT_RF = 356 mm   A-STD-alt
```

NPS12结构长度标准公差：

```text
±3 mm
```

即长型标准制造总长控制：

```text
610 ±3 mm
```

注意：项目/客户数据单如果明确给出另一结构长度，项目数据单优先。

---

# 4. 为什么当前选610而不是356

当前12寸内部骨架：

```text
BALL_W_X = 348
BALL_X_FACE = ±174
WSEAT_ENV ≈58 / side
```

## 4.1 610长型

```text
X_END = ±610/2 = ±305
```

每侧从球体闭阀端基准到端法兰面：

```text
305-174 = 131 mm
```

扣掉约58mm阀座功能空间：

```text
131-58 = 73 mm/side
```

这73mm还可分给：

```text
阀盖承压颈
体盖分界面
中法兰
端法兰颈部/过渡
制造余量
```

几何可行。

## 4.2 356短型

```text
X_END = ±178
178-174 = 4 mm/side
```

当前单侧阀座功能空间约58mm，所以在当前：

```text
球体宽348
+
当前阀座结构
```

不变的前提下，356短型明显不兼容。

因此：

```text
VALVE_F2F_CAD = 610 mm    A/C+
F2F_SHORT_356 = A-STD-alt / H-R-for-current-geometry
```

这里“356 H/R”不是说API 6D的356无效，而是说：

> **它不适合当前这套12寸球体/阀座几何链。**

---

# 5. 为什么838不能用于当前Q347F

838来自另一结构类型表，对应顶装式/相关球阀表。

顶装式典型维护路径：

```text
从阀体顶部拆出球体/阀内件
```

当前Q347F骨架：

```text
中央阀体
+
左右体盖/端盖
+
侧向阀座总成
+
上前盖仅承担上支承/阀杆功能
```

不是顶装阀体。

所以：

```text
F2F_TOP_ENTRY_REF = 838 mm    REF-STD
```

但当前设计状态：

```text
H/R-for-this-construction
```

禁止把838写进当前SolidWorks总长。

---

# 6. SolidWorks端面基准正式恢复

当前主配置：

```text
VALVE_F2F_CAD = 610
F2F_TOL_STD = ±3

X_END_FACE_R = +305
X_END_FACE_L = -305
```

定义：

```text
PLN_END_R = X=+305
PLN_END_L = X=-305
```

这些是：

```text
最终阀门端法兰密封面骨架基准
```

不是：

```text
BODY/COVER分界面
阀座接触面
```

三类面继续严格分开。

---

# 7. V19标准来源权限

结构长度来源顺序：

```text
项目数据表 / 客户明确尺寸       A0
↓
API 6D Side Entry表             A-STD
↓
ASME B16.10相关引用/交叉         A-STD-XREF
↓
公司成熟Q347F模型               C+
↓
历史经验值                       H
```

当前仓库尚未发现项目数据单明确覆盖610，因此：

```text
VALVE_F2F_CAD = 610 A/C+
```

制造图出图前再做一次项目数据单覆盖检查。

---

# 8. 对V18的正式补充

V18的几何判断：

```text
610可行
356与当前几何链不兼容
```

继续有效。

V19补充的是：

```text
610和356不只是“常见候选”
而是API 6D Side Entry表中的正式标准值
```

同时明确：

```text
838 = 顶装式相关标准值
不能混入当前Q347F侧装结构
```

---

# 9. 当前F2F变量块

```text
BODY_ENTRY_TYPE = SIDE_ENTRY

F2F_SIDE_LONG_RF  = 610
F2F_SIDE_SHORT_RF = 356
F2F_TOP_ENTRY_REF = 838
F2F_TOL_STD       = 3

VALVE_F2F_CAD = 610
X_END_FACE_R  = +305
X_END_FACE_L  = -305

F2F_PROJECT_OVERRIDE = ?   # 若客户/项目数据单有明确覆盖
```

---

# 10. 下一步

F2F标准口径关闭后，X方向端面不再阻塞第6步。

下一步进入：

```text
第6A 阀体承压边界
↓
ASME B16.34 Class150最小壁厚
↓
公司+3~5mm余量
↓
中央BODY第一版承压外包络
↓
中法兰螺栓面积
↓
局部Boss / VENT / DRAIN肉厚
```

---

# 11. V19变更记录

## 2026-08-22

- 关闭610与838同时出现的API 6D表口径冲突；
- 明确610=仅侧装式NPS12 Class150 RF长型；
- 明确356=同一侧装式表短型；
- 明确838属于顶装式相关表，不适用于当前两片式Q347F；
- 用公司铸钢固定球规则确认NPS12/CL150为两片式枢轴固定；
- 将当前结构分类固定为SIDE_ENTRY；
- 恢复 `VALVE_F2F_CAD=610`，端面基准 `X=±305`；
- 记录NPS12结构长度标准公差±3mm；
- 356保留为标准替代值，但对当前348球宽+58阀座链标记H/R-for-current-geometry。
