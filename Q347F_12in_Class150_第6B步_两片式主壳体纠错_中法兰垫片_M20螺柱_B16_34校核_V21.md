# Q347F 12寸 Class150——第6B步：两片式主壳体纠错 / 中法兰垫片 / M20螺柱 / B16.34校核 V21

> **定位**：V20 已关闭第一版承压壁厚。V21 根据 12 寸目标 BOM、20 寸成熟件逐项对应、用户提供的爆炸图、公司 `GFE-JS01-2025` 与 API 6D 侧装固定球阀结构图，先纠正主壳体拓扑，再关闭主 BODY—BODY_COVER 中法兰第一轮尺寸与螺栓面积校核。
>
> **本页最重要的纠错**：本项目主承压壳体是 `BODY ×1 + BODY_COVER ×1` 的两片式侧装结构，只有 **1 个主 BODY—BODY_COVER 分界面**。V18 中 `LEFT COVER — BODY — RIGHT COVER` 的双主阀盖抽象降为 `H/R`；V18 的阀座局部坐标、中法兰参数公式、上/下小盖接口公式仍继续有效。

[← 总导航](./00_Q347F_12in_Class150_文档导航_从这里开始.md)  
[← 数字化总装骨架总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)  
[← V20 阀体最小壁厚 / 承压外壳](./Q347F_12in_Class150_第6A步_ASME_B16_34阀体最小壁厚_压力等级风险_承压外壳_V20.md)

---

# 1. 先把“阀盖”三个概念永久拆开

本项目以后统一采用以下名称，避免“阀盖 / 前盖 / 底盖”互相混淆：

```text
BODY
= 主阀体，绿色主承压壳体

BODY_COVER
= BOM里的“阀盖”
= 侧装主阀盖 / 主连接体
= 两片式主压力壳体的第二大件

STEM_COVER
= BOM里的“前盖”
= Z轴上部的小型阀杆 / 上支承 / 密封盖

BOTTOM_COVER
= BOM里的“底盖”
= Z轴下部的小型下支承盖
```

所以：

```text
两片式主壳体
= BODY ×1
+ BODY_COVER ×1
```

并不等于整阀只有两个零件。

---

# 2. 当前正确一级拓扑

以球心 `O=(0,0,0)`：

```text
                           STEM_COVER
                               │
                               │ +Z
                               │
END FLANGE ── BODY ── SEAT ─── ● ─── SEAT ── BODY_COVER ── END FLANGE
                               │
                               │ -Z
                               │
                         BOTTOM_COVER
```

其中：

```text
● = BALL_CENTER_O
```

结构含义：

```text
固定球 / trunnion mounted
= 球体由上下支承约束

两片式 / two-piece body
= 主承压壳体由 BODY + BODY_COVER 两大件组成

侧装式 / side entry
= 拆开 BODY_COVER 后，从X轴一侧装入/检修球体、阀座等内件
```

这三个词回答不同问题，不冲突。

---

# 3. V18必须退役的旧拓扑

V18曾为了参数化先抽象为：

```text
LEFT COVER ─ BODY ─ RIGHT COVER
```

并建立：

```text
IF-X-L
IF-X-R
X_BODY_COVER_IF_L
X_BODY_COVER_IF_R
```

结合当前 BOM 与爆炸图后，这一层拓扑不再成立。

正式处理：

```text
IF-X-L / IF-X-R 作为“两只主阀盖接口” → H/R
X_BODY_COVER_IF_L/R 镜像假设              → H/R
```

替换为：

```text
IF-X-BODY-JOINT
= 唯一 BODY ↔ BODY_COVER 主中法兰接口

X_BODY_JOINT
= 主中法兰分界面X坐标
= ? D
```

**注意**：左右两套阀座仍存在，左右阀座局部坐标 `u_L / u_R` 继续有效；壳体不对称分件，不代表密封副不能左右对称。

---

# 4. 12寸BOM把主中法兰零件组直接串起来了

12寸目标 BOM 中连续出现：

```text
O形圈            VITON          φ466×7             ×1
金属缠绕垫        316+柔性石墨    φ500×φ490×3.2      ×1
圆柱销            A29 1035       φ12×22             ×2
螺柱              A193 B7M       M20×85             ×20
螺母              A194 2HM       M20                ×20
阀盖              A216 WCB       12-Q347F-150LB      ×1
```

而“逐项对应表”又将它们对应到20寸成熟主中法兰：

```text
12寸 φ466×7              ↔ 20寸 φ730×5.3
12寸 φ500×φ490×3.2       ↔ 20寸 φ774×φ759×4.5
12寸 20×M20×85           ↔ 20寸 32×M30×145
12寸 BODY_COVER ×1       ↔ 20寸 阀盖 ×1
```

因此 V21 当前绑定：

| 参数 | 当前值 | 状态 |
|---|---:|---|
| `BODY_QTY` | 1 | A |
| `BODY_COVER_QTY` | 1 | A |
| `BODY_JOINT_COUNT` | 1 | A/C+ |
| `BODY_JOINT_ORING` | φ466×7 | A/C+ |
| `MID_GASKET` | φ500×φ490×3.2 | A/C+ |
| `MID_STUD` | M20×85 | A/C+ |
| `MID_STUD_QTY` | 20 | A/C+ |
| `MID_NUT` | M20 | A/C+ |
| `MID_NUT_QTY` | 20 | A/C+ |

此前把 `M24×100×10` 视为主中法兰候选的判断：

```text
H/R
```

`M24×100×10` 当前功能重新回到：

```text
M24_GROUP_FUNCTION = ? D
```

后续根据装配位置另行归属。

---

# 5. φ500×φ490×3.2缠绕垫与公司中法兰规则完全吻合

公司 `GFE-JS01-2025` 对固定球阀中法兰要求：

```text
金属缠绕垫
垫片宽度 5~10 mm
```

12寸 BOM：

```text
OD = 500
ID = 490
T  = 3.2
```

环形垫片单边径向宽度：

```text
MID_GASKET_RADIAL_W
= (OD-ID)/2
= (500-490)/2
= 5.0 mm
```

因此：

```text
MID_GASKET_RADIAL_W = 5.0 mm
```

刚好落在公司：

```text
5~10 mm
```

的下限。

状态：

```text
A/B + C+
```

这也是“φ500×φ490×3.2 确实是主中法兰垫片”的一条独立结构验证。

---

# 6. M20孔径：第一版CAD用φ22，但暂不制造冻结

M20普通粗牙：

```text
P = 2.5 mm
```

ISO 273 常用间隙孔：

```text
紧配 / close   = φ21
普通 / normal = φ22
松配 / loose  = φ24
```

在没有12寸主中法兰加工图明确孔径前，第一版参数化CAD采用：

```text
MID_BOLT_HOLE_D_CAD = 22 mm
```

状态：

```text
C / STD-default
```

禁止把φ22直接写成最终加工冻结值。

---

# 7. 按公司公式直接得到BCD范围

公司规则：

```text
MID_BCD
= MID_GASKET_OD
+ MID_BOLT_HOLE_D
+ 3~6 mm
```

代入：

```text
MID_GASKET_OD = 500
MID_BOLT_HOLE_D_CAD = 22
```

得到：

```text
MID_BCD_MIN = 500+22+3 = 525 mm
MID_BCD_MAX = 500+22+6 = 528 mm
```

所以：

```text
MID_BCD_GUIDE = φ525~φ528 mm
```

第一版SolidWorks中值：

```text
MID_BCD_CAD = φ526.5 mm
```

状态：`C`。

20只螺柱等分时，圆周节距约：

```text
π×526.5/20
≈82.7 mm
```

从纯圆周空间看不存在明显拥挤，但最终还要检查扳手、螺母、铸造圆角和相邻附件干涉。

---

# 8. 中法兰外径：目前只能关闭到敏感性区间

公司公式：

```text
MID_FLANGE_OD
= MID_BCD + MID_COUNTERBORE_OR_SPOTFACE_D
```

M20 的 GB/T 6175 2型六角螺母：

```text
对边 s = 30 mm
对角 e_min ≈32.95 mm
```

为了给螺母/套筒留操作空间，第一轮可建立：

```text
MID_SPOTFACE_D_CAD ≈36 mm   C
```

但这个36不是公司标准值，也不是制造冻结值。

敏感性：

| 假定锪平/沉孔直径 | BCD范围 | 中法兰OD范围 |
|---:|---:|---:|
| 35 | 525~528 | 560~563 |
| 36 | 525~528 | 561~564 |
| 38 | 525~528 | 563~566 |

当前粗模默认：

```text
MID_FLANGE_OD_CAD ≈562.5 mm
```

仅作为 `C` 级包络。

值得注意的是：

```text
V20 BODY_MID中央承压外包络 ≈φ498.2
主中法兰垫片OD             =φ500
```

两者仅差约1.8mm直径，量级非常一致；而真正中法兰外圆因为要容纳M20螺栓和螺母，会局部鼓到约φ560级。这是合理的几何层级。

---

# 9. ASME B16.34-2025：这里应按“sectional body joint”而不是普通小阀盖螺栓理解

当前 BODY_COVER 是两片式压力壳体的第二大件，并带一侧端部连接；其连接螺柱不仅夹紧密封，还可能承受管路机械载荷。

因此 V21 采用：

```text
ASME B16.34-2025 §6.4.2 Body Joints
```

作为当前主校核口径。

该条适用于：

```text
sectional body construction
+
bolted/threaded body joint
+
joint subject to piping mechanical loads
```

这比把它当普通 `bonnet/cover joint` 更符合当前两片式侧装结构角色。

---

# 10. §6.4.2.1：20×M20第一轮有效拉应力面积校核

B16.34-2025 对螺栓连接 body joint 的最低面积关系：

```text
Pc*Ag/Ab <= min(K2*Sa, Limit)
```

其中：

```text
Pc = 压力等级Class数字
Ag = 有效密封外围包围面积
Ab = 全部螺栓有效拉应力面积
```

对于本项目：

```text
Pc = 150
```

特别强调：

```text
Pc = 150
≠ P_LOAD_CALC = 2.00 MPa
```

B16.34这个公式里的 `Pc` 是压力等级代号，不是把2MPa直接代入。

## 10.1 Ag

当前主中法兰外有效密封外围按缠绕垫OD φ500：

```text
Ag
= π/4×500²
≈196349.5 mm²
```

φ466×7 O圈的外包络小于φ500垫片，不控制最外有效密封外围。

## 10.2 单根M20有效拉应力面积

M20粗牙：

```text
P=2.5
```

按公制螺纹常用有效拉应力面积：

```text
As_one
≈ π/4×(20-0.9382×2.5)²
≈244.79 mm²
≈245 mm²
```

20根：

```text
Ab_actual
=20×244.79
≈4895.9 mm²
```

## 10.3 右端允许值

公司计算口径取：

```text
Sa ≈20000 psi ≈138 MPa
```

B16.34-2025在 `Sa<=172MPa` 时：

```text
K2=50.76/MPa
Limit=7000
```

所以：

```text
K2×Sa
≈50.76×138
≈7004.9
```

受：

```text
Limit=7000
```

控制。

## 10.4 要求面积与裕量

```text
Ab_required
= Pc×Ag/7000
=150×196349.5/7000
≈4207.5 mm²
```

实际：

```text
Ab_actual≈4895.9 mm²
```

面积裕量：

```text
MID_BOLT_AREA_MARGIN
=4895.9/4207.5
≈1.164
```

即约：

```text
+16.4%
```

也可直接看左端：

```text
Pc×Ag/Ab_actual
≈6015.8
<7000
```

因此当前结论：

> **12寸 BOM 的 20×M20 主中法兰螺柱，按 ASME B16.34-2025 §6.4.2.1 最低总有效拉应力面积关系，第一轮通过，面积裕量约1.16。**

状态：

```text
B/C+ preliminary PASS
```

但这不是整个中法兰“最终合格”结论。

---

# 11. 2025版还有一个不能漏掉的门：§6.4.2.3 Section Modulus

B16.34-2025 对 sectional body joint 还要求检查：

```text
body nozzle section modulus Zbn
```

与：

```text
bolting arrangement section modulus Zfb
```

对圆形螺栓圈：

```text
Zfb = C*AB/4
```

其中：

```text
C  = bolt circle diameter
AB = total root area of bolting
```

并需结合：

```text
Sa = bolt allowable stress
Sb = body material allowable stress
```

满足该节的截面模量关系。

当前还缺：

```text
M20螺纹正式root area口径
WCB在约38~39℃的正式Sb
实际最小body-nozzle截面D/d
最终BCD
```

所以新增高优先级开放项：

```text
R21_02_SECTION_MODULUS = OPEN
```

> **V21只允许说“§6.4.2.1面积校核初步通过”，不允许说“中法兰已经完全满足B16.34”。**

---

# 12. 还必须补的真实工程校核

即使最低面积和截面模量都通过，制造冻结前仍需检查：

```text
垫片所需压紧载荷
螺柱预紧力
螺母/支承面局部压应力
中法兰环板弯曲
BODY/BODY_COVER局部应力
圆角应力集中
铸造最小肉厚
装配工具空间
螺柱伸出螺母2~3牙
高压气体/防火/低泄漏要求
```

因此当前 `MID_FLANGE_OD≈562.5` 只是CAD包络，不是二维加工图值。

---

# 13. 本轮对空间坐标的影响

永久保留：

```text
BALL_CENTER_O=(0,0,0)
X_END_FACE_L=-305
X_END_FACE_R=+305
X_CONTACT_L=-166.036
X_CONTACT_R=+166.036
```

但主壳体接口改为唯一：

```text
PLN_BODY_JOINT_X:
X_BODY_JOINT = ? D
```

不能再写：

```text
X_BODY_COVER_IF_L = -X_BODY_COVER_IF_R
```

因为两片式壳体的分件面并不要求关于球心镜像成两张面。

下一步真正需要关闭的是：

```text
球面接触带
↓
阀座后部轴向台阶
↓
BODY_COVER座腔
↓
BODY/BODY_COVER定位止口
↓
主中法兰密封面
↓
X_BODY_JOINT
↓
BODY_COVER到+305或-305端面的轴向包装
```

具体在哪一侧采用 `+X` 或 `-X`，以12寸总装/源模型方向统一后冻结；当前不因爆炸图视角强行指定符号。

---

# 14. V21当前SolidWorks变量块

```text
# 主壳体拓扑
BODY_PIECE_COUNT=2
BODY_QTY=1
BODY_COVER_QTY=1
BODY_JOINT_COUNT=1
X_BODY_JOINT=?

# 主中法兰密封
BODY_JOINT_ORING_D0=466
BODY_JOINT_ORING_CS=7
MID_GASKET_OD=500
MID_GASKET_ID=490
MID_GASKET_T=3.2
MID_GASKET_RADIAL_W=5

# 主中法兰紧固
MID_STUD_SIZE=M20
MID_STUD_L=85
MID_STUD_QTY=20
MID_NUT_SIZE=M20
MID_NUT_QTY=20
MID_BOLT_HOLE_D_CAD=22

# BCD
MID_BCD_MIN=525
MID_BCD_MAX=528
MID_BCD_CAD=526.5

# 外径仅CAD敏感性
MID_SPOTFACE_D=?
MID_SPOTFACE_D_CAD=36
MID_FLANGE_OD_CAD=562.5

# B16.34 §6.4.2.1
MID_AG=196349.5
MID_AS_ONE=244.79
MID_AB_ACTUAL=4895.9
MID_AB_REQUIRED=4207.5
MID_BOLT_AREA_MARGIN=1.164

# 2025版截面模量门
MID_ZBN=?
MID_ZFB=?
R21_02_SECTION_MODULUS=OPEN

# 错误候选退役
M24_MAIN_BODY_JOINT=H/R
M24_GROUP_FUNCTION=?
```

---

# 15. V21正式结论

现在主压力壳结构应理解为：

```text
BODY
│
├─ 一侧端法兰与BODY连续
├─ 中央球腔
├─ 一侧阀座腔
│
└─ 唯一主中法兰
      │
      ├─ φ466×7 O圈
      ├─ φ500×φ490×3.2缠绕垫
      ├─ 20×M20×85螺柱
      └─ φ12×22定位销×2
      │
   BODY_COVER
      │
      ├─ 另一侧阀座腔
      └─ 另一侧端法兰
```

当前已关闭：

```text
主壳体片数 =2
主中法兰数量 =1
主中法兰垫片 =φ500×φ490×3.2
垫片径向宽 =5mm
主螺柱 =20×M20×85
CAD普通孔 =φ22
BCD指导范围 =φ525~φ528
CAD BCD =φ526.5
CAD中法兰外包络 ≈φ562.5（仅C）
B16.34 §6.4.2.1面积裕量 ≈1.164，初步通过
```

仍未关闭：

```text
X_BODY_JOINT
真实锪平/沉孔直径
最终中法兰OD
B16.34-2025 §6.4.2.3截面模量
垫片压紧/预紧/局部弯曲完整校核
M24×100×10实际归属
```

---

# 16. 下一步：V22 / 第6C

下一步不再继续猜螺栓，而是沿真实装配方向关闭：

```text
X_BODY_JOINT
↓
BODY侧阀座腔轴向链
↓
BODY_COVER侧阀座腔轴向链
↓
定位止口 / O圈 / 缠绕垫轴向层
↓
BODY_COVER轴向厚度
↓
与F2F=610、端面±305闭合
↓
真正生成 BODY + BODY_COVER 两个承压实体骨架
```

> **一句话：V21以后，12寸主壳体不再是“左右两个大阀盖”，而是“一只BODY + 一只BODY_COVER + 一个主中法兰”；主中法兰已经从问号推进到 φ500/490垫片 + 20×M20 + BCD约526.5 的可建模状态。**
