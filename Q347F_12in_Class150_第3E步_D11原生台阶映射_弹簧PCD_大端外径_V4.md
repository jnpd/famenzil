# Q347F 12寸 Class 150——第3E页：D11原生台阶映射、弹簧PCD与大端外径 V4

> **定位**：承接第3D页。第3D已经把真实球面接触带修正为 `D9≈φ323.88 / D10≈φ327.13`，并得到 `D11` 33%自泄压下限约 `φ339.91`。本页利用20寸原生支承圈的 `φ531.6导向外圆 + φ560弹簧PCD + φ582大端` 做反证，把公司 `D11` 与实际台阶进一步对应，并据此重新设计12寸主O圈导向、36孔弹簧PCD和大端外径。  
> **当前有效版本：V4。**  
> [← 返回主文档](./Q347F_12in_Class150_球体设计计算底稿_第一部分_小白可读公式完整版.md) ｜ [← 第3D页](./Q347F_12in_Class150_第3D步_真实密封接触带_第二O圈_防火面_弹簧_Wseat_V3.md)

---

## 1. 先看20寸原生结构的三个关键直径

20寸原生支承圈/阀盖已提取：

```text
支承圈主导向外圆   = φ531.6
弹簧孔圆周PCD      = φ560
支承圈最大大端外径 = φ582
主弹簧孔            = φ14
```

20寸真正球面接触带：

```text
D9_20  ≈ φ521.000
D10_20 ≈ φ526.218
```

公司预紧比压：

```text
qpre = 2.5 MPa
```

所以20寸真实接触面积和弹簧总预紧力约：

```text
Aseal20 ≈ 4291.78 mm²
Q2_20 ≈ 10.73 kN/侧
```

---

## 2. 为什么 `φ531.6` 高概率就是公司D11对应的静压/活塞外径

若按公司成熟样例继续取：

```text
D4 ≈ D9 ≈ φ521
```

把20寸原生：

```text
D11 = φ531.6
```

代入公司自泄压公式：

```text
ΔP1 = 1.33Q2 / [π/4(D11²-D4²)]
```

得到：

```text
ΔP1 ≈ 1.628 MPa
```

如果它刚好对应33%额定压差，反求额定压力：

```text
P_equiv = 1.628 / 0.33
        ≈ 4.93 MPa
```

这正好落在 **Class300常温约5 MPa量级**。

也就是说：

```text
20寸原生导向OD φ531.6
```

不是一个随意结构尺寸，它与“公司2.5MPa预紧 + 33%自泄压”液压条件几乎完全对上。

同时20寸弹簧孔与这条外圆的径向关系：

```text
D11半径 = 531.6/2 = 265.8
弹簧孔中心半径 = 560/2 = 280
孔半径 = 14/2 = 7

弹簧孔内边缘到D11外缘：
280 - 7 - 265.8
= 7.2 mm
```

公司2025规范要求：

```text
新设计弹簧孔边缘距离静压面至少5 mm
```

旧成熟模型的7.2 mm正好满足这一结构逻辑。

因此当前高置信度映射为：

```text
公司 D11
≈ 金属阀座支承圈的主活塞/静压导向外径

20寸：D11≈φ531.6
```

而：

```text
φ582 = 支承圈大端/弹簧环最大物理外径
```

是另一条尺寸。

---

## 3. 这正式推翻了12寸 `导向OD≈φ330.6` 的简单比例值

第3C曾把20寸：

```text
φ531.6 × 465/748 ≈ φ330.5
```

作为12寸导向OD草模。

现在知道 `φ531.6` 实际承担的是D11液压功能后，就不能再按球径比例缩小。

12寸D11必须由本项目：

```text
真实D9/D10
+ 2MPa
+ 2.5MPa预紧
+ 33%自泄压
```

重新决定。

第3D已经算出：

```text
D11,min,relief ≈ φ339.91
```

所以V4选择：

```text
D11 = φ342 mm
```

而不是φ330.6。

回代：

```text
qm ≈ 13.92 MPa
ΔP1 ≈ 0.582 MPa
0.582 < 0.660 MPa
```

第一轮通过。

---

## 4. 主O圈 φ320×5.3 必须跟着 D11 重建

既然D11对应支承圈主活塞/导向外径，则V4：

```text
支承圈主导向OD = D11 = φ342.0
```

20寸成熟导向孔与支承圈导向OD直径间隙约：

```text
532.0 - 531.6 = 0.4 mm
```

12寸先沿用这一成熟装配习惯做C级候选：

```text
阀盖主导向孔 = φ342.4
```

公司 `φ5.3` O圈静槽深：

```text
4.2 mm
```

所以槽底OD：

```text
Droot
= 342.0 - 2×4.2
= φ333.6 mm
```

O圈自由内径：

```text
φ320
```

安装拉伸：

```text
εstretch
= (333.6-320)/320
≈ 4.25%
```

安装后径向高度：

```text
h
= (342.4-333.6)/2
= 4.4 mm
```

压缩率：

```text
εcompression
= (5.3-4.4)/5.3
≈ 16.98%
```

因此新的V4主O圈链为：

```text
主O圈            φ320×5.3
槽底OD           φ333.6
支承圈导向OD/D11 φ342.0
阀盖导向孔       φ342.4
```

其中 `4.25%` O圈内径拉伸需要后续按GB/T 3452.1配套安装要求或供应商规范确认；公司当前GFE规范只给了O圈尺寸和沟槽尺寸，没有给允许拉伸率，因此不能在这里假装“已最终合格”。

---

## 5. 36只/侧弹簧孔 PCD 现在可以反算

第3D已经确定：

```text
弹簧OD = φ8
V3主弹簧孔 = φ8.5
数量 = 36孔/侧
```

公司规则：

```text
弹簧孔边缘距静压面 ≥ 5 mm
```

现在D11静压外径已经映射为：

```text
φ342
```

弹簧孔位于D11外侧，所以：

```text
PCD/2 - 8.5/2 - 342/2 ≥ 5
```

整理：

```text
PCD ≥ 342 + 8.5 + 2×5
    ≥ 360.5 mm
```

工程圆整V4：

```text
弹簧PCD = φ362 mm
```

回代边距：

```text
362/2 - 8.5/2 - 342/2
= 181 - 4.25 - 171
= 5.75 mm
```

满足：

```text
5.75 > 5 mm
```

所以当前：

```text
36×φ8.5
PCD φ362
```

可以作为V4圆周草模。

---

## 6. 36孔圆周间距检查

36孔均布：

```text
角间距 = 360°/36 = 10°
```

PCD φ362，中心圆半径181：

相邻孔中心弦距：

```text
s = 2×181×sin(10°/2)
  ≈ 31.55 mm
```

扣除φ8.5孔径，最窄圆周净距约：

```text
31.55 - 8.5
≈ 23.05 mm
```

圆周空间充足。

---

## 7. 大端最小外径：现在由弹簧孔反推，不再按20寸比例硬缩

20寸成熟结构：

```text
最大大端半径 = 582/2 = 291
弹簧孔外边缘半径 = 560/2 + 14/2 = 287
```

所以旧模型弹簧孔外边缘到大端外圆约留：

```text
291 - 287 = 4 mm
```

这是**历史成熟结构余量H**，不是2025公司规范强制值，但可作为第一轮结构交叉。

12寸V4：

```text
PCD = 362
孔径 = 8.5
```

孔外缘半径：

```text
362/2 + 8.5/2
= 185.25 mm
```

若保留约4 mm外侧金属：

```text
Rbig,min ≈ 185.25+4
         ≈ 189.25 mm

Dbig,min ≈ φ378.5 mm
```

工程V4圆整：

```text
支承圈大端OD = φ380 mm
```

此时实际外侧金属余量：

```text
380/2 - 185.25
= 4.75 mm
```

比20寸成熟结构的4 mm略大。

这说明以前的：

```text
支承圈大端≈φ362
```

不能继续作为当前新设计制造候选。它只是20寸球径比例得到的旧空间检查值，而当前12寸增加到36只/侧小弹簧并且必须满足静压面5mm边距后，大端必须重新扩大。

---

## 8. 阀盖大端座腔

公司现行规范：

```text
≥6"阀座大端外圆名义尺寸
比阀盖对应内孔小2 mm
```

因此V4若支承圈大端：

```text
φ380
```

则阀盖大端座腔：

```text
φ382 mm
```

公差仍按公司规范对应尺寸段最终标注。

---

## 9. 现在形成四条互不混淆的径向尺寸链

### 9.1 软密封真实接触带

```text
D9  = φ323.88
D10 = φ327.13
```

### 9.2 DEVLON物理零件

```text
物理ID ≈ φ324
物理OD ≈ φ331
轴向 ≈8（草模）
```

### 9.3 主活塞 / O圈 / D11

```text
O圈自由ID        φ320
O圈槽底OD        φ333.6
D11/支承圈导向OD φ342.0
阀盖导向孔       φ342.4
```

### 9.4 弹簧环 / 大端

```text
36×φ8.5弹簧孔
PCD               φ362
支承圈大端OD      φ380
阀盖大端座腔      φ382
```

这四条尺寸从现在开始禁止再互相替代。

---

## 10. 6×φ4注脂孔如何放

公司规范对12寸阀座内部注脂孔：

```text
6×φ4
```

36个弹簧孔每10°一个。6个注脂孔每60°一个，如果与弹簧孔同角度就会重合。

V4圆周布置原则：

```text
弹簧孔：0°、10°、20°……
注脂孔：相对弹簧阵列错开5°，每60°一个
```

即候选角度：

```text
5°、65°、125°、185°、245°、305°
```

具体注脂孔PCD/油路深度仍需结合阀盖外部3/8"NPT接口确定，不在本页冻结。

---

## 11. Wseat V4

径向尺寸本页已经大幅收口，但轴向仍沿用第3D空间包络：

```text
Wseat,envelope ≈ 58 mm
```

原因：

- 20寸原生总宽91映射约56.57；
- DEVLON物理轴向约8；
- 弹簧自由高18，安装约14.7～15.6；
- 主O圈槽宽7；
- 第二O圈槽宽4.8；
- 还需防火面、K1/K3/K4和导向肩。

因此58 mm仍可作为草模空间，但下一步必须从球侧基准面逐段尺寸化，才能成为制造总宽。

---

## 12. V4当前冻结层级

| 参数 | V4 | 状态 |
|---|---:|---|
| D9真实接触 | φ323.88 | B/C |
| D10真实接触 | φ327.13 | B/C |
| D11静压/主活塞外径 | **φ342** | C；高置信度原生映射 |
| 主O圈 | φ320×5.3 | A/C |
| 主O圈槽底OD | **φ333.6** | C |
| 阀盖主导向孔 | **φ342.4** | C/H；0.4直径间隙参考成熟结构 |
| O圈拉伸 | 4.25% | B；允许值待确认 |
| O圈压缩 | 16.98% | B |
| 弹簧 | X-750 φ8×φ1.6×18×7 | A/C |
| 弹簧孔 | **36×φ8.5/侧** | C |
| 弹簧PCD | **φ362** | C；按5mm边距反算 |
| 静压面→弹簧孔边距 | **5.75 mm** | B，满足≥5 |
| 支承圈大端OD | **φ380** | C；旧结构4mm外壁交叉后圆整 |
| 阀盖大端座腔 | **φ382** | C；公司2mm规则 |
| 内部注脂孔 | 6×φ4 | A |
| 注脂孔角度 | 弹簧阵列错开5°候选 | C |
| Wseat空间包络 | 约58 | C/H |

---

## 13. 本页正式作废项

```text
旧：12寸支承圈导向OD≈φ330.6
→ 作废。该值只是20寸φ531.6按球径比例缩放，忽略了12寸本身的液压自泄压要求。

旧：主O圈槽底φ322.2 / 阀盖导向孔φ331.0
→ 随D11映射修正，V4改为槽底φ333.6 / 导向孔φ342.4。

旧：支承圈大端≈φ362、阀盖大端≈φ364
→ 作废为历史空间比例值。按36弹簧孔、D11=342、5mm边距和成熟外壁余量重新得到大端V4≈φ380、阀盖孔≈φ382。
```

---

## 14. 下一步

径向结构已经可以开始画V4剖面。下一步只剩轴向封口：

```text
球侧基准面
→ 防火唇
→ DEVLON R232.5球面与约8mm物理厚度
→ 第二O圈φ311×3.55保持槽
→ 前端金属肩
→ 36只弹簧的安装高度/孔底
→ K4
→ 主O圈7mm槽
→ K3
→ 后导向肩
→ 总宽Wseat
```

完成这一条轴向尺寸链后，第3步阀座就能真正进入SolidWorks零件建模。