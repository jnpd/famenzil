# Q347F 12寸 Class150——第4D步：底部主支承Z坐标骨架与反力中心 V12

> **承接**：[第4A V9：球体压力载荷 / 上下主支承 / 阀杆 / 滑动轴承](./Q347F_12in_Class150_第4A步_球体压力载荷_上下主支承_阀杆_滑动轴承_V9.md)  
> **上部对应页**：[第4C V11：前盖剖面 / Z坐标骨架 / 轴向下限](./Q347F_12in_Class150_第4C步_前盖剖面_Z坐标骨架_轴向下限_V11.md)  
> **同步总账**：[数字化总装骨架：尺寸参数 / 装配关系 / 空间坐标总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)  
> **本页目标**：对下部支承做与V11相同的“先相对坐标、后绝对坐标”处理。优先使用已经强交叉的 `φ70×φ65×50` 下轴承和球体 `φ70×52深` 轴承孔，不对止推垫具体台阶位置进行无依据猜测。

---

# 1. 当前硬输入

12寸BOM底部结构：

```text
底盖                  A182 F51
止推垫                316+PTFE φ65×φ20×2
滑动轴承              316+PTFE φ70×φ65×50
球体                  A182 F316
```

V9已经升级：

```text
下支承轴颈            φ65          C+
球体下轴承孔          φ70          C+
轴承长度              50           A
球体下孔最低深度      ≥50          B
球体下孔CAD候选       φ70×52深     C
```

因此最可靠的拓扑链：

```text
球体φ70下孔
↓
316+PTFE φ70×φ65×50轴承
↓
F51 φ65下支承轴颈
↓
底盖
```

---

# 2. 为什么暂时不把φ65×φ20×2止推垫硬塞进轴向链

BOM写法：

```text
φ65×φ20×2
```

它明确给出了零件规格，但目前没有真实底盖/球体下部剖面证明：

```text
它的φ20孔套在哪一个具体轴段；
它位于球体下孔口上方还是下方；
它与φ65下支承轴颈的哪个肩面配合。
```

所以V12处理：

```text
止推垫规格 = A
绝对Z位置 = D
```

不因为“厚2mm”就强行把它加在轴承某一端。

---

# 3. 定义下部局部坐标

全局仍然：

```text
球心O = Z=0
+Z = 阀杆/蜗轮方向
-Z = 底盖方向
```

定义：

```text
Z_L0_ABS = 球体φ70下轴承孔的孔口基准面
```

因为它在球心下方：

```text
Z_L0_ABS < 0
```

轴承从下孔口向球体内部、即 `+Z` 方向装入。

---

# 4. 下轴承50mm的Z范围

轴承长度：

```text
L = 50 mm
```

若轴承靠孔口正常安装：

```text
Z_LBRG_START = Z_L0_ABS
Z_LBRG_END   = Z_L0_ABS + 50
```

下主支承反力中心取轴承长度中点：

```text
Z_RL
= Z_L0_ABS + 25
```

因为整个下轴承仍位于球心下方，正常应满足：

```text
Z_L0_ABS + 25 < 0
```

因此球心到下主支承反力中心距离：

```text
b
= |Z_RL|
= -(Z_L0_ABS + 25)
```

这就是V12最重要的新关系。

---

# 5. φ70×52深与50mm轴承的2mm余量怎么表达

球体下孔CAD候选：

```text
H_bore = 52 mm
```

轴承：

```text
L_brg = 50 mm
```

所以轴承装入后，孔底侧剩余几何差：

```text
ΔH
= 52 - 50
= 2 mm
```

即：

```text
LOWER_BORE_EXTRA = 2 mm   B/C
```

若轴承外端与孔口齐平，则：

```text
孔底Z = Z_L0_ABS + 52
轴承内端Z = Z_L0_ABS + 50
```

二者相差：

```text
2 mm
```

这2mm当前只理解为：

```text
孔底过渡 / 装配余量 / 圆角逃位的总几何空间候选
```

不能自动解释成“止推垫厚度”。

---

# 6. V9的e≈27mm与V12坐标怎么对应

V9第一轮下支承弯曲敏感性使用：

```text
轴承反力在50mm长度中点 → 25mm
再加入约2mm止推/界面量级
得到 e≈27mm
```

V12现在把它拆开：

```text
轴承反力中心相对孔口 = 25 mm      B
额外根部/界面偏置       = e0         D/C

真实悬臂：
e = 25 + e0
```

V9当时：

```text
e0≈2
→ e≈27
```

继续保留为敏感性候选，但不把“2mm”与球体孔底多出的2mm混为同一个物理尺寸。

---

# 7. 下支承局部坐标表

| Station | 功能面 | 全局Z | 状态 |
|---|---|---:|---|
| L0 | 球体下轴承孔口 | `Z_L0_ABS` | D/C |
| L1 | 下轴承外端 | `Z_L0_ABS` | B/C |
| L2 | 下轴承反力中心 | `Z_L0_ABS+25` | B/C |
| L3 | 下轴承内端 | `Z_L0_ABS+50` | B/C |
| L4 | φ70×52孔底理论面 | `Z_L0_ABS+52` | C |

因此：

```text
Z_RL = Z_L0_ABS + 25
b    = -(Z_L0_ABS + 25)
```

---

# 8. 与V11上部坐标直接拼起来

V11得到：

```text
Z_RU = Z_U0_ABS + 27

a = Z_U0_ABS + 27
```

V12得到：

```text
Z_RL = Z_L0_ABS + 25

b = -(Z_L0_ABS + 25)
```

于是整个上下支承的真实几何只剩两个关键绝对基准：

```text
Z_U0_ABS = 上部止推垫球体侧端面
Z_L0_ABS = 球体下轴承孔口面
```

一旦这两个基准从真实球体/前盖/底盖剖面得到：

```text
a = Z_U0_ABS + 27
b = -(Z_L0_ABS + 25)
```

随后无需再猜上下82.35/82.35。

---

# 9. 真实上下支承反力自动回算公式

V9：

```text
F_support = 164.69 kN
```

因此：

```text
Ru = 164.69 × b/(a+b)  kN
Rl = 164.69 × a/(a+b)  kN
```

代入坐标形式：

```text
a = Z_U0_ABS + 27
b = -(Z_L0_ABS + 25)
```

得到：

```text
Ru
= 164.69 × [-(Z_L0_ABS+25)]
  / [(Z_U0_ABS+27) -(Z_L0_ABS+25)]

Rl
= 164.69 × (Z_U0_ABS+27)
  / [(Z_U0_ABS+27) -(Z_L0_ABS+25)]
```

以后 SolidWorks 骨架只要输出两个绝对站位，就可以自动回填支承反力。

---

# 10. 对称设计目标现在变成一个明确坐标条件

以前只写：

```text
a ≈ b
```

现在可翻译成：

```text
Z_U0_ABS + 27
≈
-(Z_L0_ABS + 25)
```

整理：

```text
Z_U0_ABS + Z_L0_ABS + 52 ≈ 0
```

即：

```text
Z_U0_ABS + Z_L0_ABS ≈ -52 mm
```

注意：

- 这是基于V11上部当前顺序和V12下轴承中心定义得到的**几何对称目标方程**；
- 不是说两个基准面必须关于球心完全镜像；
- 如果真实结构不满足，直接用真实a/b回算反力即可。

---

# 11. SolidWorks骨架新增变量

```text
Z_UPPER_LOCAL0      = Z_U0_ABS
Z_RU                = Z_U0_ABS + 27
A_SUPPORT_ARM       = Z_RU

Z_LOWER_BORE_MOUTH  = Z_L0_ABS
Z_LOWER_BRG_END     = Z_L0_ABS + 50
Z_LOWER_BORE_BOTTOM = Z_L0_ABS + 52
Z_RL                = Z_L0_ABS + 25
B_SUPPORT_ARM       = -Z_RL

SUPPORT_SYM_ERROR
= (Z_U0_ABS + 27) - (-(Z_L0_ABS + 25))
```

设计目标：

```text
SUPPORT_SYM_ERROR ≈ 0
```

但不强制为0；真实结构优先。

---

# 12. 当前还不能填死的下部尺寸

```text
Z_L0_ABS
φ65×φ20×2止推垫实际位置
底盖轴肩根部位置
底盖外密封/O圈绝对位置
下支承轴根部圆角
真实悬臂e
```

这些需要：

```text
12寸底盖真实剖面
或20寸原生底盖结构拓扑
+ 12寸BOM
+ 局部强度
```

共同关闭。

---

# 13. V12本轮新增结果

```text
下轴承：
Z_LBRG = [Z_L0_ABS, Z_L0_ABS+50]

下反力中心：
Z_RL = Z_L0_ABS+25

下支承力臂：
b = -(Z_L0_ABS+25)

φ70×52孔对50轴承的额外孔深：
2 mm

上下对称目标：
Z_U0_ABS + Z_L0_ABS + 52 ≈ 0
```

这些都可以进入数字化骨架参数，不需要先知道底盖总高。

---

# 14. 下一步

现在第4步已经从：

```text
“上下都用φ70×φ65×50”
```

推进到：

```text
上反力中心 = Z_U0_ABS+27
下反力中心 = Z_L0_ABS+25
```

接下来最值钱的工作不是继续发明新直径，而是关闭：

```text
Z_U0_ABS
Z_L0_ABS
```

优先读取/还原：

```text
20寸原生前盖剖面
20寸原生底盖剖面
```

只提取：

```text
谁靠谁
哪个面是轴向基准
轴承从哪个面起
止推垫在哪个肩
底盖轴根部在哪里
```

再用12寸已有尺寸重建，而不是直接比例缩放。

---

# 15. V12变更记录

## 2026-08-21

- 建立下部支承全局Z坐标表达；
- 得到 `Z_RL=Z_L0_ABS+25`；
- 得到 `b=-(Z_L0_ABS+25)`；
- 把φ70×52与50mm轴承的2mm差值明确为孔底几何余量，不冒充止推垫厚度；
- 将V9 `e≈27` 拆成 `25+e0`，避免物理意义混淆；
- 与V11组合得到上下支承对称目标 `Z_U0_ABS+Z_L0_ABS+52≈0`。
