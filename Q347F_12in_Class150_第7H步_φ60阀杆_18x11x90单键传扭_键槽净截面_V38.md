# Q347F 12寸 Class150——第7H步：φ60阀杆 / 18×11×90单键传扭 / 键槽净截面 V38

> **定位**：V37已经把连接盘的标准CAD接口收敛到 ISO 5211 F25，并确认12寸BOM的 `8×M16` 与F25完全对应。V38继续关闭真正传扭的“蜗轮→键→φ60阀杆”链。
>
> **核心纠错**：公司 `GFE-JS01-2025` 明确规定：为了适应驱动装置键槽方向，阀杆设计成双键槽，但除特殊情况外装配时只装一个键。因此12寸BOM虽然列 `18×90键×2`，强度计算不能依赖两键50/50均分，必须按“一键承担100%最大扭矩1800N·m”作为主工况。双键均分仅作参考。

[← 总导航](./00_Q347F_12in_Class150_文档导航_从这里开始.md)  
[← 数字化总装骨架总账](./Q347F_12in_Class150_数字化总装骨架_尺寸参数_装配关系_空间坐标总账.md)  
[← V37 ISO 5211 F25驱动接口](./Q347F_12in_Class150_第7G步_连接盘_ISO5211_F25_8xM16与1800Nm驱动接口_V37.md)

---

# 1. 当前输入

```text
最大校核扭矩 T = 1800 N·m
              = 1,800,000 N·mm

阀杆上部键轴 d = φ60 mm
平键 = 18×90，GB/T 1096 C型
公司/标准截面 = 18×11 mm
轴键槽深 t = 7.0 mm
毂键槽深 ≈4.4 mm
BOM数量 = 2
```

公司规范：

```text
设计双键槽
除特殊情况外，装配时只装一个键
```

所以：

```text
KEY_INSTALLED_LOAD_COUNT_DESIGN = 1
```

状态：`A-policy`。

---

# 2. φ60轴为什么正好对应18×11键

GB/T 1095 / GB/T 1096当前表：

```text
轴径 >58~65 mm
→ 键宽 b=18
→ 键高 h=11
→ 轴槽深 t=7.0
→ 毂槽深约4.4
```

当前φ60正处在该区间。

所以：

```text
STEM_KEY_D=60        C+
KEY_B=18             A/C+
KEY_H=11             STD/C+
KEY_L=90             A
SHAFT_KEYWAY_DEPTH=7 STD
```

---

# 3. 单键承担全部1800N·m——主设计工况

轴表面切向力：

```text
F_t
=2T/d
=2×1,800,000/60
=60,000 N
```

因此：

```text
KEY_TANGENTIAL_FORCE_DESIGN=60 kN
```

---

# 4. 单键剪切应力

先按90mm名义全长：

```text
τ_key
=F_t/(b×L)
=60000/(18×90)
≈37.04 MPa
```

这是V11已经得到的正确φ60结果。

当前：

```text
KEY_SHEAR_STRESS_NOMINAL≈37.0 MPa
```

---

# 5. 单键挤压/承压应力——两种口径都保留

## 5.1 传统半键高口径

普通平键常用简化承压高度：

```text
h_eff=h/2=5.5 mm
```

则：

```text
σ_bearing_half_h
=60000/(5.5×90)
≈121.21 MPa
```

这也是V11历史当前值。

## 5.2 按实际轴槽深得到的更保守工作高度

标准：

```text
h=11
轴槽深t=7
```

键高超出轴圆柱理论外表面的高度约：

```text
h-t=4 mm
```

若按这4mm作为保守毂侧有效承压高度：

```text
σ_bearing_geom
=60000/(4×90)
≈166.67 MPa
```

因此当前不只保留121MPa，而增加：

```text
KEY_BEARING_STRESS_SCREEN≈121~167 MPa
```

最终应该用公司正式键连接强度程序/实际接触定义统一口径。

---

# 6. C型单圆头键：90mm不能盲目全部当有效直段

本项目BOM为：

```text
GB/T1096 C型平键 18×90
```

C型一端为圆头。为了不高估有效承载长度，V38增加一个保守CAD筛查：

```text
L_eff_screen
≈L-b/2
=90-9
=81 mm
```

这不是GB/T 1096强制“有效长度公式”，而是为了剔除单圆头端部的保守工程筛查。

按81mm：

```text
τ_key_C_screen
=60000/(18×81)
≈41.15 MPa
```

半键高承压：

```text
σ_half_C_screen
=60000/(5.5×81)
≈134.68 MPa
```

几何4mm承压高度：

```text
σ_geom_C_screen
=60000/(4×81)
≈185.19 MPa
```

所以当前单键最保守第一轮包络：

```text
KEY_SHEAR_SCREEN≈41.2 MPa
KEY_BEARING_SCREEN≈185.2 MPa
```

---

# 7. 双键均分只能作为参考，不能用于最低设计

如果未来厂家图/装配图证明：

```text
两条键同时装配
且具备可靠均载控制
```

理想均分时每键：

```text
F≈30 kN
τ≈18.52 MPa
σ_half≈60.61 MPa
```

但是实际多键连接会受：

```text
键槽位置误差
键宽/槽宽公差
装配间隙
接触先后
```

影响，不能默认100%均分。

因此：

```text
TWO_KEY_50_50 = P-XREF
SINGLE_KEY_100_PERCENT = CURRENT DESIGN BASIS
```

---

# 8. BOM“两只键”与公司“通常装一只键”如何处理

当前有两个事实：

```text
12寸BOM：18×90键 ×2
公司规范：双键槽，但除特殊情况外通常装1键
```

它们并不一定互相矛盾，因为BOM数量2可能表示：

```text
两个槽都实际装键
或
配置/备用/装配方向需要
或
该12寸项目属于公司规范中的“特殊情况”
```

在没有蜗轮厂家图/最终总装剖面前，不猜具体原因。

新增：

```text
R38_01_KEY_INSTALLED_QTY=OPEN
```

但强度主线已经不受它阻塞，因为按单键100%扭矩设计更保守。

---

# 9. φ60纯圆轴扭转基线

不考虑键槽：

```text
τ60
=16T/(πd³)
=16×1,800,000/(π×60³)
≈42.44 MPa
```

当前：

```text
STEM_TORSION_SMOOTH_60≈42.44 MPa
```

---

# 10. 键槽对φ60阀杆净截面的削弱不能忽略

轴键槽深：

```text
t=7 mm
```

单槽局部从φ60外圆向内切7mm。

为了看键槽削弱敏感性，V38建立两个“等效圆直径筛查”，但明确：**它们不是最终真实非圆截面应力。**

## 10.1 单槽局部保守圆直径

```text
d_eq1≈60-7=53 mm
```

若极保守把该局部截面当φ53整圆：

```text
τ_eq53
=16T/(π×53³)
≈61.58 MPa
```

## 10.2 两个键槽最不利相对布置敏感性

如果两个槽恰好在截面上形成最不利的“根到根”削弱，几何极限参考：

```text
d_eq2≈60-2×7=46 mm
```

若再次极保守等效为φ46整圆：

```text
τ_eq46≈94.18 MPa
```

状态：

```text
φ53 / φ46 equivalent-circle = P-XREF
```

因为真实键槽截面：

```text
不是圆形
具有平底槽/圆角
两个槽角度未知
极惯性矩/扭转常数不等于圆轴
```

最终必须做真实截面或FEA。

---

# 11. 还缺一个关键项：键槽根部应力集中Kt

当前GB/T表给出键槽圆角范围，但真实阀杆零件：

```text
槽底刀具圆角
槽端圆弧
双键槽夹角
```

尚未制造冻结。

所以：

```text
STEM_KEYWAY_KT_FINAL=? D
```

真正危险应力应是：

```text
τ_local ≈ Kt × τ_nominal_non-circular
```

不能仅拿42.44MPa宣布φ60最终合格。

---

# 12. 为什么φ60仍可以继续作为CAD主候选

目前三档应力筛查：

```text
完整φ60圆轴           ≈42.4 MPa
单槽极保守φ53等效      ≈61.6 MPa
双槽极端φ46等效敏感性  ≈94.2 MPa
```

键自身最保守第一轮：

```text
剪切≈41.2 MPa
承压≈185.2 MPa
```

这些数值没有出现“几何上一算就明显失控”的结果。

因此：

```text
STEM_KEY_D=60
```

继续保持：

```text
C / SolidWorks主候选
```

但制造冻结仍需：

```text
A182 F51设计温度许用
真实双键槽角度
真实槽底圆角Kt
键A29 1045实际热处理/强度
蜗轮厂家输入孔及键槽
```

---

# 13. SolidWorks变量块

```text
# STEM DRIVE KEY DESIGN
DRIVE_T_CHECK=1800                    # 最大校核扭矩N·m
STEM_KEY_D=60                         # 上部键轴直径CAD候选
KEY_TYPE=GBT1096_C                    # C型普通平键
KEY_B=18                              # 键宽
KEY_H=11                              # 键高
KEY_L=90                              # 名义长度
SHAFT_KEYWAY_DEPTH=7                  # 轴键槽深
HUB_KEYWAY_DEPTH_REF=4.4              # 毂槽深标准参考

# COMPANY ASSEMBLY BASIS
STEM_KEYWAY_SLOT_QTY=2                # 阀杆设计双键槽
KEY_INSTALLED_LOAD_COUNT_DESIGN=1     # 强度按1键承担全部扭矩
KEY_BOM_QTY=2                         # BOM数量
R38_01_KEY_INSTALLED_QTY=OPEN         # 实际同时装几键待最终装配确认

# SINGLE KEY 100% TORQUE
KEY_FT_DESIGN=60000                   # 单键切向力N
KEY_SHEAR_NOMINAL=37.04               # 90mm全长剪应力MPa
KEY_BEARING_HALF_H_NOMINAL=121.21     # 半键高承压MPa
KEY_BEARING_GEOM_NOMINAL=166.67       # 4mm工作高度保守承压MPa

# C-TYPE CONSERVATIVE EFFECTIVE LENGTH SCREEN
KEY_L_EFF_SCREEN=81                   # C型圆头保守有效直段筛查
KEY_SHEAR_SCREEN=41.15                # MPa
KEY_BEARING_HALF_H_SCREEN=134.68      # MPa
KEY_BEARING_GEOM_SCREEN=185.19        # MPa

# STEM KEYWAY SECTION SENSITIVITY
STEM_TORSION_SMOOTH_60=42.44          # φ60无槽纯扭MPa
STEM_EQ_D_ONE_SLOT=53                  # 单槽极保守等效直径，仅P-XREF
STEM_EQ_TAU_ONE_SLOT=61.58             # MPa, P-XREF
STEM_EQ_D_TWO_SLOT_WORST=46            # 双槽极端等效直径，仅P-XREF
STEM_EQ_TAU_TWO_SLOT_WORST=94.18       # MPa, P-XREF
STEM_KEYWAY_KT_FINAL=?                 # 键槽根部真实应力集中系数
DRIVE_KEY_ANGLE_FINAL=?                # 双键槽夹角/厂家方向
```

---

# 14. V38当前结论

当前传扭设计必须采用：

```text
1800N·m
↓
φ60键轴
↓
单个18×11×90键承担100%扭矩
```

得到第一轮最保守键应力：

```text
剪切约41MPa
承压约185MPa
```

φ60键轴即使做非常保守的键槽净截面敏感性，也没有出现一眼失控的结果，因此可以继续SolidWorks草模。

---

# 15. 下一步 V39

下一步继续关闭连接盘本体，而不是等厂家图：

```text
ISO 5211 F25
+
8×M16 / PCD254
+
φ60键轴
+
连接盘厚度24历史候选
↓
连接盘承扭截面 / M16圆周载荷
↓
F25连接盘第一版外形厚度
↓
建立 F25_VENDOR_PLACEHOLDER
```

厂家正式蜗轮图到位后，只替换：

```text
止口
输入孔
键槽角度
最终高度
```

不再破坏BODY/球体/阀座主骨架。

> **V38一句话结论：双键槽≠双键均载；当前必须按单个18×11×90键独立承担1800N·m设计，φ60键轴仍可作为CAD主候选，但键槽根部Kt和实际厂家键槽方向仍是制造冻结门。**