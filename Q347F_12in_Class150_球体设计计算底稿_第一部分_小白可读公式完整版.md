# Q347F 12寸 Class 150 天然气固定球阀——从内到外设计计算主线

> **设计对象**：Q347F 固定球阀，NPS 12 / DN300，Class 150，天然气，全通径，非金属软密封。  
> **设计方法**：总体工况先锁定，机械结构尺寸按 **从内到外** 生长；每向外增加一层，都回做空间、强度、密封和装配校核。  
> **维护方式**：后续一直更新本文件，不再另外拆分新的计算主线文档。  
> **当前进度**：第1步流道已锁；第2步球体V1骨架已补齐；第3步阀座密封副正在计算。

---

# 0. 整台阀门的唯一设计主线

```text
第0步  工况 / 边界条件
DN / Class / 压力 / 温度 / 介质 / 材料 / 扭矩
          ↓
第1步  流道
φ303
          ↓
第2步  球体
球径 / 流道 / 球宽 / 上驱动接口 / 下支承接口
          ↓
第3步  阀座密封副   ← 当前做到这里
DEVLON + 金属阀座圈 + 弹簧 + O形圈
          ↓
第4步  上下支承系统
阀杆 + 下支承轴 + 滑动轴承
          ↓
第5步  内部整体空间
球体 + 阀座 + 支承 + 旋转 / 移动空间
          ↓
第6步  阀体内腔
座腔 / 中腔 / 上下支承腔
          ↓
第7步  阀体外壁
承压壁厚 + 局部加强 + 铸造 / 加工余量
          ↓
第8步  左右端盖 / 阀盖
          ↓
第9步  底盖 / 上盖 / 填料函
          ↓
第10步 标准法兰
NPS 12 / Class 150 / RF
          ↓
第11步 紧固与密封附件
          ↓
第12步 连接盘 / 蜗轮箱
          ↓
第13步 总装尺寸链
          ↓
第14步 最终校核
压力 / 密封 / 扭矩 / 强度 / 温度 / FEA
```

## 0.1 参数状态规则

| 状态 | 含义 |
|---|---|
| **A 已确认输入** | 当前设计基线，不因试算随意修改 |
| **B 公式计算值** | 由当前输入按明确公式得到，输入变化必须重算 |
| **C 设计候选值** | 已有第一轮计算依据，可以继续往外设计，但尚未冻结制造 |
| **D 待确定** | 前置条件不够，明确写 `?` |
| **H 历史/待回代候选** | 以前算过，但当前不作为主方案 |

> 没有依据的尺寸不硬猜；C候选后续遇到阀座、支承空间或强度冲突时允许回代调整。

---

# 1. 当前总状态

| 层级 | 参数 | 当前值 | 状态 |
|---|---|---:|---|
| 工况 | 公称尺寸 | NPS 12 / DN300 | A |
| 工况 | 压力等级 | Class 150 | A |
| 工况 | 介质 | 天然气 | A |
| 工况 | 结构 | 固定球 | A |
| 工况 | 流道形式 | 全通径 | A |
| 工况 | 设计压力 | 2 MPa | A |
| 工况 | 最大关闭压差 | 暂按2 MPa | C |
| 材料 | 阀体 | ASTM A216 WCB | A |
| 材料 | 球体 | ASTM A182 F316 | A |
| 材料 | 阀杆 | ASTM A182 F51 | A |
| 材料 | 阀座密封材料 | DEVLON | A，具体牌号待补 |
| 材料 | O形圈 | VITON | A，具体牌号待补 |
| 第1层 | 流道 | φ303 mm | A |
| 第2层 | 球体外径 | φ465 mm | C |
| 第2层 | 球体半径 | 232.5 mm | B |
| 第2层 | 基础径向几何空间 | 81 mm | B |
| 第2层 | **球体总宽** | **348 mm** | **C，V1候选** |
| 第2层 | **上驱动接口** | **50×70×35 mm** | **C，V1候选** |
| 第2层 | **下支承接口** | **φ70×40 mm深，R4** | **C，V1候选** |
| 第3层 | 密封角 | 45° | A |
| 第3层 | 实际斜面密封带宽 | 4～6 mm | A |
| 第3层 | V1斜面密封带宽 | 5 mm | C |
| 第3层 | 密封接触中心直径 | φ328.80 mm | B |
| 第3层 | V1密封面内径 | φ325.27 mm | B/C |
| 第3层 | V1密封面外径 | φ332.34 mm | B/C |
| 第3层 | V1密封投影面积 | 约3652 mm² | B/C |
| 第3层 | 金属阀座圈外径 / 总宽 | ? | D，下一小步 |
| 第3层 | 有效受压直径 Deff | ? | D，下一小步 |
| 第4层 | 阀杆有效直径 | φ60 mm | C |
| 第4层 | 下支承轴最终尺寸 | ? | D，后续回代 |
| 第4层 | 滑动轴承最终规格 | ? | D，后续回代 |

---

# 2. 第1步：流道

当前全通径控制值：

```text
d = 303 mm
```

尺寸向外传递：

```text
φ303流道
  ↓
球体流道
  ↓
阀座内部通径
  ↓
端盖 / 阀体流道
```

---

# 3. 第2步：球体V1几何骨架

## 3.1 已知基础尺寸

```text
球体外径 Db = 465 mm
球体半径 Rb = Db / 2 = 232.5 mm
流道直径 d = 303 mm
流道半径 r = d / 2 = 151.5 mm
```

球面到流道外缘之间的基础径向几何空间：

```text
tg = (Db - d) / 2
   = (465 - 303) / 2
   = 81 mm
```

注意：

```text
81 mm ≠ 最终球体净壁厚
```

因为后续还会布置上驱动槽、下支承孔、圆角和局部承力区。

---

## 3.2 球体总宽 Wball：V1先取348 mm

球体沿流道方向两端需要削平形成流道口，因此球体总宽不会直接等于球体外径465 mm。

V1先取：

```text
Wball = 348 mm
```

球心到单侧端面的距离：

```text
x = Wball / 2
  = 348 / 2
  = 174 mm
```

球面在该端面位置的截面半径：

```text
Rend = √(Rb² - x²)
```

代入：

```text
Rend = √(232.5² - 174²)
     ≈ 154.21 mm
```

所以球体端面外径：

```text
Dend = 2 × Rend
     ≈ 308.42 mm
```

而流道直径：

```text
d = 303 mm
```

因此端面流道边缘到球体端面外缘的径向剩余实体：

```text
tedge = (Dend - d) / 2
```

代入：

```text
tedge = (308.42 - 303) / 2
      ≈ 2.71 mm
```

所以：

> **Wball=348 mm时，φ303流道口附近仍有约2.71 mm的几何边缘实体。**

反算关系也可写成：

```text
Wball = 2 × √[Rb² - (r + tedge)²]
```

如果第一轮仅作几何敏感性，取端口边缘实体：

```text
tedge = 2.5 mm → Wball ≈ 348.37 mm

tedge = 3.0 mm → Wball ≈ 347.48 mm
```

因此工程草模阶段圆整：

```text
Wball = 348 mm
```

状态：`C 设计候选值`。

> **重要：2.5～3 mm只是本轮几何敏感性假设，不是规范规定的最终允许最小实体厚度。正式制造前仍需结合球体局部强度、加工圆角及实际结构复核。**

---

## 3.3 上驱动接口：V1 = 50×70×35 mm

当前最大输入扭矩暂按：

```text
T = 1800 N·m
  = 1,800,000 N·mm
```

V1候选：

```text
驱动宽度 B = 50 mm
接触长度 L = 70 mm
插入深度 H = 35 mm
```

即：

```text
B × L × H = 50 × 70 × 35 mm
```

第一轮简化有效力臂：

```text
reff = B / 2
     = 50 / 2
     = 25 mm
```

扭矩对应的等效切向力：

```text
Ft = T / reff
```

代入：

```text
Ft = 1,800,000 / 25
   = 72,000 N
   = 72 kN
```

简化接触面积：

```text
Acontact = L × H
         = 70 × 35
         = 2450 mm²
```

第一轮平均接触应力：

```text
pavg = Ft / Acontact
```

代入：

```text
pavg = 72,000 / 2450
     ≈ 29.4 MPa
```

槽底到φ303流道之间的第一轮理论剩余实体：

```text
tr = tg - H
   = 81 - 35
   = 46 mm
```

因此当前V1：

```text
上驱动接口 = 50×70×35 mm
1800 N·m时等效切向力 ≈ 72 kN
第一轮平均接触应力 ≈ 29.4 MPa
槽底理论剩余实体 ≈ 46 mm
```

状态：`C 设计候选值`。

正式冻结前还需复核：

```text
F316球体槽壁局部挤压
槽根剪切 / 圆角应力集中
F51驱动头本体及根部
实际装配间隙 / 公差
1800 N·m最终定义
```

---

## 3.4 下支承接口：V1 = φ70×40 mm深，R4

V1候选：

```text
下支承孔名义直径 Dlower = 70 mm
下支承孔深度 Hlower = 40 mm
孔底圆角 R = 4 mm
```

先做球体几何剩余实体检查：

```text
tlower = tg - Hlower
```

代入：

```text
tlower = 81 - 40
       = 41 mm
```

所以第一轮几何上：

```text
φ70×40深
→ 孔底到φ303流道理论剩余实体约41 mm
```

再做第一轮简化承压敏感性。

当前若仍暂按上下支承对称：

```text
Rl ≈ 72.11 kN
   ≈ 72,107 N
```

把球体下孔与插入段的简化投影承压面积近似为：

```text
Ab = Dlower × Hlower
```

代入：

```text
Ab = 70 × 40
   = 2800 mm²
```

平均承压敏感性：

```text
pb = Rl / Ab
```

代入：

```text
pb = 72,107 / 2800
   ≈ 25.8 MPa
```

如果做极端敏感性，假设第一轮144.2 kN全部作用到下支承：

```text
pb,max = 144,213 / 2800
       ≈ 51.5 MPa
```

因此目前：

```text
下支承接口 = φ70×40 mm深，R4
孔底理论剩余实体 ≈ 41 mm
对称72.1 kN假设下简化平均承压 ≈ 25.8 MPa
```

状态：`C 设计候选值`。

> **注意：D×H只是第一轮简化投影承压面积，真实下支承局部接触压力并不会完全均匀分布。最终必须在第4～5步得到真实下支承结构、a/b/e和配合后重新校核。**

R4的作用：

- 避免盲孔底部尖锐90°转角；
- 降低局部应力集中；
- 给后续加工和有限元提供更真实几何。

R4当前同样属于 `C V1候选`，不是标准强制值。

---

## 3.5 第2层当前结论

第2层球体目前已经可以形成一个完整V1几何骨架：

```text
球体外径 Db = φ465 mm
流道 d = φ303 mm
球体总宽 Wball = 348 mm

上驱动接口 = 50×70×35 mm

下支承接口 = φ70×40 mm深，R4
```

当前状态：

| 球体指标 | V1当前值 | 状态 |
|---|---:|---|
| 球体外径 | φ465 mm | C |
| 流道 | φ303 mm | A |
| 球体总宽 | **348 mm** | C |
| 上驱动接口 | **50×70×35 mm** | C |
| 下支承接口 | **φ70×40 mm深，R4** | C |

> **这意味着第2步不再是一堆问号，而是已经形成“可以继续向第3层阀座生长”的球体V1。后续若阀座、支承空间或局部强度发生冲突，再回到第2步修正。**

---

# 4. 第3步：阀座密封副——当前工作重点

## 4.1 已知输入

```text
球体外径 Db = 465 mm
球体半径 Rb = 232.5 mm
流道 d = 303 mm
密封角 α = 45°
实际斜面密封带宽 w = 4～6 mm
```

本章采用：

> `α = 45°` 表示阀座密封斜面与流道轴线的夹角。

45°时：

```text
sin45° = cos45° ≈ 0.7071
```

---

## 4.2 区分实际斜面密封带与径向投影宽度

定义：

```text
w  = 实际45°斜面密封带宽，mm
br = 密封带径向投影宽度，mm
D1 = 密封带外缘直径，mm
D2 = 密封带内缘直径，mm
Dc = 密封接触中心直径，mm
```

关系：

```text
br = w × cos45°
```

并且：

```text
br = (D1 - D2) / 2
```

因此以前直接由 `(D1-D2)/2` 得到的是径向投影宽度，不是沿45°斜面量的4～6 mm实际密封带宽。

---

## 4.3 密封接触中心直径 Dc

45°密封接触中心：

```text
Dc = Db × cos45°
```

代入：

```text
Dc = 465 × 0.7071
   ≈ 328.80 mm
```

所以：

```text
Dc ≈ φ328.80 mm
```

状态：`B 公式计算值`。

---

## 4.4 由w计算D1、D2

公式链：

```text
br = w × cos45°

D1 = Dc + br

D2 = Dc - br
```

比较4 / 5 / 6 mm：

| 实际45°斜面密封带宽 w | 径向投影 br | 密封内径 D2 | 密封外径 D1 |
|---:|---:|---:|---:|
| 4 mm | 2.83 mm | φ325.98 | φ331.63 |
| **5 mm** | **3.54 mm** | **φ325.27** | **φ332.34** |
| 6 mm | 4.24 mm | φ324.56 | φ333.05 |

当前V1取中值：

```text
w = 5 mm
br ≈ 3.54 mm
D2 ≈ φ325.27 mm
D1 ≈ φ332.34 mm
```

---

## 4.5 V1密封投影面积 Aproj

第一轮环形投影面积：

```text
Aproj = π × (D1² - D2²) / 4
```

代入：

```text
D1 = 332.34 mm
D2 = 325.27 mm
```

得到：

```text
Aproj ≈ 3652 mm²
```

4～6 mm敏感性：

| w | Aproj |
|---:|---:|
| 4 mm | ≈2922 mm² |
| **5 mm** | **≈3652 mm²** |
| 6 mm | ≈4383 mm² |

---

## 4.6 用历史q=2.5 MPa做预紧力敏感性

历史参考：

```text
q = 2.5 MPa = 2.5 N/mm²
```

当前仅作为 `H 历史参考比压`，不是DEVLON最终批准比压。

公式：

```text
Fpre = q × Aproj
```

V1：

```text
Fpre = 2.5 × 3652
     ≈ 9130 N
     ≈ 9.13 kN / 每侧阀座
```

---

## 4.7 若暂按36根弹簧

历史敏感性假设：

```text
Ns = 36
```

单根弹簧第一轮预紧力：

```text
Fspring = Fpre / Ns
```

代入：

```text
Fspring = 9130 / 36
        ≈ 253.6 N
        ≈ 254 N / 根
```

4～6 mm敏感性：

| w | 每侧总预紧力 | 若36根，单根弹簧力 |
|---:|---:|---:|
| 4 mm | ≈7.30 kN | ≈203 N/根 |
| **5 mm** | **≈9.13 kN** | **≈254 N/根** |
| 6 mm | ≈10.96 kN | ≈304 N/根 |

注意：36根和254 N/根都不是最终选型值。正式弹簧设计还要叠加介质压力、背压面积、O形圈摩擦、阀座滑动摩擦、安装压缩量、轴向浮动量和温度影响。

---

## 4.8 对旧“333/303=15 mm密封带”的纠正

旧计算曾把：

```text
D1 = 333 mm
D2 = 303 mm
```

直接算成：

```text
(333 - 303) / 2 = 15 mm
```

现在应明确：

```text
φ303 = 流道 / 阀座内部通径控制尺寸
```

当前实际球面密封区域V1约为：

```text
D2 ≈ φ325.27 mm
D1 ≈ φ332.34 mm
```

所以φ303不能再直接当成实际密封带内径。

---

## 4.9 第3步当前结果

```text
【A 已确认】
密封角 α = 45°
实际斜面密封带 w = 4～6 mm

【C V1候选】
w = 5 mm

【B / 基于V1计算】
Dc ≈ φ328.80 mm
br ≈ 3.54 mm
D2 ≈ φ325.27 mm
D1 ≈ φ332.34 mm
Aproj ≈ 3652 mm²

【H 敏感性假设】
q = 2.5 MPa
Ns = 36根 / 每侧

【基于H假设】
Fpre ≈ 9.13 kN / 每侧
Fspring ≈ 254 N / 根
```

---

# 5. 下一小步：DEVLON截面 + 金属阀座圈

现在第2层球体V1已经补齐，第3层下一步继续从密封区域向外生长：

```text
φ303流道
   ↓
φ325～φ332附近实际密封区
   ↓
DEVLON厚度 / 嵌入深度 / 截面形式
   ↓
金属阀座圈内径
   ↓
金属阀座圈外径 D3
   ↓
阀座总成轴向宽度 Wseat
   ↓
有效受压直径 Deff
   ↓
弹簧 + O形圈 + 浮动量
```

下一步要输出：

```text
DEVLON截面 = ?
D3 = 金属阀座圈 / 支承圈外径
D4 = 阀座内部有效通径
Deff = 有效受压直径
Wseat = 阀座总成轴向宽度
xseat = 阀座轴向浮动量
```

---

# 6. 已建立的其他基础计算

## 6.1 φ60阀杆纯扭转

```text
τ = 16 × T / (π × d³)
```

结果：

```text
1500 N·m → τ ≈ 35.4 MPa
1800 N·m → τ ≈ 42.4 MPa
```

φ60继续作为候选，最终还要叠加弯矩、最小净截面、驱动头和温度材料准则。

## 6.2 球体压力推力第一轮基线

```text
Ap = π × 303² / 4
   ≈ 72,107 mm²
```

暂按：

```text
ΔP = 2 MPa
```

得到：

```text
F = ΔP × Ap
  ≈ 144.2 kN
```

等阀座 `Deff` 确定后必须重新计算。

---

# 7. 历史试算参考

| 历史试算 | 曾用值 | 当前处理 |
|---|---:|---|
| 对称支承 | Ru=Rl≈72.1 kN | H，等a/b后重算 |
| 假设悬臂 | e=50 mm | H，真实结构后重算 |
| 下轴V0 | φ60 | H |
| 下轴V1 | φ65 | H |
| 轴承V1 | φ70×φ65×50 | H |

> 上驱动槽50×70×35和球体下孔φ70×40/R4已经从历史试算升级为当前第2层V1候选，但后续仍需回代。

---

# 8. 公式速查

```text
Rb = Db / 2

tg = (Db - d) / 2

Rend = √[Rb² - (Wball/2)²]

Dend = 2 × Rend

tedge = (Dend - d) / 2

Wball = 2 × √[Rb² - (r + tedge)²]

reff = B / 2

Ft = T / reff

pavg = Ft / (L × H)

tl = tg - Hlower

Ab = Dlower × Hlower

pb = R / Ab

Dc = Db × cos45°

br = w × cos45°

D1 = Dc + br

D2 = Dc - br

Aproj = π × (D1² - D2²) / 4

Fpre = q × Aproj

Fspring = Fpre / Ns

F = ΔP × A

τ = 16 × T / (π × d³)

σb = 32 × M / (π × d³)

p = R / (d × L)
```

---

# 9. 当前结论

现在内部设计主线已经推进为：

```text
第1层 流道 φ303
        ↓
第2层 球体V1
φ465 × 总宽348
上驱动50×70×35
下支承孔φ70×40深、R4
        ↓
第3层 阀座密封副
45° + 实际密封带4～6 mm
V1取5 mm
Dc≈φ328.80
D2≈φ325.27
D1≈φ332.34
        ↓
下一步：DEVLON截面 + 金属阀座圈
```

**文件状态：从内到外主线 / 第2步球体V1骨架已补齐完整公式 / 第3步继续设计阀座密封副。**