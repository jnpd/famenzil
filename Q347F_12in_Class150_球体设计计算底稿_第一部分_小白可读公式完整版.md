# Q347F 12寸 Class 150 天然气固定球阀——从内到外设计计算主线

> **设计对象**：Q347F 固定球阀，NPS 12 / DN300，Class 150，天然气，全通径，非金属软密封。  
> **设计方法**：总体工况先锁定，机械结构尺寸按 **从内到外** 生长；每向外增加一层，都回做空间、强度、密封和装配校核。  
> **维护方式**：后续持续更新本文件，不再另外拆分新的计算主线文档。  
> **本次重要更新**：已用公司文件 `GFE-JS01-2025《BS系列固定球阀设计规范》` 重新校正第3步阀座计算顺序，统一采用公司 `D9 / D10 / D11` 术语，并取消没有规范依据的“DEVLON厚12、嵌入8、D3≈362、Wseat≈35”等猜测尺寸。  
> **当前进度**：第1步流道已锁；第2步球体V1骨架已形成；第3步已完成密封区第一轮几何和弹簧预紧力人工复算，下一步是按公司规范求 `D11 阀座静压面外径`。

---

# 0. 整台阀门的唯一设计主线

公司规范给出的设计顺序是：

```text
基本参数
→ 球体
→ 阀座
→ 阀杆
→ 阀体
→ 阀盖
→ 压盖
→ 支架
```

本项目在这个顺序基础上细化为：

```text
第0步  工况 / 边界条件
DN / Class / 压力 / 温度 / 介质 / 材料 / 扭矩
          ↓
第1步  流道
φ303
          ↓
第2步  球体
球径 / 流道 / 球宽 / 上驱动接口 / 下支承接口
          ↓
第3步  阀座密封副   ← 当前做到这里
D9 / D10 / D11 + DEVLON + 弹簧 + O形圈 + 防火面
          ↓
第4步  上下支承系统
阀杆 + 下支承轴 + 滑动轴承
          ↓
第5步  内部整体空间
球体 + 阀座 + 支承 + 旋转 / 移动空间
          ↓
第6步  阀体内腔
座腔 / 中腔 / 上下支承腔
          ↓
第7步  阀体外壁
承压壁厚 + 局部加强 + 铸造 / 加工余量
          ↓
第8步  左右端盖 / 阀盖
          ↓
第9步  底盖 / 上盖 / 填料函
          ↓
第10步 标准法兰
NPS 12 / Class 150 / RF
          ↓
第11步 紧固与密封附件
          ↓
第12步 连接盘 / 蜗轮箱
          ↓
第13步 总装尺寸链
          ↓
第14步 最终校核
压力 / 密封 / 扭矩 / 强度 / 温度 / FEA
```

## 0.1 参数状态规则

| 状态 | 含义 |
|---|---|
| **A 已确认输入/公司明确规定** | 当前设计基线，或公司规范直接给出的规则/数值 |
| **B 公式计算值** | 由当前输入按明确公式得到，输入变化必须重算 |
| **C 设计候选值** | 已有第一轮依据，可以继续往外设计，但尚未冻结制造 |
| **D 待确定** | 前置条件或公司计算程序缺失，明确写 `?` |
| **H 历史/敏感性假设** | 以前用过或仅用于敏感性分析，不作为当前冻结依据 |
| **R 风险/冲突** | 当前输入与公司规范、材料温度范围或其他边界存在冲突，必须闭合后才能冻结 |

> 核心规则：**规范没给、公式算不出、结构没确定的尺寸，不硬猜。**

## 0.2 本文件的依据优先级

本项目以后统一按下面优先级判断“这个数能不能用”：

```text
客户/项目正式输入
    ↓
公司受控设计规范 GFE-JS01-2025
    ↓
API / ASME / GB 等正式标准
    ↓
公司固定球阀计算程序 / 历史受控计算书
    ↓
公式人工复算
    ↓
成熟结构参考
    ↓
旧三维模型 / 旧BOM仅用于交叉检查
```

特别说明：

- `GFE-JS01-2025` 当前上传件页眉显示版本 `2025-A/0`；修改记录中还出现了后续 `A/01` 变更记录。制造冻结前要确认公司最终受控版本和变更单是否全部生效。
- 本文件是“计算主线”，不是最终制造图。

---

# 1. 当前总状态

| 层级 | 参数 | 当前值 | 状态/来源 |
|---|---|---:|---|
| 工况 | 公称尺寸 | NPS 12 / DN300 | A，项目输入 |
| 工况 | 压力等级 | Class 150 | A，项目输入 |
| 工况 | 介质 | 天然气 | A，项目输入 |
| 工况 | 结构 | 固定球 | A，项目输入 |
| 工况 | 流道形式 | 全通径 | A，项目输入 |
| 工况 | 设计压力 | 2 MPa | A，项目输入 |
| 工况 | 最大关闭压差 | 暂按2 MPa | C，待项目工况表最终确认 |
| 材料 | 阀体 | ASTM A216 WCB | A，项目输入 |
| 材料 | 球体 | ASTM A182 F316 | A，项目输入 |
| 材料 | 阀杆 | ASTM A182 F51 | A，项目输入 |
| 材料 | 阀座密封材料 | DEVLON | A项目输入；温度适用性另见R风险 |
| 材料 | O形圈 | VITON | A项目输入；具体牌号/硬度待补 |
| 第1层 | 流道 | φ303 mm | A，当前全通径控制值 |
| 第2层 | 球体外径 | **φ465 mm** | C，公司规范第8页：F316、12"、≤CL300优选值 |
| 第2层 | 球体半径 | 232.5 mm | B |
| 第2层 | 基础径向几何空间 | 81 mm | B |
| 第2层 | 球体总宽 | 348 mm | C，V1候选，非公司查表值 |
| 第2层 | 上驱动接口 | 50×70×35 mm | C，V1候选 |
| 第2层 | 下支承接口 | φ70×40 mm深，R4 | C，V1候选 |
| 第3层 | 密封角 | 45° | A，当前项目密封几何输入 |
| 第3层 | 实际斜面密封带宽 w | 4～6 mm | A，当前项目设计输入；**不要与公司K4混为一谈** |
| 第3层 | V1斜面密封带宽 | 5 mm | C，中值试算 |
| 第3层 | 密封接触中心直径 Dc | φ328.80 mm | B |
| 第3层 | 公司术语 D9：密封面内径 | φ325.27 mm | B/C，w=5时V1 |
| 第3层 | 公司术语 D10：密封面外径 | φ332.34 mm | B/C，w=5时V1 |
| 第3层 | 密封带轴向投影面积 Aproj | 约3652 mm² | B/C |
| 第3层 | 预紧比压 qpre | **2.5 MPa** | A，公司规范第15页明确规定 |
| 第3层 | 每侧总弹簧预紧力 Fpre | 约9.13 kN | B/C，按当前人工复算口径 |
| 第3层 | 公司术语 D11：阀座静压面外径 | ? | D，必须比压 + 公司固定球阀计算程序 |
| 第3层 | O形圈槽K2 | ? | D，先定O形圈截面直径再查公司表 |
| 第3层 | 阀座大端外圆 | ? | D，D11和阀盖内孔确定后再定 |
| 第3层 | Deff等效有效受压直径 | ? | D，压力边界确定后派生，不替代D11 |
| 第4层 | 阀杆有效直径 | φ60 mm | C |
| 第4层 | 下支承轴最终尺寸 | ? | D，后续回代 |
| 第4层 | 滑动轴承最终规格 | ? | D，后续回代 |

---

# 2. 第1步：流道

当前全通径控制值：

```text
d = φ303 mm
```

公司规范要求通道直径来源于 API 6D 表1，并说明 API 6D 的通道直径是最小值；正式制造尺寸还要按公司要求处理公差。

本项目尺寸传递链：

```text
φ303流道
  ↓
球体通道
  ↓
阀座内部通径
  ↓
端盖 / 阀体流道
```

> φ303 是“流道控制尺寸”，**不是阀座球面密封带内径**。

---

# 3. 第2步：球体V1几何骨架

## 3.1 球体外径为什么是 φ465

公司规范 `GFE-JS01-2025` 第8页对软密封、低碳奥氏体球体给出优选球径表：

```text
球体材料：F304/F304L/F316/F316L/F321 等
尺寸：12"
压力等级：≤CL300
优选球体直径 Sφ = 465 mm
```

本项目：

```text
NPS 12
Class 150
球体 ASTM A182 F316
软密封
```

因此第一轮采用：

```text
Db = φ465 mm
```

这里的 `φ465` 已经有公司规范依据，不再属于“旧模型测量值”。

状态：`C 公司规范优选 + 后续结构校核`。

## 3.2 基础几何

```text
Db = 465 mm
Rb = Db / 2 = 232.5 mm

d = 303 mm
r = d / 2 = 151.5 mm
```

球体外圆到流道外缘之间的基础径向几何空间：

```text
tg = (Db - d) / 2
   = (465 - 303) / 2
   = 81 mm
```

注意：

```text
81 mm ≠ 最终球体净壁厚
```

因为还要布置上驱动槽、下支承孔、圆角和局部承力区。

## 3.3 球体总宽 Wball：当前V1 = 348 mm

当前V1：

```text
Wball = 348 mm
```

球心到单侧端面的距离：

```text
x = 348 / 2 = 174 mm
```

球面在端面位置的截面半径：

```text
Rend = √(Rb² - x²)
     = √(232.5² - 174²)
     ≈ 154.21 mm
```

端面外径：

```text
Dend = 2 × Rend
     ≈ 308.42 mm
```

端面流道边缘到球体端面外缘的径向剩余实体：

```text
tedge = (Dend - d) / 2
      = (308.42 - 303) / 2
      ≈ 2.71 mm
```

所以：

> `Wball=348 mm` 在几何上能够留下约 `2.71 mm` 的端口边缘实体，但这个 `2.71 mm` 不是公司规范规定的最小强度值。

状态：`C V1候选`。

## 3.4 上驱动接口：当前V1 = 50×70×35 mm

最大输入扭矩暂按：

```text
T = 1800 N·m
  = 1,800,000 N·mm
```

V1：

```text
B = 50 mm
L = 70 mm
H = 35 mm
```

第一轮简化有效力臂：

```text
reff = B / 2 = 25 mm
```

切向力：

```text
Ft = T / reff
   = 1,800,000 / 25
   = 72,000 N
   = 72 kN
```

简化接触面积：

```text
Acontact = L × H
         = 70 × 35
         = 2450 mm²
```

平均接触应力敏感性：

```text
pavg = Ft / Acontact
     ≈ 29.4 MPa
```

槽底到φ303流道的理论剩余实体：

```text
tr = tg - H
   = 81 - 35
   = 46 mm
```

状态：`C V1候选`。正式冻结仍需局部挤压、剪切、圆角应力集中和实际配合校核。

## 3.5 下支承接口：当前V1 = φ70×40 mm深，R4

```text
Dlower = 70 mm
Hlower = 40 mm
R = 4 mm
```

孔底到φ303流道理论剩余实体：

```text
tlower = 81 - 40 = 41 mm
```

若第一轮仍用 `72.11 kN` 作为对称支承敏感性载荷：

```text
Ab = Dlower × Hlower
   = 70 × 40
   = 2800 mm²

pb = 72,107 / 2800
   ≈ 25.8 MPa
```

这只是简化投影承压敏感性，不是最终接触应力。

---

# 4. 第3步：阀座密封副——按公司规范重排后的正式主线

## 4.1 先把公司规范说了什么写清楚

`GFE-JS01-2025` 第10～16页与本项目阀座直接相关的规则如下：

1. **阀座材料表**：DEVLON 适用压力 `150LB～1500LB`，公司给出的适用温度 `-29～120℃`，摩擦系数 `0.08`。
2. **阀座尺寸顺序**：先按球体结构初步确定密封面内外径 `D9、D10`。
3. **D11来源**：通过“必须比压”计算确定阀座静压面外径 `D11`。
4. **最终尺寸**：再通过“固定球阀计算程序”确定最终尺寸。
5. **结构间距**：`K1=2～3`，`K2=O形圈槽宽度`，`K3=3～5`，`K4=4～6`。
6. **防火结构**：正常工况下金属防火面与球体留 `0.8～1 mm` 间隙。
7. **阀座大端外圆**：NPS≥6" 时，阀座大端外圆名义尺寸比阀盖内孔小 `2 mm`，并按规范给定公差。
8. **预紧弹簧**：NPS≥2" 固定球阀采用圆柱弹簧预紧，**预紧比压规定为 2.5 MPa**。
9. **O形圈**：尺寸查 GB/T 3452.1，公司规范第16页另给沟槽深度、槽宽、圆角等设计值。

这意味着公司真正的阀座设计主线不是：

```text
猜DEVLON厚度
→ 猜金属圈外径
→ 猜Deff
```

而应该是：

```text
球体结构
↓
D9 / D10 密封面内外径
↓
密封面积 / 密封力
↓
必须比压计算
↓
D11 阀座静压面外径
↓
O形圈压力边界 + K1/K2/K3/K4
↓
固定球阀计算程序复核
↓
最终阀座大端、轴向宽度、弹簧、浮动量
```

---

## 4.2 统一公司术语：D9、D10、D11

从现在开始，本文件不再把阀座关键直径只写成自定义 `D1/D2/D3`。

统一为公司规范术语：

```text
D9  = 密封面内径
D10 = 密封面外径
D11 = 阀座静压面外径
```

为了保留前面的计算关系，可做如下映射：

```text
旧文档 D2 → 公司 D9
旧文档 D1 → 公司 D10
```

原来准备使用的 `D3=金属阀座圈外径` 暂时停用，因为公司规范真正先要求求的是 `D11 静压面外径`。

> **重要：D11不是“金属圈随便往外加几毫米”的结果，而是必须比压 + 阀座活塞受力计算的结果。**

---

## 4.3 当前密封几何输入

当前项目采用：

```text
球体外径 Db = 465 mm
球体半径 Rb = 232.5 mm
流道 d = 303 mm
密封斜面角 α = 45°
实际斜面密封带宽 w = 4～6 mm
```

其中：

- `α=45°`、`w=4～6 mm` 是当前项目设计输入。
- 公司规范第11页的 `K4=4～6 mm` 是 fig 5 中另一项结构间距，**不能因为数值一样就把 K4 当成密封带宽 w**。

---

## 4.4 密封接触中心直径 Dc 为什么是 φ328.80

这是球面与45°密封斜面的几何关系，不是经验值。

球体半径：

```text
Rb = 465 / 2
   = 232.5 mm
```

45°时接触圆半径：

```text
Rc = Rb × cos45°
```

所以密封接触中心直径：

```text
Dc = 2 × Rc
   = Db × cos45°
```

代入：

```text
Dc = 465 × 0.7071
   ≈ 328.80 mm
```

得到：

```text
Dc ≈ φ328.80 mm
```

状态：`B 公式计算值`。

前提：当前结构确实采用45°锥形密封面与φ465球面形成接触。如果正式阀座剖面不是这个几何关系，则必须重算。

---

## 4.5 由实际斜面带宽 w 求公司 D9 / D10

定义：

```text
w  = 沿45°斜面量取的实际密封带宽
br = 该密封带在径向方向的总投影宽度
```

45°时：

```text
br = w × cos45°
```

由于 `D9/D10` 是直径尺寸，若密封带以接触中心近似对称展开：

```text
D10 = Dc + br
D9  = Dc - br
```

### w = 4 mm

```text
br = 4 × 0.7071
   ≈ 2.83 mm

D9  ≈ 328.80 - 2.83 = φ325.98
D10 ≈ 328.80 + 2.83 = φ331.63
```

### w = 5 mm（当前V1）

```text
br = 5 × 0.7071
   ≈ 3.54 mm

D9  ≈ 328.80 - 3.54 = φ325.27
D10 ≈ 328.80 + 3.54 = φ332.34
```

### w = 6 mm

```text
br = 6 × 0.7071
   ≈ 4.24 mm

D9  ≈ 328.80 - 4.24 = φ324.56
D10 ≈ 328.80 + 4.24 = φ333.05
```

汇总：

| 实际斜面密封带 w | 径向投影 br | D9 密封面内径 | D10 密封面外径 |
|---:|---:|---:|---:|
| 4 mm | 2.83 mm | φ325.98 | φ331.63 |
| **5 mm** | **3.54 mm** | **φ325.27** | **φ332.34** |
| 6 mm | 4.24 mm | φ324.56 | φ333.05 |

当前V1：

```text
D9  ≈ φ325.27 mm
D10 ≈ φ332.34 mm
```

状态：`B/C`。几何公式成立，但最终仍要经公司固定球阀计算程序和真实阀座剖面回代。

---

## 4.6 为什么 φ303 不能再当 D9

旧试算曾写：

```text
333 / 303
```

并把：

```text
(333 - 303) / 2 = 15 mm
```

理解成密封带。

现在必须分开：

```text
φ303 = 流道控制尺寸

D9 ≈ φ325.27 = 当前V1密封面内径
D10≈ φ332.34 = 当前V1密封面外径
```

所以 `φ303` 不能直接当作球面密封带内缘。

---

## 4.7 密封带轴向投影面积 Aproj

当前人工复算采用轴向投影面积：

```text
Aproj = π × (D10² - D9²) / 4
```

V1代入：

```text
D9  = 325.27 mm
D10 = 332.34 mm
```

得到：

```text
Aproj ≈ 3652 mm²
```

敏感性：

| w | Aproj |
|---:|---:|
| 4 mm | ≈2922 mm² |
| **5 mm** | **≈3652 mm²** |
| 6 mm | ≈4383 mm² |

为什么用投影面积：若 `q` 表示密封面的法向平均接触比压，则斜面法向力换算到阀座轴向时，可以等价写成 `q × 轴向投影面积`。

> 这属于当前人工复算口径。公司固定球阀计算程序若采用不同面积定义，以公司程序为最终控制口径。

---

## 4.8 公司明确规定：预紧比压 qpre = 2.5 MPa

这一点现在已经不是“历史猜测”。

`GFE-JS01-2025` 第15页明确规定：

```text
≥2" 固定球阀
→ 用圆柱弹簧预紧
→ 预紧比压规定为 2.5 MPa
```

因此本项目：

```text
qpre = 2.5 MPa
     = 2.5 N/mm²
```

状态：`A 公司规范明确值`。

### 按当前人工复算口径求每侧总预紧力

```text
Fpre = qpre × Aproj
```

V1：

```text
Fpre = 2.5 × 3652
     ≈ 9130 N
     ≈ 9.13 kN / 每侧阀座
```

敏感性：

| w | Aproj | qpre=2.5 MPa时每侧Fpre |
|---:|---:|---:|
| 4 mm | ≈2922 mm² | ≈7.30 kN |
| **5 mm** | **≈3652 mm²** | **≈9.13 kN** |
| 6 mm | ≈4383 mm² | ≈10.96 kN |

> **关键区别：2.5 MPa是公司规定的“预紧比压”，不是第11页所说的“必须比压”的同义词。两者不能自动画等号。**

---

## 4.9 36根弹簧仍然只保留为历史敏感性

如果暂按历史结构：

```text
Ns = 36根 / 每侧
```

则：

```text
Fspring = Fpre / Ns
        = 9130 / 36
        ≈ 254 N / 根
```

但是公司规范第15～16页同时说明：

- 新设计圆柱弹簧应通过计算程序计算；
- 原则上减小弹簧直径、增大节距、降低弹簧高度；
- 与介质接触的弹簧材料有 INCONEL X-750 / X-718 等公司规定。

所以：

```text
36根 = H 历史结构敏感性
254 N/根 = H 基于36根得到的敏感性结果
```

不能直接作为最终BOM。

---

## 4.10 下一关键尺寸是 D11，不是先猜 DEVLON 厚度

公司规范第11页原始设计逻辑：

```text
① 根据球体结构尺寸初步确定 D9、D10
② 通过必须比压计算确定 D11
③ 通过固定球阀计算程序确定最终尺寸
```

所以我们现在真正应该求：

```text
D11 = 阀座静压面外径
```

### 为什么现在还不能给D11数字

因为当前这份设计规范只告诉我们：

```text
D11 要通过“必须比压”计算
```

但规范正文**没有给出**：

```text
必须比压的具体公式
DEVLON必须比压在本工况下的取值
D11完整力平衡公式
公司固定球阀计算程序的输入/输出定义
```

因此当前正确状态是：

```text
D11 = ?
状态：D 待确定
```

这不是“不会算”，而是缺公司受控计算口径。

### 下一步必须找的资料

优先找以下任一项：

```text
《固定球阀计算程序》
或
旧版受控固定球阀计算书
或
公司D9/D10/D11计算Excel
或
一份带D9/D10/D11标注的成熟阀座零件图 + 配套计算书
```

拿到以后再把公式完整还原到本文件。

---

## 4.11 D11 与 Deff 不是一回事

以前文档准备直接写：

```text
D3 → Deff
```

这会把结构尺寸和液压有效面积混在一起。

现在统一：

```text
D11 = 公司规范定义的阀座静压面外径
```

而 `Deff` 如果后续继续使用，只作为人工计算中的“等效有效受压直径”，必须等下面这些边界先确定：

```text
D11
+
O形圈真正的密封直径
+
阀座压力从哪一侧进入
+
SPE / DPE结构
+
中腔自泄压方向
```

再先求：

```text
Aeff = 实际压力作用面积差
```

然后才能定义：

```text
Deff = √(4 × Aeff / π)
```

因此：

> **Deff是派生量，不是公司规范要求先查表或直接加工的直径；不能拿Deff替代D11。**

---

## 4.12 K1 / K2 / K3 / K4 怎么用

公司规范第11页规定：

```text
K1 = 2～3 mm
K2 = O形圈槽宽度
K3 = 3～5 mm
K4 = 4～6 mm
```

当前要注意两点：

### 第一：K2不能先拍脑袋

必须先定O形圈截面直径 `d1`，再查公司第16页沟槽表。

例如，如果后续阀座活塞O形圈最终选择：

```text
d1 = 5.3 mm
```

公司表对应：

```text
静密封槽深 = 4.2 mm
动密封槽深 = 4.4 mm
槽宽 b = 7 mm
最小导角长度 z = 4 mm
槽底圆角 r1 = 0.6 mm
槽口圆角 r2 = 0.2 mm
```

则：

```text
K2 = 7 mm
```

但阀座本身会轴向浮动，实际应按“动密封还是静密封”由真实O形圈安装位置确认，不能在没有剖面时直接冻结槽深。

### 第二：K4=4～6 ≠ 密封带宽w=4～6

两者只是数值碰巧相同：

```text
w  = 当前项目定义的45°实际斜面密封带宽
K4 = 公司fig 5中的阀座结构间距
```

必须分开管理。

---

## 4.13 DEVLON：压力等级符合，但温度范围必须单独闭合

公司规范第10页给出的阀座材料表：

```text
DEVLON
适用压力：150LB～1500LB
适用温度：-29～120℃
摩擦系数：0.08
```

所以对于本项目 `Class150`：

```text
压力等级方面：符合公司推荐适用范围
```

但是，如果本项目最终设计温度仍要求覆盖此前资料中的：

```text
-46～200℃
```

那么会出现直接冲突：

```text
公司DEVLON范围：-29～120℃
项目温度范围： -46～200℃
```

此时必须：

```text
方案A：取得具体DEVLON牌号厂家书面批准
或
方案B：按项目温度重新选择阀座材料
或
方案C：项目确认实际压力-温度工况不是全范围同时适用
```

在这个问题闭合前：

```text
DEVLON = 项目指定材料
但温度适用性状态 = R 风险/待确认
```

### DEVLON厚度为什么现在仍然是问号

公司规范没有给出：

```text
12" Class150 → DEVLON厚度固定多少
DEVLON嵌入深度固定多少
```

因此：

```text
DEVLON总厚度 = ?
嵌入深度 = ?
外露工作层 = ?
```

目前全部保持 `D 待确定`。

这些尺寸应在 `D11 + O形圈 + 防火面 + 嵌件固定结构 + 材料抗挤出/蠕变` 明确后一起设计。

---

## 4.14 防火面还有一个公司明确尺寸

公司规范第11页规定：

正常工作时：

```text
金属防火面与球体间隙 = 0.8～1 mm
```

正常情况下由软密封环与球体密封；失火、软密封被烧毁后，阀座在介质压力作用下向球体移动，使金属防火面接触球体，形成临时密封。

所以将来设计阀座轴向截面时，至少同时满足：

```text
软密封工作接触
+
金属防火面0.8～1 mm正常间隙
+
阀座轴向浮动量
+
弹簧压缩行程
+
O形圈有效密封行程
```

这也是为什么不能现在就随便定 `Wseat`。

---

## 4.15 阀座大端外圆也不是独立随便定

公司规范规定：

```text
NPS ≥ 6"
阀座大端外圆名义尺寸
= 阀盖内孔名义尺寸 - 2 mm
```

并规定：

```text
尺寸＜400：公差 (0, -0.1)
尺寸＞400：公差 (0, -0.2)
```

因此阀座最终大端外圆还会和“阀盖内孔/座腔”形成装配尺寸链。

这进一步说明：

> 旧的 `D3≈362 mm` 不能因为“看起来像旧模型”就冻结。

---

## 4.16 第3步当前已经真正闭合了什么

### 已有公司/项目依据

```text
球体 Db = φ465 mm
密封角 α = 45°
实际斜面密封带 w = 4～6 mm
DEVLON：Class150压力等级适用
预紧比压 qpre = 2.5 MPa
K1 = 2～3
K3 = 3～5
K4 = 4～6
防火面正常间隙 = 0.8～1 mm
```

### V1几何和人工复算

```text
w = 5 mm
Dc ≈ φ328.80 mm
D9 ≈ φ325.27 mm
D10≈ φ332.34 mm
Aproj ≈ 3652 mm²
Fpre ≈ 9.13 kN / 每侧
```

### 仍不能硬算/硬猜

```text
D11 静压面外径 = ?
DEVLON厚度 = ?
DEVLON嵌入深度 = ?
O形圈具体规格 = ?
K2 = ?
阀座大端外圆 = ?
阀座总宽 Wseat = ?
阀座轴向浮动量 xseat = ?
Aeff = ?
Deff = ?
最终弹簧数量/规格 = ?
```

---

# 5. 第3步下一小节：真正应该怎么继续

下一步不要再从“DEVLON厚度”开始猜。

正确顺序改为：

```text
【已经完成】
φ465球体
↓
45°密封几何
↓
D9 / D10
↓
Aproj
↓
qpre = 2.5 MPa
↓
Fpre ≈ 9.13 kN/侧

【下一步】
找公司“必须比压”计算口径
↓
求 D11 静压面外径
↓
明确阀座SPE/DPE及压力方向
↓
选择VITON O形圈具体截面
↓
查K2 / 沟槽深度 / 导角 / 圆角
↓
布置K1/K2/K3/K4
↓
设计DEVLON嵌件保持结构和厚度
↓
加入0.8～1 mm金属防火面间隙
↓
确定阀座大端外圆 / Wseat / xseat
↓
求Aeff / Deff
↓
介质压力推座力 + 弹簧力 + 摩擦
↓
回代密封比压 / 启闭扭矩
↓
固定球阀计算程序复核
```

## 5.1 下一步最缺的资料

按优先级：

1. **公司固定球阀计算程序或输出截图**：最关键。
2. **一份成熟固定球阀阀座计算书**：最好有 D9、D10、D11。
3. **12寸或20寸的阀座剖面/支承圈/密封圈零件图**：只学结构关系，不直接抄尺寸。
4. **具体 DEVLON 牌号数据表**：温度、压缩、蠕变、抗挤出、允许接触应力。
5. **具体 VITON O形圈牌号/硬度和截面**。
6. **阀座功能定义**：SPE、DPE、DIB-1/DBB等。

---

# 6. 已建立的其他基础计算

## 6.1 φ60阀杆纯扭转

```text
τ = 16 × T / (π × d³)
```

结果：

```text
1500 N·m → τ ≈ 35.4 MPa
1800 N·m → τ ≈ 42.4 MPa
```

φ60继续作为候选，最终还要叠加弯矩、最小净截面、驱动头和温度材料准则。

## 6.2 φ303通径压力推力第一轮基线

```text
Ap = π × 303² / 4
   ≈ 72,107 mm²
```

暂按：

```text
ΔP = 2 MPa
```

得到：

```text
F = ΔP × Ap
  ≈ 144.2 kN
```

注意：这是按φ303整圆面积得到的“通径压力推力基线”，**不是阀座最终压力活塞推力**。阀座最终压力推力必须等 D11、O形圈压力边界和SPE/DPE结构明确后重算。

---

# 7. 历史试算与已撤销值

| 项目 | 历史/曾提值 | 当前处理 |
|---|---:|---|
| 对称支承 | Ru=Rl≈72.1 kN | H，等真实支承受力后重算 |
| 假设悬臂 | e=50 mm | H |
| 下轴V0 | φ60 | H |
| 下轴V1 | φ65 | H |
| 轴承V1 | φ70×φ65×50 | H |
| 弹簧数量 | 36根/侧 | H，仅敏感性 |
| DEVLON总厚度 | 12 mm | **撤销：无公司规范/公式依据** |
| DEVLON嵌入深度 | 8 mm | **撤销：无公司规范/公式依据** |
| DEVLON外露层 | 4 mm | **撤销：无公司规范/公式依据** |
| 金属阀座圈外径 | D3≈φ362 mm | **撤销：公司规范应先求D11** |
| 阀座总宽 | Wseat≈35 mm | **撤销：需D11/O形圈/防火面/浮动量后确定** |
| 直接令Dp≈D3求Deff | — | **撤销：压力边界不成立** |

---

# 8. 公式速查

## 球体

```text
Rb = Db / 2

tg = (Db - d) / 2

Rend = √[Rb² - (Wball/2)²]

Dend = 2 × Rend

tedge = (Dend - d) / 2
```

## 驱动与支承敏感性

```text
reff = B / 2

Ft = T / reff

pavg = Ft / (L × H)

Ab = Dlower × Hlower

pb = R / Ab
```

## 阀座密封区

```text
Dc = Db × cos45°

br = w × cos45°

D9  = Dc - br
D10 = Dc + br

Aproj = π × (D10² - D9²) / 4

Fpre = qpre × Aproj

Fspring = Fpre / Ns
```

## 有效受压面积——只能在压力边界明确后使用

```text
Aeff = 实际阀座压力作用面积差

Deff = √(4 × Aeff / π)

Fpressure = ΔP × Aeff
```

> `D11` 的正式公式当前没有从公司规范正文取得，必须等“固定球阀计算程序/受控计算书”后补入，禁止自行编造。

---

# 9. 当前结论——小白只看这一段也能知道做到哪里

现在真正可信的设计链已经变成：

```text
第1层
流道 φ303
    ↓
第2层
F316 + NPS12 + Class150
按公司规范优选球体 φ465
    ↓
第3层
45°软密封
实际密封带 4～6 mm
V1取 5 mm
    ↓
Dc ≈ φ328.80
    ↓
D9 ≈ φ325.27
D10≈ φ332.34
    ↓
Aproj ≈ 3652 mm²
    ↓
公司规定预紧比压 qpre = 2.5 MPa
    ↓
人工复算 Fpre ≈ 9.13 kN / 每侧
    ↓
【当前卡点】
必须比压计算
    ↓
D11 阀座静压面外径
```

所以当前**不要**往下硬写：

```text
DEVLON厚度 = 12
D3 = 362
Wseat = 35
Deff = 196.5
```

这些没有公司计算程序/结构边界支撑，已经从主方案撤销。

下一次计算只聚焦：

> **找到“必须比压 → D11”的公司计算公式或计算程序，把D11算清楚。**

---

# 10. 本次使用的公司规范索引

来源文件：`01-BS固定球阀设计规范(1).docx`  
文件号：`GFE-JS01-2025`  
标题：`BS系列固定球阀设计规范`

| 页码 | 本项目用到的内容 |
|---:|---|
| 第5页 | 规范适用范围；公司设计顺序：基本参数→球体→阀座→阀杆→阀体→阀盖→压盖→支架 |
| 第6～7页 | 通道直径依据 API 6D；固定球结构分类 |
| 第8页 | F304/F316系列球体优选直径表：12"、≤CL300 → φ465 |
| 第10页 | 阀座材料表：DEVLON 150LB～1500LB、-29～120℃、μ=0.08 |
| 第11页 | D9/D10/D11设计顺序；K1/K2/K3/K4；防火面0.8～1mm；阀座大端外圆规则 |
| 第15页 | ≥2"固定球阀圆柱弹簧预紧；预紧比压2.5 MPa |
| 第16页 | O形圈尺寸/沟槽深度/槽宽/圆角；弹簧设计程序说明 |

**文件状态：从内到外主线 / 已按 GFE-JS01-2025 校正阀座章节 / 下一步只攻 D11。**