# Q347F 12寸 Class 150 天然气固定球阀——从内到外设计计算主线

> **设计对象**：Q347F 固定球阀，NPS 12 / DN300，Class 150，天然气，全通径，非金属软密封。  
> **设计方法**：总体工况先锁定，机械结构尺寸按 **从内到外** 生长；每向外增加一层，都要回做一次空间、强度、密封和装配校核。  
> **文档目标**：只保留当前有效设计主线。历史 V0/V1 试算放在文末参考区，不再与当前尺寸混在一起。

---

# 0. 先看这张图：整台阀门只按这一条主线设计

```text
第0步  工况/边界条件
DN / Class / 压力 / 温度 / 介质 / 材料 / 扭矩
          ↓
第1步  流道
φ303
          ↓
第2步  球体
球径 / 流道 / 球宽 / 上槽 / 下支承接口
          ↓
第3步  阀座密封副
DEVLON + 金属阀座圈 + 弹簧 + O形圈
          ↓
第4步  上下支承系统
阀杆 + 下支承轴 + 滑动轴承
          ↓
第5步  内部整体空间
球体 + 阀座 + 支承 + 旋转/移动空间
          ↓
第6步  阀体内腔
座腔 / 中腔 / 上下支承腔
          ↓
第7步  阀体外壁
由承压、局部强度、铸造和加工余量向外包
          ↓
第8步  左右端盖 / 阀盖
从阀座和流道向标准法兰连接
          ↓
第9步  底盖 / 上盖 / 填料函
支承、密封、防吹出、填料结构
          ↓
第10步 标准法兰
NPS 12 / Class 150 / RF 外部接口
          ↓
第11步 紧固与密封附件
螺栓 / O形圈 / 垫片 / 防火石墨
          ↓
第12步 外部驱动
连接盘 / 蜗轮箱
          ↓
第13步 总装
干涉 / 间隙 / 公差 / 装拆 / 90°旋转
          ↓
第14步 最终校核
压力 / 密封 / 扭矩 / 强度 / 温度 / FEA
```

## 0.1 “从内到外”真正是什么意思

不是先把外壳画出来，再往里面塞零件。

正确关系是：

```text
流道
  ↓
球体
  ↓
阀座
  ↓
支承
  ↓
内部腔体
  ↓
阀体外壁
```

但同时还存在一个外部标准边界：

```text
内部结构：φ303 → 球体 → 阀座 → 阀体

外部接口：NPS12 / Class150 / RF 标准法兰
```

最后由阀体、端盖把“内部结构”和“标准外部接口”连接起来。

所以更准确的设计原则是：

> **内部结构从内向外生长，同时受压力等级、结构长度、标准法兰等外部边界约束。**

---

# 1. 参数状态规则——以后避免文档再次混乱

以后所有参数只允许有4种状态：

| 状态 | 含义 | 例子 |
|---|---|---|
| **A 已确认输入** | 当前设计基线，不因试算随意修改 | DN300、Class150、φ303流道 |
| **B 公式计算值** | 由已知输入计算，输入变化必须重算 | Rb=232.5、tg=81、F≈144.2kN |
| **C 设计候选值** | 可以用于下一轮设计，但尚未冻结制造 | 球体φ465、阀杆φ60 |
| **D 待确定** | 前置条件不足，明确写“?” | 阀座总宽、真实a/b/e、下轴最终长度 |

> **规则：前置条件不够时直接写 `?`，不再为了继续往下算而凭空补尺寸。**

---

# 2. 当前总状态——我们已经走到哪里

| 层级 | 核心指标 | 当前值 | 状态 |
|---|---|---:|---|
| 工况 | 公称尺寸 | NPS 12 / DN300 | A |
| 工况 | 压力等级 | Class 150 | A |
| 工况 | 介质 | 天然气 | A |
| 工况 | 结构 | 固定球 | A |
| 工况 | 流道形式 | 全通径 | A |
| 工况 | 设计压力 | 2 MPa | A |
| 工况 | 最大关闭压差 | 暂按2 MPa | C，待正式工况确认 |
| 材料 | 阀体 | ASTM A216 WCB | A |
| 材料 | 球体 | ASTM A182 F316 | A |
| 材料 | 阀杆 | ASTM A182 F51 | A |
| 材料 | 阀座密封材料 | DEVLON | A，具体牌号待补 |
| 材料 | O形圈 | VITON | A，具体牌号待补 |
| 第1层 | 流道 | φ303 mm | A |
| 第2层 | 球体外径 | φ465 mm | C |
| 第2层 | 球体半径 | 232.5 mm | B |
| 第2层 | 流道半径 | 151.5 mm | B |
| 第2层 | 基础径向几何空间 | 81 mm | B |
| 第2层 | 球体总宽 | ? | D |
| 第2层 | 上驱动接口最终尺寸 | ? | D |
| 第2层 | 下支承接口最终尺寸 | ? | D |
| 第3层 | 阀座密封面直径 | ? | D，**当前下一步** |
| 第3层 | DEVLON密封带宽 | ? | D，**当前下一步** |
| 第3层 | 金属阀座圈尺寸 | ? | D，**当前下一步** |
| 第3层 | 阀座总成轴向宽度 | ? | D，**当前下一步** |
| 第4层 | 阀杆有效直径 | φ60 mm | C |
| 第4层 | 下支承轴最终尺寸 | ? | D |
| 第4层 | 滑动轴承最终规格 | ? | D |
| 第5层以后 | 阀体座腔 / 中腔 / 外壁 / 底盖 | ? | D |

## 当前进度一句话

> **第1步流道已经锁住，第2步球体基础尺寸已有第一轮；现在应该进入第3步“阀座密封副”，而不是继续提前猜底盖和下轴总长度。**

---

# 3. 第0步：先锁工况和外部边界

这一步不是机械尺寸设计，但所有后续尺寸都受它控制。

当前已经明确：

```text
阀门：Q347F 固定球阀
尺寸：NPS 12 / DN300
压力等级：Class 150
介质：天然气
流道：全通径
设计压力：2 MPa
球体：A182 F316
阀杆：A182 F51
阀座密封：DEVLON
O形圈：VITON
```

当前仍需最终补齐：

```text
设计温度 / 最低设计温度 = ?
1800 N·m 的正式定义 = ?
DEVLON具体牌号 = ?
VITON具体牌号 = ?
金属阀座圈材料 = ?
滑动轴承材料 = ?
```

这些不妨碍先做几何主线，但在最终强度、温度和密封放行前必须补齐。

---

# 4. 第1步：流道——整个内部结构的第一基准

当前全通径：

```text
d = 303 mm
```

这个尺寸会向外依次影响：

```text
φ303流道
  ↓
球体孔径
  ↓
阀座内径
  ↓
端盖流道
  ↓
阀体流道
```

所以后续内部零件都必须围绕 `φ303` 建立。

当前结论：

> **流道φ303作为当前结构设计控制值继续向外传递。**

---

# 5. 第2步：球体——第二层核心结构

当前球体候选外径：

```text
Db = 465 mm
```

流道：

```text
d = 303 mm
```

## 5.1 球体半径

```text
Rb = Db / 2
   = 465 / 2
   = 232.5 mm
```

## 5.2 流道半径

```text
r = d / 2
  = 303 / 2
  = 151.5 mm
```

## 5.3 球面到流道边缘的基础径向空间

```text
tg = (Db - d) / 2
```

代入：

```text
tg = (465 - 303) / 2
   = 81 mm
```

所以：

```text
球体φ465
流道φ303
基础径向几何空间 = 81 mm
```

注意：

```text
81 mm ≠ 最终球体净壁厚
```

因为后续还要扣除：

- 上驱动槽；
- 下支承孔；
- 局部圆角；
- 加工余量；
- 局部承力区。

## 5.4 球体这一层最终必须输出的指标

| 球体指标 | 当前状态 |
|---|---|
| 流道φ303 | 已确认 |
| 球体外径φ465 | 第一轮候选 |
| 球体半径R232.5 | 已计算 |
| 球体总宽 | 待定 |
| 球面有效密封区 | 要与阀座一起确定 |
| 上驱动槽 | 先保留历史试算，不冻结 |
| 下支承孔 | 等支承系统反推，不冻结 |

也就是说：

> **球体主体先定住，球体的“上下接口”允许等阀座和支承系统确定后再回填。**

---

# 6. 第3步：阀座密封副——现在真正要做的下一步

球体确定后，马上设计阀座。

固定球阀的密封关系可以理解成：

```text
金属阀座圈
    ↓
DEVLON Seat Insert
    ↓
压向φ465球面
```

阀座这一层至少要确定以下指标：

| 指标 | 符号 | 当前值 |
|---|---:|---:|
| 阀座密封面内径 | D2 | ? |
| 阀座密封面外径 | D1 | ? |
| 密封带宽 | b | ? |
| 阀座圈配合外径 | D3 | ? |
| 阀座内部有效通径 | D4 | 至少围绕φ303建立 |
| 有效受压直径 | Deff | ? |
| DEVLON截面宽度/厚度 | — | ? |
| 金属阀座圈轴向宽度 | — | ? |
| 阀座总成轴向宽度 | Wseat | ? |
| 弹簧数量/布置圆 | — | ? |
| O形圈规格/槽 | — | ? |
| 阀座轴向浮动量 | xseat | ? |

## 6.1 密封带宽基本关系

如果密封面内径为 `D2`、外径为 `D1`：

```text
b = (D1 - D2) / 2
```

这一步不是为了先追求一个“漂亮数字”，而是要回答：

1. DEVLON到底压在球体哪个球面区域；
2. 密封带宽是否足够；
3. 阀座圈要多大；
4. 阀座移动时需要多少轴向空间；
5. O形圈和弹簧布在哪里；
6. 阀座总成最终会占用多大外径和宽度。

## 6.2 为什么这一步会决定后面的阀体

尺寸传递关系：

```text
球体φ465
    ↓
阀座密封直径
    ↓
DEVLON尺寸
    ↓
金属阀座圈
    ↓
阀座总成外径和总宽
    ↓
阀体座腔尺寸
```

所以：

> **阀座总成外径和宽度出来以后，阀体内部腔体才第一次有可靠依据。**

---

# 7. 第4步：上/下支承系统——球体定了以后向上下生长

球体支承关系：

```text
       上：阀杆 / 上支承
              ↑
            球体
              ↓
       下：下支承轴 / 滑动轴承
```

这一层要确定：

```text
上阀杆最小有效直径
上支承位置
下支承轴直径
滑动轴承内径/外径/长度
球心到上下支承中心距离 a、b
局部等效悬臂距离 e
```

## 7.1 当前阀杆纯扭转第一轮结果

当前候选：

```text
ds = 60 mm
```

实心圆轴扭转公式：

```text
τ = 16 × T / (π × ds³)
```

1500 N·m：

```text
T = 1,500,000 N·mm

τ1500
= 16 × 1,500,000 / (π × 60³)
≈ 35.4 MPa
```

1800 N·m：

```text
T = 1,800,000 N·mm

τ1800
= 16 × 1,800,000 / (π × 60³)
≈ 42.4 MPa
```

当前只允许写：

> **φ60阀杆可以继续作为候选，最终还要叠加弯矩、实际最小截面、台阶/驱动头和温度材料准则。**

## 7.2 下支承轴为什么现在不能先锁φ60或φ65

下轴弯曲必须知道真实载荷和真实悬臂距离：

```text
M = R × e
```

弯曲应力：

```text
σb = 32 × M / (π × d³)
```

反求轴径：

```text
d_min = [32 × R × e / (π × σallow)]^(1/3)
```

因此：

```text
真实R = ?
真实e = ?
↓
下轴最终直径 = ?
```

之前出现过的 `φ60、φ65` 只保留为敏感性试算，不再提前冻结。

---

# 8. 第5步：把球体、阀座、支承放在一起——内部空间骨架

这一层第一次建立真正的整阀内部空间。

要同时放进去：

```text
球体
+
左右阀座总成
+
阀座轴向移动量
+
上阀杆支承
+
下支承轴/轴承
+
90°球体旋转包络
+
装拆空间
```

最终要得到：

```text
a = 球心 → 上支承反力中心
b = 球心 → 下支承反力中心

球体左右所需最小空间 = ?
球体上下所需最小空间 = ?
阀座总成占用空间 = ?
90°旋转包络 = ?
```

这一步完成后，支承真实载荷和阀体内腔才可以正式收敛。

---

# 9. 第6步：由内部零件反推阀体内腔

阀体内腔不是先画一个大空腔。

它由下面这些东西共同“撑出来”：

```text
球体φ465
+
左右阀座外径
+
阀座移动量
+
弹簧/O形圈空间
+
球体旋转空间
+
上下支承空间
+
装拆空间
```

因此需要确定：

```text
阀体座腔直径 = ?
中腔最大内径 = ?
上支承孔/腔 = ?
下支承孔/腔 = ?
左右过流连接腔 = ?
```

---

# 10. 第7步：由内腔向外包阀体壁厚

有了真实内腔以后，才开始向外确定阀体外轮廓。

不能只用一个圆筒薄壁公式定最终阀体厚度。

至少要同时满足：

- 压力强度；
- 规范最小壁厚；
- 阀座腔开孔削弱；
- 上阀杆孔削弱；
- 下支承孔削弱；
- 中腔局部应力；
- 铸造圆角；
- 腐蚀/加工余量；
- 变形对密封的影响。

关系是：

```text
阀体内腔尺寸
    ↓
最小承压厚度
    ↓
局部加强
    ↓
铸造/加工余量
    ↓
阀体外轮廓
```

---

# 11. 第8～10步：端盖、底盖、填料函和标准法兰

## 11.1 左右端盖 / 阀盖

端盖内部从阀座和φ303流道向外生长：

```text
阀座
  ↓
端盖内孔
  ↓
端盖承压壁
  ↓
标准RF法兰
```

## 11.2 底盖 / 上部填料函

底盖必须等下支承轴和滑动轴承确定以后再反推：

```text
下支承轴
  ↓
滑动轴承
  ↓
轴承座
  ↓
底盖密封
  ↓
底盖厚度和螺栓
```

所以下轴总长、台阶、止推垫、底盖孔都属于这一阶段，不应提前冻结。

## 11.3 标准法兰

法兰属于外部标准边界：

```text
NPS 12
Class 150
RF
```

其外径、PCD、孔数、孔径、厚度、RF等应由正式采用的法兰标准确定，而不是由内部尺寸自由设计。

---

# 12. 第11～12步：紧固、密封附件和蜗轮箱

内部主体稳定后，再补齐：

```text
螺柱/螺母
O形圈
缠绕垫
防火石墨
填料
注脂结构
放空/排泄
```

驱动链：

```text
球体
  ↓
阀杆
  ↓
连接盘
  ↓
蜗轮箱
```

蜗轮箱输出扭矩必须与最终计算操作扭矩匹配，不能只根据当前1800 N·m输入直接冻结。

---

# 13. 第13步：总装尺寸链

到这一阶段才真正计算：

```text
球体总宽
阀座左右位置
阀座压缩/浮动量
阀杆轴向定位
下支承轴总长
轴承位置
止推位置
底盖深度
端盖位置
法兰到法兰总长
```

同时检查：

- 90°旋转干涉；
- 阀座退让空间；
- 拆装路线；
- 轴端间隙；
- 热膨胀；
- 公差叠加；
- 密封压缩量。

> **总长度、台阶长度和底盖深度必须由装配尺寸链得到，而不是先猜一个总长再反推。**

---

# 14. 第14步：最终压力、密封、扭矩和强度回代

所有结构尺寸确定后统一回代。

## 14.1 球体压力推力第一轮基线

当前简化按φ303流道面积：

```text
Ap = π × d² / 4
```

代入：

```text
Ap = π × 303² / 4
   ≈ 72,107 mm²
```

暂按：

```text
ΔP = 2 MPa = 2 N/mm²
```

得到：

```text
F = ΔP × Ap
  = 2 × 72,107
  ≈ 144,213 N
  ≈ 144.2 kN
```

这个值目前只作为第一轮载荷量级。

阀座 `Deff` 确定后，要按真实有效受压面积重新计算。

## 14.2 上下支承反力

如果球心到上、下支承中心距离分别为 `a、b`：

```text
Ru + Rl = F
```

并有：

```text
Ru = F × b / (a + b)

Rl = F × a / (a + b)
```

只有当：

```text
a = b
```

才可简化为：

```text
Ru = Rl = F / 2
   ≈ 72.1 kN
```

所以当前 `72.1 kN / 72.1 kN` 仅为对称假设，不是最终设计载荷。

---

# 15. 当前真正的下一步工作卡

现在不要继续做底盖，也不要继续定下轴总长。

## 下一步：完成第3步“阀座密封副”

必须先得到：

```text
1. D2 = 阀座密封面内径
2. D1 = 阀座密封面外径
3. b  = DEVLON有效密封带宽
4. D3 = 金属阀座圈/支承圈外径
5. D4 = 阀座内部有效通径
6. Deff = 阀座有效受压直径
7. Wseat = 阀座总成轴向宽度
8. xseat = 阀座需要的轴向浮动/移动量
9. 弹簧布置
10. O形圈位置和槽
```

完成以后，尺寸才能继续按下面方向生长：

```text
球体φ465
   ↓
阀座密封副
   ↓
阀座总成外径/总宽
   ↓
上/下支承与整体空间
   ↓
阀体座腔
   ↓
阀体外壁
```

---

# 16. 历史试算参考——不作为当前冻结尺寸

为了保留设计过程，下面只记录曾经做过的敏感性试算，不进入当前尺寸主线。

| 历史试算 | 曾用值 | 现在怎么处理 |
|---|---:|---|
| 上驱动槽V1 | 50×70×35 mm | 只保留为传扭计算示例 |
| 假设支承对称 | Ru=Rl≈72.1 kN | 等a、b确定后重算 |
| 假设悬臂 | e=50 mm | 真实结构出来后重算 |
| 下轴V0 | φ60 | 不冻结 |
| 下轴V1 | φ65 | 不冻结 |
| 轴承V1 | φ70×φ65×50 | 不冻结 |
| 球体下孔V1 | φ70×40深、R4 | 不冻结 |

这些试算的意义是帮助理解公式和尺寸敏感性，而不是直接变成制造图尺寸。

---

# 17. 公式速查

球体半径：

```text
Rb = Db / 2
```

基础径向几何空间：

```text
tg = (Db - d) / 2
```

圆面积：

```text
A = π × d² / 4
```

压力推力：

```text
F = ΔP × A
```

上下支承反力：

```text
Ru = F × b / (a + b)

Rl = F × a / (a + b)
```

圆轴纯扭：

```text
τ = 16 × T / (π × d³)
```

弯矩：

```text
M = R × e
```

圆轴弯曲：

```text
σb = 32 × M / (π × d³)
```

弯曲 + 扭转等效应力：

```text
σvm = √(σb² + 3τ²)
```

滑动轴承平均面压：

```text
p = R / (d × L)
```

阀座密封带宽：

```text
b = (D1 - D2) / 2
```

---

# 18. 当前结论

现在整套设计主线已经重新统一为：

> **工况先定 → 流道 → 球体 → 阀座 → 上下支承 → 内部整体空间 → 阀体内腔 → 阀体外壁 → 端盖/底盖 → 标准法兰 → 附件/驱动 → 总装 → 最终校核。**

当前已经完成：

```text
工况主体基线
↓
φ303流道
↓
φ465球体第一轮
↓
球体基础几何
↓
压力推力量级
↓
φ60阀杆纯扭第一轮
```

当前真正应该继续的是：

```text
第3步：阀座密封副
```

下一版文档将围绕阀座展开，不再跳到外层底盖和阀体尺寸。

---

**文件状态：从内到外主线整理版 / 当前有效设计逻辑已重构 / 历史V0-V1仅保留为参考。**
